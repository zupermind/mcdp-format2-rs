//! Placeholder smoke test so `cargo nextest run` finds at least one test and does
//! not fail CI with "no tests to run" (exit code 4). Replace with real behavioral
//! tests as the crate matures.

#[test]
fn smoke() {}
