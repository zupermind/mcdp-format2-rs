# autogenerated
openapi: "3.1.1"
info:
  title: "MCDP Format 2 schema"
  version: "2.19.1"
  license:
    name: "Apache-2.0"
    url: "https://www.apache.org/licenses/LICENSE-2.0.html"
servers: []
components:
  schemas:
    Address:
      title: "Specifies the origin of an object from a repo and a library."
      type: "object"
      properties:
        type:
          type: "string"
          enum:
          - "Address"
          description: "Type marker"
        repo:
          description: "The Git repository URL"
          type: "string"
        library:
          description: "The library name"
          type: "string"
        spec:
          description: "The type of object"
          type: "string"
          enum:
          - "models"
          - "templates"
          - "values"
          - "posets"
          - "primitivedps"
          - "interfaces"
          - "queries"
        thing:
          description: "The name of the object"
          type: "string"
      required:
      - "type"
      - "library"
      - "spec"
      - "thing"
      x-latex-schema:
        top_level: false
        group: "misc"
    Check:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Checks for the maps, as used in test cases."
        properties:
          type:
            type: "string"
            enum:
            - "L1Check"
            - "LCheck"
            - "MapCheck"
            - "SL1Check"
            - "SLCheck"
            - "SU1Check"
            - "SUCheck"
            - "U1Check"
            - "UCheck"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "Check"
            enum:
            - "Check"
            default: "Check"
        x-latex-schema:
          top_level: true
          order: 1000
          group: "misc"
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            L1Check: "#/components/schemas/L1Check"
            LCheck: "#/components/schemas/LCheck"
            MapCheck: "#/components/schemas/MapCheck"
            SL1Check: "#/components/schemas/SL1Check"
            SLCheck: "#/components/schemas/SLCheck"
            SU1Check: "#/components/schemas/SU1Check"
            SUCheck: "#/components/schemas/SUCheck"
            U1Check: "#/components/schemas/U1Check"
            UCheck: "#/components/schemas/UCheck"
    ComputeJob:
      allOf:
      - $ref: "#/components/schemas/Root"
      - description: |
          Computation jobs 
        properties:
          address:
            $ref: "#/components/schemas/Address"
          axes:
            type: "object"
            additionalProperties:
              type: "integer"
            description: |
              The names of the axes of the computation job.
          points:
            type: "array"
            items:
              $ref: "#/components/schemas/ComputePoint"
          type:
            type: "string"
            enum:
            - "ComputeJob_F_to_R"
            - "ComputeJob_R_to_F"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "ComputeJob"
            enum:
            - "ComputeJob"
            default: "ComputeJob"
        x-latex-schema:
          top_level: false
          excluded: true
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            ComputeJob_F_to_R: "#/components/schemas/ComputeJob_F_to_R"
            ComputeJob_R_to_F: "#/components/schemas/ComputeJob_R_to_F"
    ComputeJob_F_to_R:
      allOf:
      - $ref: "#/components/schemas/ComputeJob"
      - properties:
          f_r:
            $ref: "#/components/schemas/SU1Map"
          f_b_r:
            $ref: "#/components/schemas/SUMap"
          f_i_r:
            $ref: "#/components/schemas/SUMap"
          type:
            type: "string"
            const: "ComputeJob_F_to_R"
            enum:
            - "ComputeJob_F_to_R"
            default: "ComputeJob_F_to_R"
        type: "object"
        description: "F to R query"
        required:
        - "f_b_r"
        - "f_i_r"
        - "f_r"
        - "type"
    ComputeJob_R_to_F:
      allOf:
      - $ref: "#/components/schemas/ComputeJob"
      - properties:
          r_f:
            $ref: "#/components/schemas/SL1Map"
          r_b_f:
            $ref: "#/components/schemas/SLMap"
          r_i_f:
            $ref: "#/components/schemas/SLMap"
          type:
            type: "string"
            const: "ComputeJob_R_to_F"
            enum:
            - "ComputeJob_R_to_F"
            default: "ComputeJob_R_to_F"
        type: "object"
        description: "F to R query"
        required:
        - "r_b_f"
        - "r_f"
        - "r_i_f"
        - "type"
    ComputePoint:
      type: "object"
      properties:
        key:
          type: "string"
          description: |
            Key of the point, to be referenced later.
        value:
          description: "Value of the point"
      x-latex-schema:
        top_level: false
        excluded: true
    Connection:
      title: "Represents a connection between two nodes in the NDP graph"
      type: "object"
      properties:
        type:
          type: "string"
          enum:
          - "Connection"
          description: "Type marker."
        source:
          description: "The source of the connection."
          $ref: "#/components/schemas/ConnectionSource"
        target:
          description: "The target of the connection."
          $ref: "#/components/schemas/ConnectionTarget"
      required:
      - "source"
      - "target"
      - "type"
    ConnectionSource:
      properties:
        type:
          type: "string"
          enum:
          - "ModelFunctionality"
          - "NodeRequirement"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          ModelFunctionality: "#/components/schemas/ModelFunctionality"
          NodeRequirement: "#/components/schemas/NodeRequirement"
      title: "The source of a connection."
      required:
      - "type"
    ConnectionTarget:
      properties:
        type:
          type: "string"
          enum:
          - "ModelRequirement"
          - "NodeFunctionality"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          ModelRequirement: "#/components/schemas/ModelRequirement"
          NodeFunctionality: "#/components/schemas/NodeFunctionality"
      title: "The target of a connection."
      required:
      - "type"
    DP:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: |
          Design problem with implementations (DPI)
        properties:
          F:
            $ref: "#/components/schemas/Poset"
            description: "Poset of functionalities"
            x-latex-property:
              symbol: "\\funsp"
              color: "functionalities"
          R:
            $ref: "#/components/schemas/Poset"
            description: "Poset of requirements"
            x-latex-property:
              symbol: "\\ressp"
              color: "requirements"
          B:
            $ref: "#/components/schemas/Poset"
            description: "Poset of blueprints. If not present, it is the smash unit."
            x-latex-property:
              symbol: "\\bpsp"
              color: "blueprints"
          I:
            $ref: "#/components/schemas/Poset"
            description: "Poset of implementations. If not present, it is the smash unit."
            x-latex-property:
              symbol: "\\impsp"
              color: "implementations"
          address:
            description: "Pointer to the entity that generated this object."
            $ref: "#/components/schemas/Address"
          type:
            type: "string"
            enum:
            - "DP_All_Constants_And_F_Leq_R"
            - "DP_All_Constants_Leq_R"
            - "DP_All_Fi_Leq_R"
            - "DP_AmbientConversion"
            - "DP_Any_Constants_Or_F_Leq_R"
            - "DP_Any_Fi_Leq_R"
            - "DP_C_ExplicitApprox"
            - "DP_C_Intersection"
            - "DP_C_Parallel"
            - "DP_C_Series"
            - "DP_C_Trace"
            - "DP_C_Union"
            - "DP_Catalog"
            - "DP_Compiled"
            - "DP_F_Leq_All_Constants"
            - "DP_F_Leq_All_R_And_Constants"
            - "DP_F_Leq_All_Ri"
            - "DP_F_Leq_Any_R_And_Constants"
            - "DP_F_Leq_Any_Ri"
            - "DP_False"
            - "DP_FuncNotMoreThan"
            - "DP_GenericConstant"
            - "DP_Identity"
            - "DP_Iso"
            - "DP_LiftL"
            - "DP_LiftU"
            - "DP_ResNotLessThan"
            - "DP_True"
            - "DP_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "DP"
            enum:
            - "DP"
            default: "DP"
        required:
        - "F"
        - "R"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 110
          color: "color-dp"
          preamble: |
            \definecolor{color-dp}{rgb}{0.6,0.2,0.5}
            \definecolor{functionalities}{rgb}{0.2,0.6,0.2}
            \definecolor{requirements}{rgb}{0.6,0.2,0.2}
            \definecolor{implementations}{rgb}{1.0,0.5,0.0}
            \definecolor{blueprints}{rgb}{1.0,0.5,0.0}
            \newcommand{\funsp}{\mathcal{\color{functionalities}F}}
            \newcommand{\ressp}{\mathcal{\color{requirements}R}}
            \newcommand{\impsp}{\mathcal{\color{implementations}I}}
            \newcommand{\bpsp}{\mathcal{\color{blueprints}B}}
            \newcommand{\fun}{{\color{functionalities}f}}
            \newcommand{\res}{{\color{requirements}r}}
            \newcommand{\imp}{{\color{implementations}i}}
            \newcommand{\bp}{{\color{blueprints}b}}
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            DP_All_Constants_And_F_Leq_R: "#/components/schemas/DP_All_Constants_And_F_Leq_R"
            DP_All_Constants_Leq_R: "#/components/schemas/DP_All_Constants_Leq_R"
            DP_All_Fi_Leq_R: "#/components/schemas/DP_All_Fi_Leq_R"
            DP_AmbientConversion: "#/components/schemas/DP_AmbientConversion"
            DP_Any_Constants_Or_F_Leq_R: "#/components/schemas/DP_Any_Constants_Or_F_Leq_R"
            DP_Any_Fi_Leq_R: "#/components/schemas/DP_Any_Fi_Leq_R"
            DP_C_ExplicitApprox: "#/components/schemas/DP_C_ExplicitApprox"
            DP_C_Intersection: "#/components/schemas/DP_C_Intersection"
            DP_C_Parallel: "#/components/schemas/DP_C_Parallel"
            DP_C_Series: "#/components/schemas/DP_C_Series"
            DP_C_Trace: "#/components/schemas/DP_C_Trace"
            DP_C_Union: "#/components/schemas/DP_C_Union"
            DP_Catalog: "#/components/schemas/DP_Catalog"
            DP_Compiled: "#/components/schemas/DP_Compiled"
            DP_F_Leq_All_Constants: "#/components/schemas/DP_F_Leq_All_Constants"
            DP_F_Leq_All_R_And_Constants: "#/components/schemas/DP_F_Leq_All_R_And_Constants"
            DP_F_Leq_All_Ri: "#/components/schemas/DP_F_Leq_All_Ri"
            DP_F_Leq_Any_R_And_Constants: "#/components/schemas/DP_F_Leq_Any_R_And_Constants"
            DP_F_Leq_Any_Ri: "#/components/schemas/DP_F_Leq_Any_Ri"
            DP_False: "#/components/schemas/DP_False"
            DP_FuncNotMoreThan: "#/components/schemas/DP_FuncNotMoreThan"
            DP_GenericConstant: "#/components/schemas/DP_GenericConstant"
            DP_Identity: "#/components/schemas/DP_Identity"
            DP_Iso: "#/components/schemas/DP_Iso"
            DP_LiftL: "#/components/schemas/DP_LiftL"
            DP_LiftU: "#/components/schemas/DP_LiftU"
            DP_ResNotLessThan: "#/components/schemas/DP_ResNotLessThan"
            DP_True: "#/components/schemas/DP_True"
            DP_Unknown: "#/components/schemas/DP_Unknown"
    DP_All_Constants_And_F_Leq_R:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_All_Constants_And_F_Leq_R"
            enum:
            - "DP_All_Constants_And_F_Leq_R"
            default: "DP_All_Constants_And_F_Leq_R"
        type: "object"
        title: "Compares resources to a function and a set of constants (conjunction)."
        x-latex-schema:
          order: 200
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
                (c_1 \leq \res ) \wedgeDots (c_n \leq \res) \quad \wedge \quad (\fun \leq \res)
            \end{equation*}
        required:
        - "constants"
        - "type"
    DP_All_Constants_Leq_R:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_All_Constants_Leq_R"
            enum:
            - "DP_All_Constants_Leq_R"
            default: "DP_All_Constants_Leq_R"
        type: "object"
        title: "Compare a resource to a set of constants"
        x-latex-schema:
          order: 150
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
            ( c_1 \leq \res)
                \wedgeDots (c_n \leq \res)
            \end{equation*}

            Note: This extended form is needed when the constants are not a lattice.
            In the case of a lattice, the relation would be given by:
            \begin{equation*}
                \bigvee_i c_i \leq \res
            \end{equation*}
            and could be realized using DP\_GenericConstant.
        required:
        - "constants"
        - "type"
    DP_All_Fi_Leq_R:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_All_Fi_Leq_R"
            enum:
            - "DP_All_Fi_Leq_R"
            default: "DP_All_Fi_Leq_R"
        type: "object"
        title: "Compares a vector of functions to a resource (conjunction)."
        x-latex-schema:
          order: 100
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            % 
            \begin{equation*}
                (\fun_1 \leq \res)\wedgeDots (\fun_n \leq \res )
            \end{equation*}
          preamble: |
            \newcommand{\wedgeDots}{\quad\wedge\quad\cdots\quad\wedge\quad}
            \newcommand{\veeDots}{\quad\vee\quad\cdots\quad\vee\quad}
        required:
        - "type"
    DP_AmbientConversion:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          common:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\posC"
              description: |
                The ambient poset.
          type:
            type: "string"
            const: "DP_AmbientConversion"
            enum:
            - "DP_AmbientConversion"
            default: "DP_AmbientConversion"
        type: "object"
        title: "Compares functionality and resources in an ambient poset."
        x-latex-schema:
          group: "g05_first"
          preamble: "\\newcommand{\\posC}{\\mathcal{C}}"
          description: |
            Let $\posC$ be an ambient poset for both $\funsp$ and $\ressp$:
            \begin{align*}
                \funsp &\subseteq \posC \\
                \ressp &\subseteq \posC
            \end{align*}

            Then this DP corresponds to the feasibility relation
            \begin{align*}
                \fun \leq_{\posC} \res
            \end{align*}
          order: 40
        required:
        - "common"
        - "type"
        x-mcdp-dp:
          functors:
            I:
            B: "P_C_ProductSmash([])"
            F:
            R:
            prov:
            req:
            f_r:
            f_b_r:
            f_i_r:
            r_f:
            r_b_f:
            r_i_f:
            i_b:
            i_codfeas:
            i_availability:
    DP_Any_Constants_Or_F_Leq_R:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_Any_Constants_Or_F_Leq_R"
            enum:
            - "DP_Any_Constants_Or_F_Leq_R"
            default: "DP_Any_Constants_Or_F_Leq_R"
        type: "object"
        title: "Compares resources to a function and a set of constants (disjunction)."
        x-latex-schema:
          order: 200
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
               (c_1 \leq \res ) \veeDots (c_n \leq \res)  \quad \vee \quad (\fun \leq \res)
            \end{equation*}
        required:
        - "constants"
        - "type"
    DP_Any_Fi_Leq_R:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_Any_Fi_Leq_R"
            enum:
            - "DP_Any_Fi_Leq_R"
            default: "DP_Any_Fi_Leq_R"
        type: "object"
        title: "Compares a vector of functions to a resource (disjunction)."
        x-latex-schema:
          order: 100
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            % 
            \begin{equation*}
                (\fun_1 \leq \res) \veeDots (\fun_n \leq \res)
            \end{equation*}
        required:
        - "type"
    DP_C_ExplicitApprox:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          optimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
            description: |
              List of optimistic DPs.
          optimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the optimistic DPs.
          pessimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
            description: |
              List of pessimistic DPs.
          pessimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the pessimistic DPs.
          type:
            type: "string"
            const: "DP_C_ExplicitApprox"
            enum:
            - "DP_C_ExplicitApprox"
            default: "DP_C_ExplicitApprox"
        type: "object"
        title: "Multi-resolution DP"
        x-latex-schema:
          group: "g90_special"
          description: |
            This DP assembles a "multi resolution" DP from a list of optimistic and pessimistic DPs.
        examples:
        - kind: "DP"
          type: "DP_C_ExplicitApprox"
          F:
            kind: "Poset"
            type: "P_Decimal"
          R:
            kind: "Poset"
            type: "P_Decimal"
          optimistic:
          - kind: "DP"
            type: "DP_True"
            F:
              kind: "Poset"
              type: "P_Decimal"
            R:
              kind: "Poset"
              type: "P_Decimal"
            value:
              kind: "Value"
              type: "VU"
              value: []
              poset:
                kind: "Poset"
                type: "P_C_ProductSmash"
                subs: []
                naked: []
                ranges: []
          pessimistic:
          - kind: "DP"
            type: "DP_False"
            F:
              kind: "Poset"
              type: "P_Decimal"
            R:
              kind: "Poset"
              type: "P_Decimal"
        required:
        - "optimistic"
        - "pessimistic"
        - "type"
    DP_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          dps:
            description: "A list of design problems (DPs) to be composed."
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels.
          type:
            type: "string"
            const: "DP_C_Intersection"
            enum:
            - "DP_C_Intersection"
            default: "DP_C_Intersection"
        type: "object"
        title: "Intersection of design problems"
        x-latex-schema:
          group: "g30_composition"
          order: 200
        required:
        - "dps"
        - "type"
        x-mcdp-dp:
          params:
          - "dps"
          functors:
            I: "P_C_ProductSmash(I(dps))"
            B: "P_C_ProductSmash(B(dps))"
            prov: "M_C_Series([ M_C_DomSmashCodProd (prov(dps)), M_Meet])"
            req: "M_C_Series([ M_C_DomSmashCodProd (req(dps)), M_Join])"
            f_r: "SU1_C_Intersection(f_r(dps))"
            f_b_r: "SU_C_Intersection(f_b_r(dps))"
            f_i_r: "SU_C_Intersection(f_i_r(dps))"
            r_f: "SL1_C_Intersection(r_f(dps))"
            r_b_f: "SL_C_Intersection(r_b_f(dps))"
            r_i_f: "SL_C_Intersection(r_i_f(dps))"
            i_b: "M_C_ProductSmash(i_b(dps))"
            i_codfeas:
            i_availability:
    DP_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          dps:
            description: "A list of design problems (DPs) to be composed."
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels.
          type:
            type: "string"
            const: "DP_C_Parallel"
            enum:
            - "DP_C_Parallel"
            default: "DP_C_Parallel"
        type: "object"
        title: "Monoidal product of design problems."
        x-latex-schema:
          group: "g30_composition"
        required:
        - "dps"
        - "type"
        x-mcdp-dp:
          params:
          - "dps"
          functors:
            I: "P_C_ProductSmash(I(dps))"
            B: "P_C_ProductSmash(B(dps))"
            F: "P_C_Product(F(dps))"
            R: "P_C_Product(R(dps))"
            prov: "M_C_Parallel(prov(dps))"
            req: "M_C_Parallel(req(dps))"
            f_r: "SU1_C_Parallel(f_r(dps))"
            f_b_r: "SU_C_Parallel(f_b_r(dps))"
            f_i_r: "SU_C_Parallel(f_i_r(dps))"
            r_f: "SL1_C_Parallel(r_f(dps))"
            r_b_f: "SL_C_Parallel(r_b_f(dps))"
            r_i_f: "SL_C_Parallel(r_i_f(dps))"
            i_b: "M_C_ParallelSmash(i_b(dps))"
            i_codfeas:
            i_availability:
    DP_C_Series:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          dps:
            description: "A list of design problems (DPs) to be composed."
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels.
          type:
            type: "string"
            const: "DP_C_Series"
            enum:
            - "DP_C_Series"
            default: "DP_C_Series"
        type: "object"
        title: "Series composition of DPs."
        x-latex-schema:
          group: "g30_composition"
          description: |
            Series composition of DPs.
        required:
        - "dps"
        - "type"
        x-mcdp-dp:
          params:
          - "dps"
          functors:
            F: "F(dps[0)"
            R: "R(dps[-1])"
            I: "P_C_ProductSmash(I(dps))"
            B: "P_C_ProductSmash(B(dps))"
            prov: "M_C_Series([M_TakeRange(..), prov(dps[0]))])"
            req: "M_C_Series([M_TakeRange(..), req(dps[-1]))])"
            i_b: "M_C_ProductSmash(i_b(dps))"
            f_r: "SU1_C_Series(f_r(dps))"
            f_b_r: "SU_C_Series(f_b_r(dps))"
            f_i_r: "SU_C_Series(f_i_r(dps))"
            r_f: "SL1_C_Series(r_f(dps))"
            r_b_f: "SL_C_Series(r_b_f(dps))"
            r_i_f: "SL_C_Series(r_i_f(dps))"
    DP_C_Trace:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          dp:
            $ref: "#/components/schemas/DP"
            description: |
              The design problem that is being traced.
          type:
            type: "string"
            const: "DP_C_Trace"
            enum:
            - "DP_C_Trace"
            default: "DP_C_Trace"
        type: "object"
        title: "Trace of a design problem."
        x-latex-schema:
          group: "g40_composition_single"
        required:
        - "dp"
        - "type"
        x-mcdp-dp:
          params:
          - "dp"
          functors:
            I: "@(*)"
            B: "@(*)"
            F: "@(*)[0]"
            R: "@(*)[0]"
            prov: "M_C_Series([@(*), M_TakeIndex(0, 2)])"
            req: "M_C_Series([@(*), M_TakeIndex(0, 2)])"
            f_r: "SU1_C_Trace(@(*))"
            f_b_r: "SU_C_Trace(@(*))"
            f_i_r: "SU_C_Trace(@(*))"
            r_f: "SL1_C_Trace(@(*))"
            r_b_f: "SL_C_Trace(@(*))"
            r_i_f: "SL_C_Trace(@(*))"
            i_b: "@(*)"
            i_codfeas: "M_C_Series([M_C_Product([prov(*), req(*)]), M_Leq])"
            i_availability: "@(*)"
    DP_C_Union:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          dps:
            description: "A list of design problems (DPs) to be composed."
            type: "array"
            items:
              $ref: "#/components/schemas/DP"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels.
          type:
            type: "string"
            const: "DP_C_Union"
            enum:
            - "DP_C_Union"
            default: "DP_C_Union"
        type: "object"
        title: |
          Union of design problems (DPs).
        x-latex-schema:
          group: "g30_composition"
          order: 200
        required:
        - "dps"
        - "type"
        x-mcdp-dp:
          params:
          - "dps"
          functors:
            F: "P_F_C_Union(F(dps))"
            R: "P_F_C_Union(R(dps))"
            I: "P_C_SumSmash(I(dps))"
            B: "P_C_SumSmash(B(dps))"
            prov: "M_C_CoproductSmash(prov(dps))"
            req: "M_C_CoproductSmash(req(dps))"
            i_b: "M_C_SumSmash(i_b(dps))"
            f_r: "SU1_C_Union(f_r(dps))"
            f_b_r: "SU_C_Union(f_b_r(dps))"
            f_i_r: "SU_C_Union(f_i_r(dps))"
            r_f: "SL1_C_Union(r_f(dps))"
            r_b_f: "SL_C_Union(r_b_f(dps))"
            r_i_f: "SL_C_Union(r_i_f(dps))"
            i_codfeas: "M_C_CoproductSmash(i_codfeas(dps))"
            i_availability: "M_C_CoproductSmash(i_availability(dps))"
    DP_Catalog:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          options:
            description: |
              A list of options that define the design problem.
              Each option is a tuple of functionality, requirement, blueprint, and implementation.
            type: "array"
            items:
              $ref: "#/components/schemas/DP_Catalog_Options"
          type:
            type: "string"
            const: "DP_Catalog"
            enum:
            - "DP_Catalog"
            default: "DP_Catalog"
        type: "object"
        title: "A DP defined explicitly by a set of options."
        x-latex-schema:
          group: "g05_first"
          description: |
            This DP is defined explicitly by a set of options.
            Each option is a tuple of functionality, requirement, blueprint, and implementation.

            The relation is given by:
            %
            \begin{equation*}
            \bigvee_{ (f_j, r_j, b_j, i_j) \in \text{options} } \quad
               ( \fun \leq f_j) \wedge
                (r_j \leq \res)
            \end{equation*}
          order: 50
        required:
        - "options"
        - "type"
    DP_Catalog_Options:
      type: "object"
      title: "One option for the catalog"
      properties:
        f:
          description: "Functionality"
        r:
          description: "Requirement"
        b:
          description: "Blueprint"
        i:
          description: "Implementation"
      required:
      - "f"
      - "r"
      - "b"
      - "i"
      x-latex-property:
        title: "An option in the design problem"
        description: |
          An option in the design problem, where `f` is the functionality, `r` is the requirement, `b` is the blueprint, and `i` is the implementation.
    DP_Compiled:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          f_r:
            $ref: "#/components/schemas/SU1Map"
            description: "The function that returns minimal resources needed to satisfy the requirements."
          f_b_r:
            description: "The function that returns the maximum functionality given the budget of resources as well as the blueprint."
            $ref: "#/components/schemas/SUMap"
          f_i_r:
            description: "The function that returns the maximum functionality given the budget of resources as well as the implementation."
            $ref: "#/components/schemas/SUMap"
          r_f:
            description: "The function that returns the maximum functionality given the budget of requirements."
            $ref: "#/components/schemas/SL1Map"
          r_b_f:
            description: "The function that returns the maximum functionality given the budget of resources as well as the blueprint."
            $ref: "#/components/schemas/SLMap"
          r_i_f:
            description: "The function that returns the maximum functionality given the budget of resources as well as the implementation."
            $ref: "#/components/schemas/SLMap"
          i_b:
            description: "The function that maps implementations to blueprints."
            $ref: "#/components/schemas/MonotoneMap"
          i_codfeas:
            description: "The function that maps implementations to their internal feasibility."
            $ref: "#/components/schemas/MonotoneMap"
          i_availability:
            description: |
              The function that maps implementations to their availability.
            $ref: "#/components/schemas/MonotoneMap"
          prov:
            description: |
              The "provides" map from implementations to functionalities.
            $ref: "#/components/schemas/MonotoneMap"
          req:
            description: |
              The "requires" map from implementations to requirements.
            $ref: "#/components/schemas/MonotoneMap"
          type:
            type: "string"
            const: "DP_Compiled"
            enum:
            - "DP_Compiled"
            default: "DP_Compiled"
        type: "object"
        title: "An \"opaque\" DP defined explicitly by its interface."
        x-latex-schema:
          group: "g90_special"
        required:
        - "f_b_r"
        - "f_i_r"
        - "f_r"
        - "i_availability"
        - "i_b"
        - "i_codfeas"
        - "prov"
        - "r_b_f"
        - "r_f"
        - "r_i_f"
        - "req"
        - "type"
    DP_F_Leq_All_Constants:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_F_Leq_All_Constants"
            enum:
            - "DP_F_Leq_All_Constants"
            default: "DP_F_Leq_All_Constants"
        type: "object"
        title: "Compare a functionality to a set of constants"
        x-latex-schema:
          order: 150
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
            (\fun \leq c_1)
                \wedgeDots (\fun \leq c_n)
            \end{equation*}


            Note: This extended form is needed when the constants are not a lattice.
            In the case of a lattice, the relation would be given by:
            %
            \begin{equation*}
                \fun \leq \bigwedge_i c_i
            \end{equation*}
            %
            and could be realized using DP\_GenericConstant.
        required:
        - "constants"
        - "type"
    DP_F_Leq_All_R_And_Constants:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_F_Leq_All_R_And_Constants"
            enum:
            - "DP_F_Leq_All_R_And_Constants"
            default: "DP_F_Leq_All_R_And_Constants"
        type: "object"
        title: "Compares a functionality to a resource and a set of constants (conjunction)."
        x-latex-schema:
          order: 200
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
                (\fun \leq c_1) \wedgeDots (\fun \leq c_n)  \wedge (\fun \leq \res)
            \end{equation*}
        required:
        - "constants"
        - "type"
    DP_F_Leq_All_Ri:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_F_Leq_All_Ri"
            enum:
            - "DP_F_Leq_All_Ri"
            default: "DP_F_Leq_All_Ri"
        type: "object"
        title: "Compares a vector of resources to a function (conjunction)."
        x-latex-schema:
          order: 100
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            % 
            \begin{equation*}
                (\fun \leq \res_1)\wedgeDots (\fun \leq \res_n)   
            \end{equation*}

            Compare with DP\_F\_Leq\_Any\_Ri which uses disjunctions instead of conjunctions.
        required:
        - "type"
    DP_F_Leq_Any_R_And_Constants:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          constants:
            type: "array"
            items: {}
            minItems: 1
            description: |
              A list of constants c₁, ..., c_n.
            x-latex-property:
              symbol: "c_i"
              description: |
                A list of constants $c_1, \dots, c_n$.
          type:
            type: "string"
            const: "DP_F_Leq_Any_R_And_Constants"
            enum:
            - "DP_F_Leq_Any_R_And_Constants"
            default: "DP_F_Leq_Any_R_And_Constants"
        type: "object"
        title: "Compares a functionality to a resource and a set of constants (disjunction)."
        x-latex-schema:
          order: 200
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            % 
            \begin{equation*}
                (\fun \leq c_1) \veeDots (\fun \leq c_n)  \vee (\fun \leq \res)
            \end{equation*}
        required:
        - "constants"
        - "type"
    DP_F_Leq_Any_Ri:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_F_Leq_Any_Ri"
            enum:
            - "DP_F_Leq_Any_Ri"
            default: "DP_F_Leq_Any_Ri"
        type: "object"
        title: "Compares a vector of resources to a function (disjunction)."
        x-latex-schema:
          order: 100
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
                (\fun \leq \res_1)\veeDots (\fun \leq \res_n)   
            \end{equation*}

            Compare with DP\_F\_Leq\_All\_Ri which uses conjunctions instead of disjunctions.
        required:
        - "type"
    DP_False:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_False"
            enum:
            - "DP_False"
            default: "DP_False"
        type: "object"
        title: |
          The DP that is always false.
        x-latex-schema:
          group: "g05_first"
          description: |
            The relation is always false.
          order: 30
        required:
        - "type"
        x-mcdp-dp:
          functors:
            I:
            B:
            F:
            R:
            prov: "M_Empty"
            req: "M_Empty"
            f_r: "SU1_Exact(U1_Constant({}))"
            f_b_r:
            f_i_r:
            r_f:
            r_b_f:
            r_i_f:
            i_b:
            i_codfeas: "M_Constant(false)"
            i_availability: "M_Constant(false)"
    DP_FuncNotMoreThan:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          limit:
            x-latex-property:
              symbol: "L"
              description: |
                The limit for the functionality.
          type:
            type: "string"
            const: "DP_FuncNotMoreThan"
            enum:
            - "DP_FuncNotMoreThan"
            default: "DP_FuncNotMoreThan"
        type: "object"
        title: "Identity with limit to the functionality."
        x-latex-schema:
          order: 50
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
                (\fun \leq L) \quad \wedge \quad (\fun \leq \res)
            \end{equation*}
        required:
        - "limit"
        - "type"
    DP_GenericConstant:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          b_value:
            x-latex-property:
              symbol: "b"
              description: |
                The value of blueprint.
          lower_set:
            $ref: "#/components/schemas/LowerSet"
            x-latex-property:
              symbol: "L"
              description: |
                The lower set of functionalities that are compatible.
          upper_set:
            $ref: "#/components/schemas/UpperSet"
            x-latex-property:
              symbol: "U"
              description: |
                The upper set of resources that are compatible.
          type:
            type: "string"
            const: "DP_GenericConstant"
            enum:
            - "DP_GenericConstant"
            default: "DP_GenericConstant"
        type: "object"
        title: |
          A DP with exactly one implementation.
        x-latex-schema:
          group: "g05_first"
          description: |
            The relation is given by:
            % 
            \begin{equation}
               ( \fun \in L) \quad \wedge \quad (\res \in U)
            \end{equation}
        required:
        - "b_value"
        - "lower_set"
        - "type"
        - "upper_set"
    DP_Identity:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_Identity"
            enum:
            - "DP_Identity"
            default: "DP_Identity"
        type: "object"
        title: "The identity design problem."
        x-latex-schema:
          group: "g05_first"
          order: 10
        required:
        - "type"
        x-mcdp-dp:
          params:
          - "F"
          functors:
            I: "P_C_ProductSmash([P_C_Product([F, P_C_Opposite[R]])])"
            B: "P_C_ProductSmash([])"
            F: "given"
            R: "F"
            prov: "M_C_Series([M_Unlift, M_TakeIndex(0)])"
            req: "M_C_Series([M_Unlift, M_TakeIndex(1)])"
            f_r: "SU1_Exact(U1_Lift(M_Id(F)))"
            f_b_r: "SU_L_Lift1_Constant(SU1_Exact(U1_Lift(M_Id(F))))"
            f_i_r: "SU_L_Lift1_Constant(SU1_Exact(U1_Lift(M_Id(F))))"
            r_f: "SL1_Exact(L1_Lift(M_Id(R)))"
            r_b_f: "SL_L_Lift1_Constant(SL1_Exact(L1_Lift(M_Id(R))))"
            r_i_f: "SL_L_Lift1_Constant(SL1_Exact(L1_Lift(M_Id(R))))"
            i_b: "M_Constant([])"
            i_codfeas: "M_Constant(true)"
            i_availability:
    DP_Iso:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          fwd:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "\\alpha"
              description: |
                A monotone map from the poset of functionalities to the poset of requirements.
          bwd:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "\\beta"
              description: |
                A monotone map from the poset of requirements to the poset of functionalities.
          type:
            type: "string"
            const: "DP_Iso"
            enum:
            - "DP_Iso"
            default: "DP_Iso"
        type: "object"
        title: "Enforces isomorphism between functionalities and requirements."
        x-latex-schema:
          group: "g10_basic"
          preamble: |
            \newcommand{\id}{\mathrm{id}}
          description: |
            The isomorphism is defined by the two monotone maps $\alpha: \funsp \to \ressp$ and $\beta: \ressp \to \funsp$ such that $\alpha \circ \beta = \id$ and $\beta \circ \alpha = \id$.
        required:
        - "bwd"
        - "fwd"
        - "type"
    DP_LiftL:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map from the poset of requirements to the poset of functionalities.
          type:
            type: "string"
            const: "DP_LiftL"
            enum:
            - "DP_LiftL"
            default: "DP_LiftL"
        type: "object"
        title: "A DP generated from a monotone map from requirements to functionalities."
        x-latex-schema:
          group: "g10_basic"
          description: |
            The relation is 
            \begin{equation}
                \fun \leq m(\res)
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-dp:
          params:
          - "m"
          functors:
            I:
            B:
            F:
            R:
            prov:
            req:
            f_b_r:
            f_i_r:
            r_f: "L1_Lift(*)"
            r_b_f:
            r_i_f:
            i_b:
            i_codfeas:
            i_availability:
    DP_LiftU:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            description: |
              A monotone map from the poset of functionalities to the poset of requirements.
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map from the poset of functionalities to the poset of requirements.
                This is used to lift a DP from the requirements to the functionalities.
          type:
            type: "string"
            const: "DP_LiftU"
            enum:
            - "DP_LiftU"
            default: "DP_LiftU"
        type: "object"
        title: "A DP generated from a monotone map from functionality to requirements."
        x-latex-schema:
          group: "g10_basic"
          description: |
            The relation is
            \begin{equation}
                m(\fun) \leq \res
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-dp:
          params:
          - "m"
          functors:
            I:
            B:
            F:
            R:
            prov:
            req:
            f_r: "U1_Lift(*)"
            f_b_r:
            f_i_r:
            r_b_f:
            r_i_f:
            i_b:
            i_codfeas:
            i_availability:
    DP_ResNotLessThan:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          limit:
            x-latex-property:
              symbol: "L"
              description: |
                The limit for the resource.
          type:
            type: "string"
            const: "DP_ResNotLessThan"
            enum:
            - "DP_ResNotLessThan"
            default: "DP_ResNotLessThan"
        type: "object"
        title: "Identity with limit to the resource."
        x-latex-schema:
          order: 50
          group: "g60_non_lattices"
          description: |
            The relation is given by:
            %
            \begin{equation*}
                (\fun \leq \res) \quad \wedge \quad (L \leq \res)
            \end{equation*}
        required:
        - "limit"
        - "type"
    DP_True:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          value:
            description: |
              The implementation value.
            $ref: "#/components/schemas/Value"
          type:
            type: "string"
            const: "DP_True"
            enum:
            - "DP_True"
            default: "DP_True"
        type: "object"
        title: "The DP that is always true."
        x-latex-schema:
          group: "g05_first"
          description: |
            The relation is always true.
          order: 20
          examples_description:
          - |
            This is a DP which is always true, whose blueprint value is $\langle 15 \rangle$.
        examples:
        - kind: "DP"
          type: "DP_True"
          F:
            kind: "Poset"
            type: "P_C_Product"
            subs: []
          R:
            kind: "Poset"
            type: "P_C_Product"
            subs: []
          I:
            kind: "Poset"
            type: "P_C_ProductSmash"
            subs: []
          B:
            kind: "Poset"
            type: "P_C_ProductSmash"
            subs:
            - kind: "Poset"
              type: "P_Decimal"
            naked: true
            ranges: "..."
          value:
            kind: "Value"
            type: "VU"
            poset:
              kind: "Poset"
              type: "P_C_ProductSmash"
              subs:
              - kind: "Poset"
                type: "P_Decimal"
              naked: true
              ranges: "..."
            value:
            - 15
        required:
        - "type"
        - "value"
        x-mcdp-dp:
          params:
          - "F"
          - "R"
          - "B"
          - "value"
          functors:
            I: "P_C_ProductSmash([])"
            B: "given"
            F: "given"
            R: "given"
            prov: "M_C_Series([M_Unlift, M_TakeIndex(0)])"
            req: "M_C_Series([M_Unlift, M_TakeIndex(1)])"
            f_r: "SU1_Exact(U1_Entire(R))"
            f_b_r: "SU_L_Lift1_Constant(SU1_Exact(U1_Entire(R)),value)"
            f_i_r: "SU_L_Lift1_Constant(SU1_Exact(U1_Entire(R)),[])"
            r_f: "SL1_Exact(L1_Entire(F))"
            r_b_f: "SL_L_Lift1_Constant(SL1_Exact(L1_Entire(F)),value)"
            r_i_f: "SL_L_Lift1_Constant(SL1_Exact(L1_Entire(F)),[])"
            i_b: "M_Constant(value)"
            i_codfeas: "M_Constant(true)"
            i_availability: "M_Constant(true)"
    DP_Unknown:
      allOf:
      - $ref: "#/components/schemas/DP"
      - properties:
          type:
            type: "string"
            const: "DP_Unknown"
            enum:
            - "DP_Unknown"
            default: "DP_Unknown"
        type: "object"
        title: "Placeholder for an unknown design problem."
        x-latex-schema:
          group: "g90_special"
        required:
        - "type"
    L1Check:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Data to check the L1Map."
            type: "array"
            items:
              $ref: "#/components/schemas/L1Check_Data"
          type:
            type: "string"
            const: "L1Check"
            enum:
            - "L1Check"
            default: "L1Check"
        type: "object"
        title: "Check for a L1Map."
        required:
        - "data"
        - "m"
        - "type"
    L1Check_Data:
      type: "object"
      title: "An input-output pair for the L1Map"
      properties:
        x:
          x-latex-property:
            symbol: "x"
            description: |
              The input to the L1Map.
        y:
          $ref: "#/components/schemas/LowerSet"
          description: "Expected result (a lower set)."
        elapsed:
          type: "number"
          description: "Time taken for the check in seconds (optional)."
      required:
      - "x"
      - "y"
      x-latex-property:
        description: "An input-output pair"
    L1Map:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Map to lower sets of functionalities."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkdom"
              description: |
                Kleisli domain of the map
              color: "requirements"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkcod"
              description: |
                Kleisli co-domain of the map
              color: "functionalities"
          type:
            type: "string"
            enum:
            - "L1_C_CodSum"
            - "L1_C_CodSumSmash"
            - "L1_C_DomUnion"
            - "L1_C_Intersection"
            - "L1_C_Parallel"
            - "L1_C_ProdIntersection"
            - "L1_C_Product"
            - "L1_C_RefineDomain"
            - "L1_C_Series"
            - "L1_C_Trace"
            - "L1_C_Union"
            - "L1_C_WrapUnits"
            - "L1_Catalog"
            - "L1_Constant"
            - "L1_Entire"
            - "L1_Explicit"
            - "L1_FromFilter"
            - "L1_Identity"
            - "L1_IntersectionOfPrinLowerSets"
            - "L1_InvMul_Opt"
            - "L1_InvMul_Pes"
            - "L1_InvSum_Opt"
            - "L1_InvSum_Pes"
            - "L1_L_Linv"
            - "L1_Lift"
            - "L1_RepresentPrincipalLowerSet"
            - "L1_TopAlternating"
            - "L1_UnionOfPrinLowerSets"
            - "L1_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "L1Map"
            enum:
            - "L1Map"
            default: "L1Map"
        required:
        - "kdom"
        - "kcod"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 30
          color: "color-lower"
          preamble: |
            \definecolor{color-lower}{RGB}{245,164,66}
            \definecolor{color-upper}{RGB}{12,12,179}

            \newcommand{\signLOMap}[3]{#1 \stackrel{#3}{\to}_{\text{\textbf{L1Map}}} #2 }
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            L1_C_CodSum: "#/components/schemas/L1_C_CodSum"
            L1_C_CodSumSmash: "#/components/schemas/L1_C_CodSumSmash"
            L1_C_DomUnion: "#/components/schemas/L1_C_DomUnion"
            L1_C_Intersection: "#/components/schemas/L1_C_Intersection"
            L1_C_Parallel: "#/components/schemas/L1_C_Parallel"
            L1_C_ProdIntersection: "#/components/schemas/L1_C_ProdIntersection"
            L1_C_Product: "#/components/schemas/L1_C_Product"
            L1_C_RefineDomain: "#/components/schemas/L1_C_RefineDomain"
            L1_C_Series: "#/components/schemas/L1_C_Series"
            L1_C_Trace: "#/components/schemas/L1_C_Trace"
            L1_C_Union: "#/components/schemas/L1_C_Union"
            L1_C_WrapUnits: "#/components/schemas/L1_C_WrapUnits"
            L1_Catalog: "#/components/schemas/L1_Catalog"
            L1_Constant: "#/components/schemas/L1_Constant"
            L1_Entire: "#/components/schemas/L1_Entire"
            L1_Explicit: "#/components/schemas/L1_Explicit"
            L1_FromFilter: "#/components/schemas/L1_FromFilter"
            L1_Identity: "#/components/schemas/L1_Identity"
            L1_IntersectionOfPrinLowerSets: "#/components/schemas/L1_IntersectionOfPrinLowerSets"
            L1_InvMul_Opt: "#/components/schemas/L1_InvMul_Opt"
            L1_InvMul_Pes: "#/components/schemas/L1_InvMul_Pes"
            L1_InvSum_Opt: "#/components/schemas/L1_InvSum_Opt"
            L1_InvSum_Pes: "#/components/schemas/L1_InvSum_Pes"
            L1_L_Linv: "#/components/schemas/L1_L_Linv"
            L1_Lift: "#/components/schemas/L1_Lift"
            L1_RepresentPrincipalLowerSet: "#/components/schemas/L1_RepresentPrincipalLowerSet"
            L1_TopAlternating: "#/components/schemas/L1_TopAlternating"
            L1_UnionOfPrinLowerSets: "#/components/schemas/L1_UnionOfPrinLowerSets"
            L1_Unknown: "#/components/schemas/L1_Unknown"
    L1_C_CodSum:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_CodSum"
            enum:
            - "L1_C_CodSum"
            default: "L1_C_CodSum"
        type: "object"
        title: "Co-domain sum combination"
        x-latex-schema:
          group: "20_compositions"
          description: |
            Given two maps $f: A \to B$ and $g: A \to C$, the codomain sum of these maps is a map that combines the codomains of both maps:
            %
            \begin{equation}
                SELF(f, g) : A \to P\_C\_Sum(B, C)
            \end{equation}

            It is defined as follows:
            %
            \begin{equation}
                x \mapsto  \{ \text{in}_1 (b) \mid b \in f(x) \}  \cup \{ \text{in}_2 (c) \mid c \in g(x) \}
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_Sum(@(ms))"
    L1_C_CodSumSmash:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_CodSumSmash"
            enum:
            - "L1_C_CodSumSmash"
            default: "L1_C_CodSumSmash"
        type: "object"
        title: "Co-domain (smash) sum combination"
        x-latex-schema:
          group: "20_compositions"
          description: |
            Given two maps $f: A \to B$ and $g: A \to C$, the codomain sum of these maps is a map that combines the codomains of both maps:
            %
            \begin{equation}
                SELF(f, g) : A \to P\_C\_SumSmash(B, C)
            \end{equation}

            It is defined as follows:
            %
            \begin{equation}
                x \mapsto  \{ \text{in}_1 (b) \mid b \in f(x) \}  \cup \{ \text{in}_2 (c) \mid c \in g(x) \}
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_SumSmash(@(ms))"
    L1_C_DomUnion:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_DomUnion"
            enum:
            - "L1_C_DomUnion"
            default: "L1_C_DomUnion"
        type: "object"
        title: "Domain union"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the equivalent of M\_C\_DomUnion.

            Given two maps $f: A \to B$ and $g: C \to B$, the domain union of these maps is a map that combines the domains of both maps:
            %
            \begin{equation}
                SELF(f, g) : P\_F\_C\_Union( A, C) \to B
            \end{equation}

            The value is defined as follows:
            %
            \begin{equation}
                SELF(f, g) : x \mapsto
                \begin{cases}
                    f(x) & \text{if } x \in A \\
                    g(x) & \text{if } x \in C
                \end{cases}
            \end{equation}

            Note that the order of the maps does matter.
            We use the first map whose domain contains the input.
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Union(@(ms))"
            kcod: "P_F_C_Union(@(ms))"
    L1_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_Intersection"
            enum:
            - "L1_C_Intersection"
            default: "L1_C_Intersection"
        type: "object"
        title: "Intersection"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X \to Y$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X  &\to Y \\
                x &\mapsto \bigcap_i m_i(x)
            \end{align*}
          order: 1000
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
    L1_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_Parallel"
            enum:
            - "L1_C_Parallel"
            default: "L1_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X_i \to Y_i$, the monoidal product is defined as:
            %
            \begin{align*}
                SELF( \{ m_i \} ) : P\_C\_Product(\{X_i\}) &\to P\_C\_Product(\{Y_i\}) 
            \end{align*}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
    L1_C_ProdIntersection:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_ProdIntersection"
            enum:
            - "L1_C_ProdIntersection"
            default: "L1_C_ProdIntersection"
        type: "object"
        title: "From product to intersection"
        x-latex-schema:
          group: "20_compositions"
          description: |
            The domain $\lkdom$ should be a product.

            The map is defined as follows:
            %
            \begin{equation}
              \langle x_i \rangle_i \mapsto \bigcap_i f_i(x_i)
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(*))"
            kcod: "P_F_C_Union(@(*))"
    L1_C_Product:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_Product"
            enum:
            - "L1_C_Product"
            default: "L1_C_Product"
        type: "object"
        title: "Product"
        x-latex-schema:
          group: "20_compositions"
          description: |2

            For a family of maps $m_i : X \to Y_i$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X &\to P\_C\_Product(\{Y_i\})
            \end{align*}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_Product(@(ms))"
    L1_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed."
          type:
            type: "string"
            const: "L1_C_RefineDomain"
            enum:
            - "L1_C_RefineDomain"
            default: "L1_C_RefineDomain"
        type: "object"
        title: "Refines the domain of a monotone map."
        x-latex-schema:
          group: "30_comp_single"
          description: ""
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "given"
            kcod: "@(*)"
    L1_C_Series:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_Series"
            enum:
            - "L1_C_Series"
            default: "L1_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
          description: ""
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
    L1_C_Trace:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed."
          type:
            type: "string"
            const: "L1_C_Trace"
            enum:
            - "L1_C_Trace"
            default: "L1_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the trace of the map. 
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "@(*)[0]"
            kcod: "@(*)[0]"
    L1_C_Union:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "L1_C_Union"
            enum:
            - "L1_C_Union"
            default: "L1_C_Union"
        type: "object"
        title: "Union"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X \to Y$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X  &\to Y \\
                x &\mapsto \bigcup_i m_i(x)
            \end{align*}
          order: 1000
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(*))"
            kcod: "P_F_C_Union(@(*))"
    L1_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed."
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: "The units for the domain"
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: "The units for the codomain"
          type:
            type: "string"
            const: "L1_C_WrapUnits"
            enum:
            - "L1_C_WrapUnits"
            default: "L1_C_WrapUnits"
        type: "object"
        title: |
          Decorates a map with units.
        x-latex-schema:
          group: "30_comp_single"
          description: ""
        required:
        - "kcod_units"
        - "kdom_units"
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          - "kdom_units"
          - "kcod_units"
          functors:
            kdom: "P_C_Units(@(m), kdom_units)"
            kcod: "P_C_Units(@(m), kcod_units)"
    L1_Catalog:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          options:
            type: "array"
            items:
              $ref: "#/components/schemas/L1_Catalog_Options"
            x-latex-property:
              symbol: |
                \tup \fun_\idx, \rescj_\idx \rangle
              description: |
                The options in the catalog
          type:
            type: "string"
            const: "L1_Catalog"
            enum:
            - "L1_Catalog"
            default: "L1_Catalog"
        type: "object"
        title: "Map induced by a catalog of options."
        x-latex-schema:
          group: "10_simple"
          description: |
            This map is defined by a catalog of options $\makelist{\tup{\fun_\idx,\rescj_\idx}}$:
            %
            \begin{equation}
                x \mapsto  \bigcup_\idx \{ \downit \fun_\idx \mid x \leq \rescj_\idx \}
            \end{equation}
          order: 100
          preamble: |-
            \newcommand{\downit}{\downarrow}
            \newcommand{\upit}{\uparrow}
        required:
        - "options"
        - "type"
    L1_Catalog_Options:
      type: "object"
      title: "An option in the catalog"
      properties:
        f:
          x-latex-property:
            symbol: "\\fun"
            description: |
              A functionality
        r:
          x-latex-property:
            symbol: "\\rescj"
            description: |
              A resource
      required:
      - "f"
      - "r"
      x-latex-schema:
        description: |
          An option in the catalog, where $f$ is a point in the domain and $r$ is a resource.
    L1_Constant:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          value:
            $ref: "#/components/schemas/LowerSet"
            x-latex-property:
              symbol: "L"
              description: |
                The constant value of the map, which is a lower set $L \subseteq \lkcod$.
          type:
            type: "string"
            const: "L1_Constant"
            enum:
            - "L1_Constant"
            default: "L1_Constant"
        type: "object"
        title: "Constant map"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a constant map, which maps every element of the domain to the same value $L$.
        required:
        - "type"
        - "value"
    L1_Entire:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_Entire"
            enum:
            - "L1_Entire"
            default: "L1_Entire"
        type: "object"
        title: "Returns the entire poset"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is the map defined as 
            %
            \begin{equation*}
                x \mapsto \lkcod
            \end{equation*}

            which maps every element of the domain to the entire codomain poset $\lkcod$.

            This is useful when the codomain doesn't have a compact representation in terms of antichains.
        required:
        - "type"
    L1_Explicit:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          options:
            description: "Pairs of input-output"
            type: "array"
            items:
              $ref: "#/components/schemas/L1_Explicit_Option"
          type:
            type: "string"
            const: "L1_Explicit"
            enum:
            - "L1_Explicit"
            default: "L1_Explicit"
        type: "object"
        title: "Map defined pointwise"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a map defined pointwise, where each option specifies a point in the domain and its corresponding value in the codomain. 
        required:
        - "options"
        - "type"
    L1_Explicit_Option:
      type: "object"
      properties:
        x:
          description: |
            A point in the domain of the map.
        y:
          $ref: "#/components/schemas/LowerSet"
          description: |
            The lower set corresponding to the point `x` in the domain.
      required:
      - "x"
      - "y"
      x-latex-property:
        title: "An option in the map"
        description: |
          An option in the map, where `x` is a point in the domain and `y` is the corresponding lower set.
    L1_FromFilter:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map $m : \lkdom \toinpos \Bool$
          type:
            type: "string"
            const: "L1_FromFilter"
            enum:
            - "L1_FromFilter"
            default: "L1_FromFilter"
        type: "object"
        title: "Filters based on a monotone map."
        x-latex-schema:
          group: "65_lift"
          preamble: |
            \newcommand{\Bool}{\mathbf{Bool}}
          description: |
            Defines the map:
            \begin{equation}
                x \mapsto \begin{cases}
                              \{ x \} & \text{if } m(x)  \\
                              \emptyset & \text{otherwise}
                \end{cases}
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "dom(*)"
            kcod: "dom(*)"
    L1_Identity:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_Identity"
            enum:
            - "L1_Identity"
            default: "L1_Identity"
        type: "object"
        title: "Lift of the identity map"
        x-latex-schema:
          group: "10_simple"
          description: |
            The identity map:
            %
            \begin{equation*}
                x \mapsto \{ x \}
            \end{equation*}
        required:
        - "type"
    L1_IntersectionOfPrinLowerSets:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_IntersectionOfPrinLowerSets"
            enum:
            - "L1_IntersectionOfPrinLowerSets"
            default: "L1_IntersectionOfPrinLowerSets"
        type: "object"
        title: "Intersection of principal lower sets."
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            The domain $\lkdom$ must be a product poset.

            This map is defined as follows:
            %
            \begin{equation}
                \langle x_i \rangle_i \mapsto \bigcap_i \downit x_i
            \end{equation}
        required:
        - "type"
    L1_InvMul_Opt:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "L1_InvMul_Opt"
            enum:
            - "L1_InvMul_Opt"
            default: "L1_InvMul_Opt"
        type: "object"
        title: "Finite-resolution optimistic approximation of the inverse of a multiplication map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a \cdot b \leq x \}
            \end{equation}

            The approximation is optimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    L1_InvMul_Pes:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "L1_InvMul_Pes"
            enum:
            - "L1_InvMul_Pes"
            default: "L1_InvMul_Pes"
        type: "object"
        title: "Finite-resolution pessimistic approximation of the inverse of an addition map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a \cdot b \leq x \}
            \end{equation}

            The approximation is pessimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    L1_InvSum_Opt:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "L1_InvSum_Opt"
            enum:
            - "L1_InvSum_Opt"
            default: "L1_InvSum_Opt"
        type: "object"
        title: "Finite-resolution optimistic approximation of the inverse of a multiplication map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a + b \leq x \}
            \end{equation}

            The approximation is optimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    L1_InvSum_Pes:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "L1_InvSum_Pes"
            enum:
            - "L1_InvSum_Pes"
            default: "L1_InvSum_Pes"
        type: "object"
        title: "Finite-resolution pessimistic approximation of the inverse of an addition map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a + b \leq x \}
            \end{equation}

            The approximation is pessimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    L1_L_Linv:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: "The original map."
          type:
            type: "string"
            const: "L1_L_Linv"
            enum:
            - "L1_L_Linv"
            default: "L1_L_Linv"
        type: "object"
        title: "Lower inverse of a monotone map"
        x-latex-schema:
          group: "65_lift"
          description: |
            Creates the lower inverse of a monotone map.
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "cod(*)"
            kcod: "dom(*)"
    L1_Lift:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map
          type:
            type: "string"
            const: "L1_Lift"
            enum:
            - "L1_Lift"
            default: "L1_Lift"
        type: "object"
        title: "Lifts a monotone map"
        x-latex-schema:
          group: "65_lift"
          description: |
            Lifts a monotone map to a L1Map in the obvious way:
            %
            \begin{equation}
                SELF(m) : x \mapsto \{ m(x) \}
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "dom(*)"
            kcod: "cod(*)"
    L1_RepresentPrincipalLowerSet:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_RepresentPrincipalLowerSet"
            enum:
            - "L1_RepresentPrincipalLowerSet"
            default: "L1_RepresentPrincipalLowerSet"
        type: "object"
        title: "Represent a principal lower set"
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            This map is defined when $\lkdom$ and $\lkcod$ are subsets of a common ambient poset $P$.
        required:
        - "type"
    L1_TopAlternating:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          upper_bounds:
            type: "array"
            items:
              type: "array"
              items: {}
            x-latex-property:
              symbol: "u_i^j"
          type:
            type: "string"
            const: "L1_TopAlternating"
            enum:
            - "L1_TopAlternating"
            default: "L1_TopAlternating"
        type: "object"
        title: "Lower inverse for the meet map"
        description: |
          This map arises as the lower inverse of M\_Meet.

          The upper bounds $u_i$ are the maximal elements of the $i$-th poset in the codomain.
        x-latex-schema:
          group: "70_misc"
        required:
        - "type"
        - "upper_bounds"
    L1_UnionOfPrinLowerSets:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_UnionOfPrinLowerSets"
            enum:
            - "L1_UnionOfPrinLowerSets"
            default: "L1_UnionOfPrinLowerSets"
        type: "object"
        title: "Union of principal lower sets."
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            The domain $\lkdom$ must be a product poset.

            This map is defined as follows:
            %
            \begin{equation}
                \langle x_i \rangle_i \mapsto \bigcup_i \downit x_i
            \end{equation}
        required:
        - "type"
    L1_Unknown:
      allOf:
      - $ref: "#/components/schemas/L1Map"
      - properties:
          type:
            type: "string"
            const: "L1_Unknown"
            enum:
            - "L1_Unknown"
            default: "L1_Unknown"
        type: "object"
        title: "Placeholder for an unknown map."
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a placeholder for a map whose type is not known. 
        required:
        - "type"
    LCheck:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/LCheck_Data"
          type:
            type: "string"
            const: "LCheck"
            enum:
            - "LCheck"
            default: "LCheck"
        type: "object"
        title: "Check for a LMap."
        required:
        - "data"
        - "m"
        - "type"
    LCheck_Data:
      type: "object"
      title: "An input-output pair for the map"
      properties:
        x:
          x-latex-property:
            symbol: "x"
            description: |
              The input to the map
        y:
          $ref: "#/components/schemas/LowerSet"
          x-latex-property:
            symbol: "y"
            description: |
              The expected result (a lower set).
        elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "y"
      x-latex-property:
        title: "An input-output pair"
    LMap:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Map to lower sets of functionalities and implementations."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkdom"
              description: |
                The Kleisli domain of the map.
              color: "requirements"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkcod"
              description: |
                The Kleisli codomain of the map.
              color: "functionalities"
          kimp:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkimp"
              description: |
                The implementation poset of the map.
              color: "implementations"
          type:
            type: "string"
            enum:
            - "L_C_ITransform"
            - "L_C_Intersection"
            - "L_C_Parallel"
            - "L_C_RefineDomain"
            - "L_C_Series"
            - "L_C_Trace"
            - "L_C_Union"
            - "L_C_WrapUnits"
            - "L_Catalog"
            - "L_Constant"
            - "L_Identity"
            - "L_L_Lift1_Constant"
            - "L_L_Lift1_Transform"
            - "L_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "LMap"
            enum:
            - "LMap"
            default: "LMap"
        required:
        - "kdom"
        - "kcod"
        - "kimp"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 50
          color: "color-lower"
          preamble: |
            \newcommand{\signLMAP}[3]{#1 \stackrel{#3}{\to}_{\text{\textbf{LMAP}}} #2 }
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            L_C_ITransform: "#/components/schemas/L_C_ITransform"
            L_C_Intersection: "#/components/schemas/L_C_Intersection"
            L_C_Parallel: "#/components/schemas/L_C_Parallel"
            L_C_RefineDomain: "#/components/schemas/L_C_RefineDomain"
            L_C_Series: "#/components/schemas/L_C_Series"
            L_C_Trace: "#/components/schemas/L_C_Trace"
            L_C_Union: "#/components/schemas/L_C_Union"
            L_C_WrapUnits: "#/components/schemas/L_C_WrapUnits"
            L_Catalog: "#/components/schemas/L_Catalog"
            L_Constant: "#/components/schemas/L_Constant"
            L_Identity: "#/components/schemas/L_Identity"
            L_L_Lift1_Constant: "#/components/schemas/L_L_Lift1_Constant"
            L_L_Lift1_Transform: "#/components/schemas/L_L_Lift1_Transform"
            L_Unknown: "#/components/schemas/L_Unknown"
    L_C_ITransform:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            x-latex-property:
              symbol: "m"
              description: |
                The map to be transformed
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "T"
              description: "The transformation to apply to the implementation of the map."
          type:
            type: "string"
            const: "L_C_ITransform"
            enum:
            - "L_C_ITransform"
            default: "L_C_ITransform"
        type: "object"
        title: "Transforms the implementation of another map."
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "m"
        - "transform"
        - "type"
    L_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "L_C_Intersection"
            enum:
            - "L_C_Intersection"
            default: "L_C_Intersection"
        type: "object"
        title: "Intersection of maps"
        x-latex-schema:
          group: "20_compositions"
          order: 200
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod:
            kimp: "P_C_ProductSmash(@(ms))"
    L_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "L_C_Parallel"
            enum:
            - "L_C_Parallel"
            default: "L_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
            kimp: "P_C_ProductSmash(@(ms))"
    L_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            x-latex-property:
              symbol: "m"
              description: |
                The map to be transformed
          type:
            type: "string"
            const: "L_C_RefineDomain"
            enum:
            - "L_C_RefineDomain"
            default: "L_C_RefineDomain"
        type: "object"
        title: "Refines the domain of a monotone map"
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "m"
        - "type"
    L_C_Series:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "L_C_Series"
            enum:
            - "L_C_Series"
            default: "L_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
            kimp: "P_C_ProductSmash(@(ms))"
            proj1: "L1_C_Series(@(ms))"
    L_C_Trace:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            x-latex-property:
              symbol: "m"
              description: |
                The map to be transformed
          m_proj:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m'"
              description: |
                The projection of the LMap $m$ to a L1Map.
          type:
            type: "string"
            const: "L_C_Trace"
            enum:
            - "L_C_Trace"
            default: "L_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the trace of a LMap $m$.

            Because of computation convenience, we also require the projection $m'$ of the LMap $m$ to a L1Map.
        required:
        - "m"
        - "m_proj"
        - "type"
    L_C_Union:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "L_C_Union"
            enum:
            - "L_C_Union"
            default: "L_C_Union"
        type: "object"
        title: "Union of maps"
        x-latex-schema:
          group: "20_compositions"
          order: 200
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_F_C_Union(@(ms))"
            kimp: "P_C_ProductSmash(@(ms))"
    L_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            x-latex-property:
              symbol: "m"
              description: |
                The map to be transformed
          kdom_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukdom$"
          kcod_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukcod$"
          kimp_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukimp$"
          type:
            type: "string"
            const: "L_C_WrapUnits"
            enum:
            - "L_C_WrapUnits"
            default: "L_C_WrapUnits"
        type: "object"
        title: "Decorates a map with units."
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "kcod_units"
        - "kdom_units"
        - "kimp_units"
        - "m"
        - "type"
    L_Catalog:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          options:
            type: "array"
            items:
              $ref: "#/components/schemas/L_Catalog_Options"
          type:
            type: "string"
            const: "L_Catalog"
            enum:
            - "L_Catalog"
            default: "L_Catalog"
        type: "object"
        title: "LMap for a catalog"
        x-latex-schema:
          group: "10_simple"
          order: 1000
          description: |
            This is the LMap that arises from a DP\_Catalog.
        required:
        - "options"
        - "type"
    L_Catalog_Options:
      title: "Options for L_Catalog"
      type: "object"
      properties:
        f:
          x-latex-property:
            symbol: "\\fun"
        r:
          x-latex-property:
            symbol: "\\rescj"
        i:
          x-latex-property:
            symbol: "\\imp"
      required:
      - "f"
      - "r"
      - "i"
    L_Constant:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          value:
            $ref: "#/components/schemas/LowerSet"
            description: |
              The lower set that is the value of the constant map.
          type:
            type: "string"
            const: "L_Constant"
            enum:
            - "L_Constant"
            default: "L_Constant"
        type: "object"
        title: "Constant map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
        - "value"
    L_Identity:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          type:
            type: "string"
            const: "L_Identity"
            enum:
            - "L_Identity"
            default: "L_Identity"
        type: "object"
        title: "Identity morphism"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    L_L_Lift1_Constant:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "m"
              description: |
                A L1Map morphism.
          value:
            x-latex-property:
              symbol: "v"
              description: |
                A constant value for the implementation of the L1Map morphism.
          type:
            type: "string"
            const: "L_L_Lift1_Constant"
            enum:
            - "L_L_Lift1_Constant"
            default: "L_L_Lift1_Constant"
        type: "object"
        title: "Lifts a L1Map morphisms with a constant value for the implementation."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
        - "value"
    L_L_Lift1_Transform:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor"
              description: |
                A L1Map morphism.
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "t"
              description: |
                A function to compute the implementation of the L1Map morphism.
          type:
            type: "string"
            const: "L_L_Lift1_Transform"
            enum:
            - "L_L_Lift1_Transform"
            default: "L_L_Lift1_Transform"
        type: "object"
        title: "Lifts a L1Map morphism with a function to compute the implementation."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "transform"
        - "type"
    L_Unknown:
      allOf:
      - $ref: "#/components/schemas/LMap"
      - properties:
          type:
            type: "string"
            const: "L_Unknown"
            enum:
            - "L_Unknown"
            default: "L_Unknown"
        type: "object"
        title: "Placeholder for an unknown map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    LowerSet:
      properties:
        kind:
          type: "string"
          const: "LowerSet"
          description: "Kind marker."
        type:
          type: "string"
          enum:
          - "LowerSet_LowerClosure"
          - "LowerSet_Unused"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          LowerSet_LowerClosure: "#/components/schemas/LowerSet_LowerClosure"
          LowerSet_Unused: "#/components/schemas/LowerSet_Unused"
      title: "Represents a lower set in a poset."
      x-latex-schema:
        top_level: false
      required:
      - "kind"
      - "type"
    LowerSet_LowerClosure:
      allOf:
      - $ref: "#/components/schemas/LowerSet"
      - properties:
          points:
            type: "array"
            items: {}
            x-latex-property:
              description: |
                The points in the lower set.
          type:
            type: "string"
            const: "LowerSet_LowerClosure"
            enum:
            - "LowerSet_LowerClosure"
            default: "LowerSet_LowerClosure"
        type: "object"
        title: "A lower set defined as the down closure of a finite set of points."
        required:
        - "points"
        - "type"
    LowerSet_Unused:
      allOf:
      - $ref: "#/components/schemas/LowerSet"
      - properties:
          type:
            type: "string"
            const: "LowerSet_Unused"
            enum:
            - "LowerSet_Unused"
            default: "LowerSet_Unused"
        type: "object"
        title: "Unused"
        x-latex-schema:
          excluded: true
        required:
        - "type"
    M_AddL:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P_i"
              description: |
                The sequence of posets in which to perform the operation.
          type:
            type: "string"
            const: "M_AddL"
            enum:
            - "M_AddL"
            default: "M_AddL"
        type: "object"
        title: "Addition in the L topology."
        x-latex-schema:
          group: "g50_maths"
          description: |
            This map takes a sequence of elements from posets and returns their sum in the L topology.

            If the domain is a product of length $n$, there are $n-1$ in \texttt{opspaces}.

            For example, if the domain is a product of 4 posets, the sum $a + b + c + d$ is computed as follows:
            %
            \begin{align*}
                t_1 &= a {+}_{\text{opspaces}_0} b \\
                t_2 &= t_1 {+}_{\text{opspaces}_1} c \\
                t_3 &= t_2 {+}_{\text{opspaces}_2} d \\
            \end{align*}
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "lower"
          functors:
            uinv: "SU1_InvSum"
    M_AddLConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to add to the input."
          type:
            type: "string"
            const: "M_AddLConstant"
            enum:
            - "M_AddLConstant"
            default: "M_AddLConstant"
        type: "object"
        title: "Add a constant in the L topology."
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x + c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_AddU:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P_i"
              description: |
                The sequence of posets in which to perform the operation.
          type:
            type: "string"
            const: "M_AddU"
            enum:
            - "M_AddU"
            default: "M_AddU"
        type: "object"
        title: "Addition in the U topology."
        x-latex-schema:
          group: "g50_maths"
          description: |
            See M\_AddL for the meaning of \texttt{opspaces}.
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "upper"
          functors:
            linv: "SL1_InvSum"
    M_AddUConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to add."
          type:
            type: "string"
            const: "M_AddUConstant"
            enum:
            - "M_AddUConstant"
            default: "M_AddUConstant"
        type: "object"
        title: "Addition of constant in the U topology."
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x + c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_BottomIfNotTop:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_BottomIfNotTop"
            enum:
            - "M_BottomIfNotTop"
            default: "M_BottomIfNotTop"
        type: "object"
        title: "Maps top to top, and everything else to bottom."
        x-latex-schema:
          group: "g70_misc"
          description: |
            \begin{equation}
                x \mapsto \begin{cases}
                              \top_C & \text{if } x = \top_D \\
                              \bot_C & \text{otherwise}
                \end{cases}
            \end{equation}
        required:
        - "type"
    M_C_Coproduct:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_Coproduct"
            enum:
            - "M_C_Coproduct"
            default: "M_C_Coproduct"
        type: "object"
        title: "Coproduct of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The coproduct of two or more monotone maps is a monotone map that selects one or the other map depending on the input.

            If $f: A \to B$ and $g: C \to B$, then
            %
            \begin{equation}
                SELF(f, g): P\_C\_Sum(A, C) \to B
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_Sum(@(*))"
            cod: "P_F_C_Union(@(*))"
            uinv: "SU1_C_CodSum(@(*))"
            linv: "SL1_C_CodSum(@(*))"
    M_C_CoproductSmash:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_CoproductSmash"
            enum:
            - "M_C_CoproductSmash"
            default: "M_C_CoproductSmash"
        type: "object"
        title: "Smash coproduct of two monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The smash coproduct of two or more monotone maps is a monotone map that selects one or the other map depending on the input.

            If $f: A \to B$ and $g: C \to B$, then
            %
            \begin{equation}
                SELF(f, g): P\_C\_SumSmash(A, C) \to B
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_SumSmash(@(*))"
            cod: "P_F_C_Union(@(*))"
    M_C_DomProdCodSmash:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_DomProdCodSmash"
            enum:
            - "M_C_DomProdCodSmash"
            default: "M_C_DomProdCodSmash"
        type: "object"
        title: |
          A monotone map from a product of domains to a smash product of codomains.
        x-latex-schema:
          group: "g31_multi"
          description: |
            For two monotone maps $f: A \to B$ and $g: C \to D$, we can define the monotone map
            %
            \begin{equation}
                SELF(f,g): P\_C\_Product(A, C) \to P\_C\_ProductSmash(B , D)
            \end{equation}
            %
            in the obvious way.
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_Product(@(*))"
            cod: "P_C_ProductSmash(@(*))"
    M_C_DomSmashCodProd:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_DomSmashCodProd"
            enum:
            - "M_C_DomSmashCodProd"
            default: "M_C_DomSmashCodProd"
        type: "object"
        title: |
          A monotone map from the smash product of domains to the product of codomains.
        x-latex-schema:
          group: "g31_multi"
          description: |
            For two monotone maps $f: A \to B$ and $g: C \to D$, we can define the monotone map
            %
            \begin{equation}
                SELF(f,g): P\_C\_ProductSmash(A, C) \to P\_C\_Product(B , D)
            \end{equation}
            in the obvious way.
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_ProductSmash(@(*))"
            cod: "P_C_Product(@(*))"
    M_C_DomUnion:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_DomUnion"
            enum:
            - "M_C_DomUnion"
            default: "M_C_DomUnion"
        type: "object"
        title: "Domain union of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            Given two monotone maps $f: A \to B$ and $g: C \to B$, the domain union of these maps is a monotone map that combines the domains of both maps:
            %
            \begin{equation}
                SELF(f, g) : P\_F\_C\_Union(A, C) \to B
            \end{equation}

            The value is defined as follows:
            %
            \begin{equation}
                SELF(f, g) : x \mapsto
                \begin{cases}
                    f(x) & \text{if } x \in A \\
                    g(x) & \text{if } x \in C
                \end{cases}
            \end{equation}

            Note that the order of the maps does matter.
            We use the first map whose domain contains the input.
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          functors:
            dom: "P_F_C_Union(@(*))"
            cod: "P_F_C_Union(@(*))"
    M_C_Leq_X:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "Poset in which the comparison is performed."
            x-latex-property:
              symbol: "P"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
            description: "Comparison value."
          type:
            type: "string"
            const: "M_C_Leq_X"
            enum:
            - "M_C_Leq_X"
            default: "M_C_Leq_X"
        type: "object"
        title: "Tests constant LEQ value."
        x-latex-schema:
          group: "g90_tests"
          title: "Tests $\\text{constant} \\leq x$"
          description: |
            The map $x \mapsto v \leq_P x$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_C_LiftToSubsets:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            description: |
              The monotone map that is lifted.
          type:
            type: "string"
            const: "M_C_LiftToSubsets"
            enum:
            - "M_C_LiftToSubsets"
            default: "M_C_LiftToSubsets"
        type: "object"
        title: "Lift of a monotone map to subsets"
        x-latex-schema:
          group: "g60_lift"
        required:
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          functors:
            dom: "P_C_Power(@(*))"
            cod: "P_C_Power(@(*))"
    M_C_Lt_X:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "Poset in which the comparison is performed."
            x-latex-property:
              symbol: "P"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
            description: "Comparison value."
          type:
            type: "string"
            const: "M_C_Lt_X"
            enum:
            - "M_C_Lt_X"
            default: "M_C_Lt_X"
        type: "object"
        title: "Tests constant LT value."
        x-latex-schema:
          group: "g90_tests"
          title: "Tests $\\text{constant} < x$"
          description: |
            The map $x \mapsto v <_P x$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_C_Op:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: "The original map"
          type:
            type: "string"
            const: "M_C_Op"
            enum:
            - "M_C_Op"
            default: "M_C_Op"
        type: "object"
        title: "Opposite of a map"
        x-latex-schema:
          group: "g30_compositions"
        required:
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          flavor: "flip"
          functors:
            dom: "P_C_Opposite(@(*))"
            cod: "P_C_Opposite(@(*))"
    M_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_Parallel"
            enum:
            - "M_C_Parallel"
            default: "M_C_Parallel"
        type: "object"
        title: "Monoidal product of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            If $f: A \to B$ and $g: C \to D$, then
            %
            \begin{equation}
                SELF(f, g): P\_C\_Product(A, C) \to  P\_C\_Product(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_Product(@(*))"
            cod: "P_C_Product(@(*))"
            uinv: "SU1_C_Parallel(@(*))"
            linv: "SL1_C_Parallel(@(*))"
    M_C_ParallelSmash:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_ParallelSmash"
            enum:
            - "M_C_ParallelSmash"
            default: "M_C_ParallelSmash"
        type: "object"
        title: "Monoidal (smash) product of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            If $f: A \to B$ and $g: C \to D$, then
            %
            \begin{equation}
                SELF(f, g): P\_C\_ProductSmash(A, C) \to  P\_C\_ProductSmash(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_ProductSmash(@(*))"
            cod: "P_C_ProductSmash(@(*))"
    M_C_Product:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_Product"
            enum:
            - "M_C_Product"
            default: "M_C_Product"
        type: "object"
        title: "Product of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The product of two or more monotone maps with the same domain is a monotone map that combines the outputs of both maps in a product.

            If $f: A \to B$ and $g: A \to C$, then
            %
            \begin{equation}
                SELF(f,g): A \to P\_C\_Product(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_F_C_Intersection(@(*))"
            cod: "P_C_Product(@(*))"
    M_C_ProductSmash:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_ProductSmash"
            enum:
            - "M_C_ProductSmash"
            default: "M_C_ProductSmash"
        type: "object"
        title: "Smash product of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The product of two or more monotone maps with the same domain is a monotone map that combines the outputs of both maps in a smash product.

            If $f: A \to B$ and $g: A \to C$, then
            %
            \begin{equation}
                SELF(f,g): A \to P\_C\_ProductSmash(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_F_C_Intersection(@(*))"
            cod: "P_C_ProductSmash(@(*))"
    M_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: "The original map"
          type:
            type: "string"
            const: "M_C_RefineDomain"
            enum:
            - "M_C_RefineDomain"
            default: "M_C_RefineDomain"
        type: "object"
        title: "A refinement of the domain of a monotone map"
        x-latex-schema:
          group: "g30_compositions"
        required:
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          flavor: "inherit"
          functors:
            dom: "given"
            cod: "@(m)"
    M_C_Series:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_Series"
            enum:
            - "M_C_Series"
            default: "M_C_Series"
        type: "object"
        title: "Series composition of monotone maps"
        x-latex-schema:
          group: "g31_multi"
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "@(maps[0])"
            cod: "@(maps[-1])"
            uinv: "SU1_C_Series(reversed(@(*)))"
            linv: "SL1_C_Series(reversed(@(*)))"
    M_C_Sum:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_Sum"
            enum:
            - "M_C_Sum"
            default: "M_C_Sum"
        type: "object"
        title: "Sum of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The sum of two or more monotone maps is a monotone map that combines the outputs of both maps.

            If $f: A \to B$ and $g: C \to D$, then
            %
            \begin{equation}
                SELF(f,g): P\_C\_Sum(A, C) \to P\_C\_Sum(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_Sum(@(*))"
            cod: "P_C_Sum(@(*))"
    M_C_SumSmash:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          maps:
            type: "array"
            items:
              $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m_i"
              description: |
                A list of monotone maps that are composed together.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the monotone maps.
          type:
            type: "string"
            const: "M_C_SumSmash"
            enum:
            - "M_C_SumSmash"
            default: "M_C_SumSmash"
        type: "object"
        title: "Smash sum of monotone maps"
        x-latex-schema:
          group: "g31_multi"
          description: |
            The smash sum of two or more monotone maps is a monotone map that combines the outputs of both maps.

            If $f: A \to B$ and $g: C \to D$, then
            %
            \begin{equation}
                SELF(f,g): P\_C\_SumSmash(A, C) \to P\_C\_SumSmash(B, D)
            \end{equation}
        required:
        - "maps"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "inherit"
          functors:
            dom: "P_C_SumSmash(@(*))"
            cod: "P_C_SumSmash(@(*))"
    M_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: "The original map"
          dom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the domain of the monotone map.
          cod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the codomain of the monotone map.
          type:
            type: "string"
            const: "M_C_WrapUnits"
            enum:
            - "M_C_WrapUnits"
            default: "M_C_WrapUnits"
        type: "object"
        title: "Wraps a monotone map with units descriptions for domain and codomain."
        x-latex-schema:
          group: "g30_compositions"
        required:
        - "cod_units"
        - "dom_units"
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          flavor: "inherit"
          functors:
            dom: "P_C_Units(@(m), dom_units)"
            cod: "P_C_Units(@(m), cod_units)"
    M_Ceil0:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          type:
            type: "string"
            const: "M_Ceil0"
            enum:
            - "M_Ceil0"
            default: "M_Ceil0"
        type: "object"
        title: "Ceiling function relative"
        x-latex-schema:
          group: "g50_maths"
          description: |
            This is a relative of the ceiling function defined only on $\mathbb{R}_{\geq0}$
            and defined as follows:
            %
            \begin{equation}
                x \mapsto \begin{cases}
                              0 & \text{if } x = 0 \\
                              \infty & \text{if } x = \infty \\
                              \text{floor} (x + 1) & \text{otherwise}
                \end{cases}
            \end{equation}

            See M\_RoundUp for implementing the regular ceiling function.
        required:
        - "opspace"
        - "type"
    M_Coerce:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Coerce"
            enum:
            - "M_Coerce"
            default: "M_Coerce"
        type: "object"
        title: "Coerces from one poset to another"
        x-latex-schema:
          group: "g10_basic"
          excluded: true
        required:
        - "type"
    M_Constant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
              description: "The constant value of the map."
          type:
            type: "string"
            const: "M_Constant"
            enum:
            - "M_Constant"
            default: "M_Constant"
        type: "object"
        title: "A constant function"
        x-latex-schema:
          group: "g10_basic"
        required:
        - "type"
        - "value"
        x-latex-property:
          description: |
            The constant map $x \mapsto v$.
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_ContainedInLowerSet:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          lower_set:
            $ref: "#/components/schemas/LowerSet"
            x-latex-property:
              symbol: "L"
              description: "The lower set"
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              description: "The poset in which the lower set is defined."
          cod:
            description: "Must be a boolean poset."
          type:
            type: "string"
            const: "M_ContainedInLowerSet"
            enum:
            - "M_ContainedInLowerSet"
            default: "M_ContainedInLowerSet"
        type: "object"
        title: "Test for containment in a lower set"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map tests whether the input is contained in the lower set:
            %
            \begin{equation}
                x\mapsto x \in L
            \end{equation}
        required:
        - "cod"
        - "lower_set"
        - "opspace"
        - "type"
    M_ContainedInUpperSet:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          upper_set:
            $ref: "#/components/schemas/UpperSet"
            x-latex-property:
              symbol: "U"
              description: "The upper set"
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              description: "The poset in which the upper set is defined"
          cod:
            description: "Must be a boolean poset."
          type:
            type: "string"
            const: "M_ContainedInUpperSet"
            enum:
            - "M_ContainedInUpperSet"
            default: "M_ContainedInUpperSet"
        type: "object"
        title: "Test for containment in an upper set"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map tests whether the input is contained in the upper set:
            %
            \begin{equation}
                x\mapsto x \in U
            \end{equation}
        required:
        - "cod"
        - "opspace"
        - "type"
        - "upper_set"
    M_DivideLConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to divide by."
          type:
            type: "string"
            const: "M_DivideLConstant"
            enum:
            - "M_DivideLConstant"
            default: "M_DivideLConstant"
        type: "object"
        title: "Division by a constant (L topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x / c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_DivideUConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to divide by."
          type:
            type: "string"
            const: "M_DivideUConstant"
            enum:
            - "M_DivideUConstant"
            default: "M_DivideUConstant"
        type: "object"
        title: "Division by a constant (U topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x / c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_Empty:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Empty"
            enum:
            - "M_Empty"
            default: "M_Empty"
        type: "object"
        title: "The unique map from the empty set to another"
        description: |
          This is the unique map from the empty set to another poset.
        x-latex-schema:
          group: "g10_basic"
        required:
        - "type"
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_Explicit:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          options:
            type: "array"
            items:
              $ref: "#/components/schemas/M_Explicit_Option"
            x-latex-property:
              description: |
                A list of pairs $(x, y)$ such that the map sends $x$ to $y$.
          type:
            type: "string"
            const: "M_Explicit"
            enum:
            - "M_Explicit"
            default: "M_Explicit"
        type: "object"
        title: "A map defined pointwise."
        x-latex-schema:
          group: "g10_basic"
        required:
        - "options"
        - "type"
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_Explicit_Option:
      type: "object"
      properties:
        x: {}
        y: {}
      required:
      - "x"
      - "y"
    M_Floor0:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          type:
            type: "string"
            const: "M_Floor0"
            enum:
            - "M_Floor0"
            default: "M_Floor0"
        type: "object"
        title: "Floor function relative"
        x-latex-schema:
          group: "g50_maths"
          description: |
            This is a relative of the floor function defined only on $\mathbb{R}_{\geq0}$
              and defined as follows:
            %
            \begin{equation}
                x \mapsto \begin{cases}
                              0 & \text{if } x = 0 \\
                              \infty & \text{if } x = \infty \\
                              \text{ceil} (x - 1) & \text{otherwise}
                \end{cases}
            \end{equation}

            See M\_RoundDown for implementing the regular floor function.
        required:
        - "opspace"
        - "type"
    M_Id:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Id"
            enum:
            - "M_Id"
            default: "M_Id"
        type: "object"
        title: "Identity map"
        x-latex-schema:
          group: "g10_basic"
        required:
        - "type"
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_IdentityBelowThreshold:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            description: |
              Value returned by the map if the input is above the threshold.
              This value must be greater than or equal to the threshold.
            x-latex-property:
              symbol: "V"
          threshold:
            $ref: "#/components/schemas/Value"
            description: "Threshold value."
            x-latex-property:
              symbol: "T"
          type:
            type: "string"
            const: "M_IdentityBelowThreshold"
            enum:
            - "M_IdentityBelowThreshold"
            default: "M_IdentityBelowThreshold"
        type: "object"
        title: "A monotone map that outputs a constant value if the input is above a threshold."
        x-latex-schema:
          group: "g70_misc"
          description: |
            \begin{equation}
                x \mapsto
                \begin{cases}
                    V & \text{if } T \leq x \\
                    x & \text{otherwise}
                \end{cases}
            \end{equation}
            where $T \leq V$.
        required:
        - "threshold"
        - "type"
        - "value"
    M_Injection:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          index:
            description: "Which space to inject into"
            type: "integer"
          type:
            type: "string"
            const: "M_Injection"
            enum:
            - "M_Injection"
            default: "M_Injection"
        type: "object"
        title: "Injection into a poset sum"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This is the injection map into the $i$-th poset of a poset sum P\_C\_Sum.
        required:
        - "index"
        - "type"
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_Join:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              The posets in which each join is defined.
          type:
            type: "string"
            const: "M_Join"
            enum:
            - "M_Join"
            default: "M_Join"
        type: "object"
        title: "Join operation"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            The join operation is defined as follows:
            %
            \begin{equation}
                x\mapsto \bigvee_{i} x_i
            \end{equation}
            where $x_i$ is the $i$-th component of $x$ in the poset sum.
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_JoinConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: |
                The constant value to join with.
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              description: |
                The poset in which the join operation is defined.
          type:
            type: "string"
            const: "M_JoinConstant"
            enum:
            - "M_JoinConstant"
            default: "M_JoinConstant"
        type: "object"
        title: "Join with a constant value"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map performs a join with a constant value:
            %
            \begin{equation}
                x\mapsto x \vee c
            \end{equation}
            where $c$ is the constant value.
        required:
        - "opspace"
        - "type"
        - "value"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_Leq:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "Poset in which the comparison is performed."
            x-latex-property:
              symbol: "P"
          type:
            type: "string"
            const: "M_Leq"
            enum:
            - "M_Leq"
            default: "M_Leq"
        type: "object"
        title: "Tests v1 LEQ v2."
        x-latex-schema:
          group: "g90_tests"
          title: "Tests $x_1 \\leq_P x_2$"
          description: |
            The map $(x_1, x_2) \mapsto x_1 \leq_P x_2$.
          order: 100
        required:
        - "opspace"
        - "type"
    M_Lift:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Lift"
            enum:
            - "M_Lift"
            default: "M_Lift"
        type: "object"
        title: "Lifts a value to a tuple with one element."
        x-latex-schema:
          group: "g80_mux"
          description: |
            The map is $ x \mapsto \langle x \rangle $

            This is the inverse of M\_Unlift.
        required:
        - "type"
    M_LiftToLowerSets:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            description: |
              The monotone map that is lifted.
          type:
            type: "string"
            const: "M_LiftToLowerSets"
            enum:
            - "M_LiftToLowerSets"
            default: "M_LiftToLowerSets"
        type: "object"
        title: "Lifts a monotone map to lower sets"
        x-latex-schema:
          group: "g60_lift"
        required:
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          functors:
            dom: "P_C_LowerSets(@(*))"
            cod: "P_C_LowerSets(@(*))"
    M_LiftToUpperSets:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            description: |
              The monotone map that is lifted.
          type:
            type: "string"
            const: "M_LiftToUpperSets"
            enum:
            - "M_LiftToUpperSets"
            default: "M_LiftToUpperSets"
        type: "object"
        title: "Lifts a monotone map to upper sets"
        x-latex-schema:
          group: "g60_lift"
        required:
        - "m"
        - "type"
        x-mcdp-map:
          params:
          - "m"
          functors:
            dom: "P_C_UpperSets(@(*))"
            cod: "P_C_UpperSets(@(*))"
    M_Meet:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              The posets in which each meet is defined.
          type:
            type: "string"
            const: "M_Meet"
            enum:
            - "M_Meet"
            default: "M_Meet"
        type: "object"
        title: "Meet operation"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            The meet operation is defined as follows:
            %
            \begin{equation}
                x\mapsto \bigwedge_{i} x_i
            \end{equation}
            where $x_i$ is the $i$-th component of $x$ in the poset sum.
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_MeetConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: |
                The constant value to meet with.
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              description: |
                The poset in which the meet operation is defined.
          type:
            type: "string"
            const: "M_MeetConstant"
            enum:
            - "M_MeetConstant"
            default: "M_MeetConstant"
        type: "object"
        title: "Meet with a constant"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map performs a meet with a constant value:
            %
            \begin{equation}
                x\mapsto x \wedge c
            \end{equation}
            where $c$ is the constant value.
        required:
        - "opspace"
        - "type"
        - "value"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_MultiplyL:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P_i"
              description: |
                The sequence of posets in which to perform the operation.
          type:
            type: "string"
            const: "M_MultiplyL"
            enum:
            - "M_MultiplyL"
            default: "M_MultiplyL"
        type: "object"
        title: "Multiplication (L topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            See M\_AddL for the meaning of the optional parameters.
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "lower"
          functors:
            uinv: "SU1_InvMultiply"
    M_MultiplyLConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to multiply by."
          type:
            type: "string"
            const: "M_MultiplyLConstant"
            enum:
            - "M_MultiplyLConstant"
            default: "M_MultiplyLConstant"
        type: "object"
        title: "Multiplication by a constant (L topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x \cdot c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_MultiplyU:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspaces:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P_i"
              description: |
                The sequence of posets in which to perform the operation.
          type:
            type: "string"
            const: "M_MultiplyU"
            enum:
            - "M_MultiplyU"
            default: "M_MultiplyU"
        type: "object"
        title: "Multiplication (U topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            See M\_AddL for the meaning of \texttt{opspaces}.
        required:
        - "opspaces"
        - "type"
        x-mcdp-map:
          params:
          - "maps"
          flavor: "upper"
          functors:
            uinv: "SL1_InvMultiply"
    M_MultiplyUConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to multiply by."
          type:
            type: "string"
            const: "M_MultiplyUConstant"
            enum:
            - "M_MultiplyUConstant"
            default: "M_MultiplyUConstant"
        type: "object"
        title: "Multiplication by a constant (U topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x \cdot c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_PowerFracL:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          num:
            type: "string"
            x-latex-property:
              symbol: "N"
              description: |
                The numerator of the fraction.
          den:
            type: "string"
            x-latex-property:
              symbol: "D"
              description: |
                The denominator of the fraction.
          type:
            type: "string"
            const: "M_PowerFracL"
            enum:
            - "M_PowerFracL"
            default: "M_PowerFracL"
        type: "object"
        title: "Lift to the power of a fraction (L topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x ^ {N/D}$.
        required:
        - "den"
        - "num"
        - "opspace"
        - "type"
    M_PowerFracU:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          num:
            type: "string"
            x-latex-property:
              symbol: "N"
              description: |
                The numerator of the fraction.
          den:
            type: "string"
            x-latex-property:
              symbol: "D"
              description: |
                The denominator of the fraction.
          type:
            type: "string"
            const: "M_PowerFracU"
            enum:
            - "M_PowerFracU"
            default: "M_PowerFracU"
        type: "object"
        title: "Lift to the power of a fraction (U topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x ^ {N/D}$.
        required:
        - "den"
        - "num"
        - "opspace"
        - "type"
    M_ReprLowerSet:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_ReprLowerSet"
            enum:
            - "M_ReprLowerSet"
            default: "M_ReprLowerSet"
        type: "object"
        description: "ReprLowerSet"
        x-latex-schema:
          group: "g15_poset_ops"
          excluded: true
        required:
        - "type"
    M_ReprUpperSet:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_ReprUpperSet"
            enum:
            - "M_ReprUpperSet"
            default: "M_ReprUpperSet"
        type: "object"
        title: "Maps a point to the largest upper set containing its closure"
        description: "ReprUpperSet"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map $m : \dom \to \cod$, with $\dom \subseteq \cod$, 
            takes a point $x$ in the domain and returns the largest upper set containing its closure.

            \begin{equation*}
                    x \mapsto \arg\min_{y \geq x} \upit y
            \end{equation*}

            If this point does not exist, the map is ``undefined''.

            Note: M\_RepresentPrincipalUpperSet\_TotalOrderBounded does the same thing but it is guaranteed to always be defined. 
          excluded: true
        required:
        - "type"
    M_RepresentPrincipalLowerSet_TotalOrderBounded:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_RepresentPrincipalLowerSet_TotalOrderBounded"
            enum:
            - "M_RepresentPrincipalLowerSet_TotalOrderBounded"
            default: "M_RepresentPrincipalLowerSet_TotalOrderBounded"
        type: "object"
        title: "Largest principal lower set in the poset."
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map $m : \dom \to \cod$, with $\dom, \cod$ subposets of a common ambient posets.
            takes a point $x$ in the domain and returns the largest lower set containing its down closure.
            % 
            \begin{equation}
                x \mapsto \arg\max_{y \leq x} \downit y
            \end{equation}  
            %
            The compilation process ensures that this map is constructed only when this is always well defined.

            Example: $\dom = \mathbb{R}$ and $\cod = \mathbb{N}$
        required:
        - "type"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_RepresentPrincipalUpperSet_TotalOrderBounded:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_RepresentPrincipalUpperSet_TotalOrderBounded"
            enum:
            - "M_RepresentPrincipalUpperSet_TotalOrderBounded"
            default: "M_RepresentPrincipalUpperSet_TotalOrderBounded"
        type: "object"
        title: "Largest principal upper set in the poset."
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This map $m : \dom \to \cod$, with $\dom, \cod$ subposets of a common ambient poset 
            takes a point $x$ in the domain and returns the largest upper set containing its closure.
            %
            \begin{equation*}
                    x \mapsto \arg\min_{y \geq x} \upit y
            \end{equation*}

            The compilation process ensures that this map is constructed only when this is always well defined.

            Example: $\dom = \mathbb{R}$ and $\cod = \mathbb{N}$
        required:
        - "type"
        x-mcdp-map:
          flavor:
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_RoundDown:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          offset:
            x-latex-property:
              symbol: "o"
              description: "The offset to subtract before rounding down."
          step:
            type: "string"
            x-latex-property:
              symbol: "s"
              description: "The step to round down to."
          type:
            type: "string"
            const: "M_RoundDown"
            enum:
            - "M_RoundDown"
            default: "M_RoundDown"
        type: "object"
        title: "Round down"
        x-latex-schema:
          group: "g50_maths"
        required:
        - "offset"
        - "opspace"
        - "step"
        - "type"
    M_RoundUp:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          offset:
            x-latex-property:
              symbol: "o"
              description: "The offset to subtract before rounding up."
          step:
            type: "string"
            x-latex-property:
              symbol: "s"
              description: "The step to round up to."
          type:
            type: "string"
            const: "M_RoundUp"
            enum:
            - "M_RoundUp"
            default: "M_RoundUp"
        type: "object"
        title: "Round up"
        x-latex-schema:
          group: "g50_maths"
        required:
        - "offset"
        - "opspace"
        - "step"
        - "type"
    M_ScaleL:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          num:
            type: "string"
            x-latex-property:
              symbol: "N"
              description: |
                The numerator of the fraction.
          den:
            type: "string"
            x-latex-property:
              symbol: "D"
              description: |
                The denominator of the fraction.
          type:
            type: "string"
            const: "M_ScaleL"
            enum:
            - "M_ScaleL"
            default: "M_ScaleL"
        type: "object"
        title: "Scaling in the L topology by a fraction."
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x \cdot  (N/D)$.
        required:
        - "den"
        - "num"
        - "opspace"
        - "type"
    M_ScaleU:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          num:
            type: "string"
            x-latex-property:
              symbol: "N"
              description: |
                The numerator of the fraction.
          den:
            type: "string"
            x-latex-property:
              symbol: "D"
              description: |
                The denominator of the fraction.
          type:
            type: "string"
            const: "M_ScaleU"
            enum:
            - "M_ScaleU"
            default: "M_ScaleU"
        type: "object"
        title: "Scaling in the U topology by a fraction."
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x \cdot_P  (N/D)$.
        required:
        - "den"
        - "num"
        - "opspace"
        - "type"
    M_SmashInjection:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          index:
            description: "Which space to inject into"
            type: "integer"
          type:
            type: "string"
            const: "M_SmashInjection"
            enum:
            - "M_SmashInjection"
            default: "M_SmashInjection"
        type: "object"
        title: "Injection into a smash sum"
        x-latex-schema:
          group: "g15_poset_ops"
          description: |
            This is the injection map into the $i$-th poset of a smash poset sum P\_C\_SumSmash.
        required:
        - "index"
        - "type"
        x-mcdp-map:
          flavor: "both"
          functors:
            dom:
            cod:
            uinv:
            linv:
            used:
    M_SubLConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to subtract."
          type:
            type: "string"
            const: "M_SubLConstant"
            enum:
            - "M_SubLConstant"
            default: "M_SubLConstant"
        type: "object"
        title: "Subtraction of a constant (L topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x - c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_SubUConstant:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            x-latex-property:
              symbol: "P"
              description: "The poset in which we do the operation."
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "c"
              description: "The constant to subtract."
          type:
            type: "string"
            const: "M_SubUConstant"
            enum:
            - "M_SubUConstant"
            default: "M_SubUConstant"
        type: "object"
        title: "Subtraction by a constant (U topology)"
        x-latex-schema:
          group: "g50_maths"
          description: |
            The map $x \mapsto x - c$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_TakeIndex:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          projection:
            $ref: "#/components/schemas/Projection"
            description: "Describes the projection"
          type:
            type: "string"
            const: "M_TakeIndex"
            enum:
            - "M_TakeIndex"
            default: "M_TakeIndex"
        type: "object"
        title: "Projection of an element in a poset product."
        x-latex-schema:
          group: "g80_mux"
          description: |
            The map $x \mapsto x_i$ where $i$ is the index of the projection.
        required:
        - "projection"
        - "type"
    M_TakeRange:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          range:
            $ref: "#/components/schemas/Range"
            description: "Describes the range of indices to take."
          type:
            type: "string"
            const: "M_TakeRange"
            enum:
            - "M_TakeRange"
            default: "M_TakeRange"
        type: "object"
        title: "Projection of a range of elements in a smash poset product."
        x-latex-schema:
          group: "g80_mux"
          description: |
            The map $x \mapsto x_{i..j}$ where $i$ and $j$ are the bounds of the range.
        required:
        - "range"
        - "type"
    M_Threshold1:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
              description: "A constant value"
          type:
            type: "string"
            const: "M_Threshold1"
            enum:
            - "M_Threshold1"
            default: "M_Threshold1"
        type: "object"
        title: "Threshold map (r-to-f for DP\\_FuncNotMoreThan)"
        x-latex-schema:
          group: "g70_misc"
          description: |
            This map returns a constant value:
            %
            \begin{equation}
                x\mapsto \begin{cases}
                             x & \text{if } x \leq v \\
                             v & \text{otherwise}
                \end{cases}
            \end{equation}

            This map is the r-to-f map for DP\_FuncNotMoreThan
        required:
        - "type"
        - "value"
    M_Threshold2:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
              description: "A constant value"
          type:
            type: "string"
            const: "M_Threshold2"
            enum:
            - "M_Threshold2"
            default: "M_Threshold2"
        type: "object"
        title: "Threshold map (f-to-r for DP\\_ResNotLessThan)"
        x-latex-schema:
          group: "g70_misc"
          description: ""
        required:
        - "type"
        - "value"
    M_TopIfNotBottom:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_TopIfNotBottom"
            enum:
            - "M_TopIfNotBottom"
            default: "M_TopIfNotBottom"
        type: "object"
        title: "Maps bottom to bottom, and everything else to top."
        x-latex-schema:
          group: "g70_misc"
          description: |
            \begin{equation}
                x \mapsto \begin{cases}
                              \bot_C & \text{if } x = \bot_D \\
                              \top_C & \text{otherwise}
                \end{cases}
            \end{equation}
        required:
        - "type"
    M_Undefined:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Undefined"
            enum:
            - "M_Undefined"
            default: "M_Undefined"
        type: "object"
        title: "Undefined map"
        description: |
          This is a map that is always undefined. It is used for debug purposes.
        x-latex-schema:
          group: "g10_basic"
        required:
        - "type"
    M_Unknown:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Unknown"
            enum:
            - "M_Unknown"
            default: "M_Unknown"
        type: "object"
        title: "Placeholder for an unknown map"
        x-latex-schema:
          group: "g10_basic"
        required:
        - "type"
    M_Unlift:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          type:
            type: "string"
            const: "M_Unlift"
            enum:
            - "M_Unlift"
            default: "M_Unlift"
        type: "object"
        title: "Unlifts a one-element tuple to its single element."
        x-latex-schema:
          group: "g80_mux"
          description: |
            The map is $ \langle x \rangle \mapsto x $

            This is the inverse of M\_Lift.
        required:
        - "type"
    M_X_Leq_C:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "Poset in which the comparison is performed."
            x-latex-property:
              symbol: "P"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
            description: "Comparison value."
          type:
            type: "string"
            const: "M_X_Leq_C"
            enum:
            - "M_X_Leq_C"
            default: "M_X_Leq_C"
        type: "object"
        title: "Tests value LEQ constant."
        x-latex-schema:
          group: "g90_tests"
          title: "Tests $x \\leq \\text{constant}$"
          description: |
            The map $x \mapsto x \leq_P v$.
        required:
        - "opspace"
        - "type"
        - "value"
    M_X_Lt_C:
      allOf:
      - $ref: "#/components/schemas/MonotoneMap"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "Poset in which the comparison is performed."
            x-latex-property:
              symbol: "P"
          value:
            $ref: "#/components/schemas/Value"
            x-latex-property:
              symbol: "v"
            description: "Comparison value."
          type:
            type: "string"
            const: "M_X_Lt_C"
            enum:
            - "M_X_Lt_C"
            default: "M_X_Lt_C"
        type: "object"
        title: "Tests value LT constant."
        x-latex-schema:
          group: "g90_tests"
          title: "Tests $x < \\text{constant}$"
          description: |
            The map $x \mapsto x <_P v$.
        required:
        - "opspace"
        - "type"
        - "value"
    MapCheck:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
            description: "The map to check."
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/MapCheck_Data"
            x-latex-property:
              description: |
                An input-output pair, where `x` is the input to the map and `y` is the expected result.
          type:
            type: "string"
            const: "MapCheck"
            enum:
            - "MapCheck"
            default: "MapCheck"
        type: "object"
        title: "Check for a monotone map."
        required:
        - "data"
        - "m"
        - "type"
    MapCheck_Data:
      type: "object"
      title: "An input-output pair for the map"
      properties:
        x:
          x-latex-property:
            symbol: "x"
            description: |
              The input to the map.
        y:
          x-latex-property:
            symbol: "y"
            description: "The expected result"
        elapsed:
          type: "number"
          description: "Time taken to compute the result."
      required:
      - "x"
      - "y"
    ModelFunctionality:
      allOf:
      - $ref: "#/components/schemas/ConnectionSource"
      - allOf:
        - $ref: "#/components/schemas/ConnectionSource"
        - type: "object"
          title: "The source of a connection is a functionality of the composite graph."
          properties:
            functionality:
              type: "string"
              x-latex-property:
                description: |
                  The name of the functionality that is provided by the composite graph.
            type:
              type: "string"
              enum:
              - "ModelFunctionality"
          required:
          - "functionality"
          - "type"
        properties:
          type:
            type: "string"
            const: "ModelFunctionality"
            enum:
            - "ModelFunctionality"
            default: "ModelFunctionality"
        type: "object"
        required:
        - "type"
    ModelRequirement:
      allOf:
      - $ref: "#/components/schemas/ConnectionTarget"
      - properties:
          requirement:
            type: "string"
            x-latex-property:
              description: |
                The name of a requirement of the ambient model.
          type:
            type: "string"
            const: "ModelRequirement"
            enum:
            - "ModelRequirement"
            default: "ModelRequirement"
        type: "object"
        title: |
          The target is the requirement of the ambient model.
        required:
        - "requirement"
        - "type"
    MonotoneMap:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Monotone maps"
        type: "object"
        properties:
          dom:
            $ref: "#/components/schemas/Poset"
            description: "Domain of the monotone map"
            x-latex-property:
              symbol: "\\dom"
          cod:
            $ref: "#/components/schemas/Poset"
            description: "Codomain of the monotone map"
            x-latex-property:
              symbol: "\\cod"
          type:
            type: "string"
            enum:
            - "M_AddL"
            - "M_AddLConstant"
            - "M_AddU"
            - "M_AddUConstant"
            - "M_BottomIfNotTop"
            - "M_C_Coproduct"
            - "M_C_CoproductSmash"
            - "M_C_DomProdCodSmash"
            - "M_C_DomSmashCodProd"
            - "M_C_DomUnion"
            - "M_C_Leq_X"
            - "M_C_LiftToSubsets"
            - "M_C_Lt_X"
            - "M_C_Op"
            - "M_C_Parallel"
            - "M_C_ParallelSmash"
            - "M_C_Product"
            - "M_C_ProductSmash"
            - "M_C_RefineDomain"
            - "M_C_Series"
            - "M_C_Sum"
            - "M_C_SumSmash"
            - "M_C_WrapUnits"
            - "M_Ceil0"
            - "M_Coerce"
            - "M_Constant"
            - "M_ContainedInLowerSet"
            - "M_ContainedInUpperSet"
            - "M_DivideLConstant"
            - "M_DivideUConstant"
            - "M_Empty"
            - "M_Explicit"
            - "M_Floor0"
            - "M_Id"
            - "M_IdentityBelowThreshold"
            - "M_Injection"
            - "M_Join"
            - "M_JoinConstant"
            - "M_Leq"
            - "M_Lift"
            - "M_LiftToLowerSets"
            - "M_LiftToUpperSets"
            - "M_Meet"
            - "M_MeetConstant"
            - "M_MultiplyL"
            - "M_MultiplyLConstant"
            - "M_MultiplyU"
            - "M_MultiplyUConstant"
            - "M_PowerFracL"
            - "M_PowerFracU"
            - "M_ReprLowerSet"
            - "M_ReprUpperSet"
            - "M_RepresentPrincipalLowerSet_TotalOrderBounded"
            - "M_RepresentPrincipalUpperSet_TotalOrderBounded"
            - "M_RoundDown"
            - "M_RoundUp"
            - "M_ScaleL"
            - "M_ScaleU"
            - "M_SmashInjection"
            - "M_SubLConstant"
            - "M_SubUConstant"
            - "M_TakeIndex"
            - "M_TakeRange"
            - "M_Threshold1"
            - "M_Threshold2"
            - "M_TopIfNotBottom"
            - "M_Undefined"
            - "M_Unknown"
            - "M_Unlift"
            - "M_X_Leq_C"
            - "M_X_Lt_C"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "MonotoneMap"
            enum:
            - "MonotoneMap"
            default: "MonotoneMap"
        required:
        - "dom"
        - "cod"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 20
          color: "color-monotone-map"
          preamble: |
            \definecolor{color-monotone-map}{rgb}{0.4,0.4,0.8}
            \newcommand{\dom}{\ensuremath{\mathcal{D}}}
            \newcommand{\cod}{\ensuremath{\mathcal{C}}}
        discriminator:
          propertyName: "type"
          mapping:
            M_AddL: "#/components/schemas/M_AddL"
            M_AddLConstant: "#/components/schemas/M_AddLConstant"
            M_AddU: "#/components/schemas/M_AddU"
            M_AddUConstant: "#/components/schemas/M_AddUConstant"
            M_BottomIfNotTop: "#/components/schemas/M_BottomIfNotTop"
            M_C_Coproduct: "#/components/schemas/M_C_Coproduct"
            M_C_CoproductSmash: "#/components/schemas/M_C_CoproductSmash"
            M_C_DomProdCodSmash: "#/components/schemas/M_C_DomProdCodSmash"
            M_C_DomSmashCodProd: "#/components/schemas/M_C_DomSmashCodProd"
            M_C_DomUnion: "#/components/schemas/M_C_DomUnion"
            M_C_Leq_X: "#/components/schemas/M_C_Leq_X"
            M_C_LiftToSubsets: "#/components/schemas/M_C_LiftToSubsets"
            M_C_Lt_X: "#/components/schemas/M_C_Lt_X"
            M_C_Op: "#/components/schemas/M_C_Op"
            M_C_Parallel: "#/components/schemas/M_C_Parallel"
            M_C_ParallelSmash: "#/components/schemas/M_C_ParallelSmash"
            M_C_Product: "#/components/schemas/M_C_Product"
            M_C_ProductSmash: "#/components/schemas/M_C_ProductSmash"
            M_C_RefineDomain: "#/components/schemas/M_C_RefineDomain"
            M_C_Series: "#/components/schemas/M_C_Series"
            M_C_Sum: "#/components/schemas/M_C_Sum"
            M_C_SumSmash: "#/components/schemas/M_C_SumSmash"
            M_C_WrapUnits: "#/components/schemas/M_C_WrapUnits"
            M_Ceil0: "#/components/schemas/M_Ceil0"
            M_Coerce: "#/components/schemas/M_Coerce"
            M_Constant: "#/components/schemas/M_Constant"
            M_ContainedInLowerSet: "#/components/schemas/M_ContainedInLowerSet"
            M_ContainedInUpperSet: "#/components/schemas/M_ContainedInUpperSet"
            M_DivideLConstant: "#/components/schemas/M_DivideLConstant"
            M_DivideUConstant: "#/components/schemas/M_DivideUConstant"
            M_Empty: "#/components/schemas/M_Empty"
            M_Explicit: "#/components/schemas/M_Explicit"
            M_Floor0: "#/components/schemas/M_Floor0"
            M_Id: "#/components/schemas/M_Id"
            M_IdentityBelowThreshold: "#/components/schemas/M_IdentityBelowThreshold"
            M_Injection: "#/components/schemas/M_Injection"
            M_Join: "#/components/schemas/M_Join"
            M_JoinConstant: "#/components/schemas/M_JoinConstant"
            M_Leq: "#/components/schemas/M_Leq"
            M_Lift: "#/components/schemas/M_Lift"
            M_LiftToLowerSets: "#/components/schemas/M_LiftToLowerSets"
            M_LiftToUpperSets: "#/components/schemas/M_LiftToUpperSets"
            M_Meet: "#/components/schemas/M_Meet"
            M_MeetConstant: "#/components/schemas/M_MeetConstant"
            M_MultiplyL: "#/components/schemas/M_MultiplyL"
            M_MultiplyLConstant: "#/components/schemas/M_MultiplyLConstant"
            M_MultiplyU: "#/components/schemas/M_MultiplyU"
            M_MultiplyUConstant: "#/components/schemas/M_MultiplyUConstant"
            M_PowerFracL: "#/components/schemas/M_PowerFracL"
            M_PowerFracU: "#/components/schemas/M_PowerFracU"
            M_ReprLowerSet: "#/components/schemas/M_ReprLowerSet"
            M_ReprUpperSet: "#/components/schemas/M_ReprUpperSet"
            M_RepresentPrincipalLowerSet_TotalOrderBounded: "#/components/schemas/M_RepresentPrincipalLowerSet_TotalOrderBounded"
            M_RepresentPrincipalUpperSet_TotalOrderBounded: "#/components/schemas/M_RepresentPrincipalUpperSet_TotalOrderBounded"
            M_RoundDown: "#/components/schemas/M_RoundDown"
            M_RoundUp: "#/components/schemas/M_RoundUp"
            M_ScaleL: "#/components/schemas/M_ScaleL"
            M_ScaleU: "#/components/schemas/M_ScaleU"
            M_SmashInjection: "#/components/schemas/M_SmashInjection"
            M_SubLConstant: "#/components/schemas/M_SubLConstant"
            M_SubUConstant: "#/components/schemas/M_SubUConstant"
            M_TakeIndex: "#/components/schemas/M_TakeIndex"
            M_TakeRange: "#/components/schemas/M_TakeRange"
            M_Threshold1: "#/components/schemas/M_Threshold1"
            M_Threshold2: "#/components/schemas/M_Threshold2"
            M_TopIfNotBottom: "#/components/schemas/M_TopIfNotBottom"
            M_Undefined: "#/components/schemas/M_Undefined"
            M_Unknown: "#/components/schemas/M_Unknown"
            M_Unlift: "#/components/schemas/M_Unlift"
            M_X_Leq_C: "#/components/schemas/M_X_Leq_C"
            M_X_Lt_C: "#/components/schemas/M_X_Lt_C"
    NDP:
      allOf:
      - $ref: "#/components/schemas/Root"
      - type: "object"
        title: "Named DPs represent a graph of DPs with named nodes and node ports."
        properties:
          F:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              color: "functionalities"
            description: |
              Dictionary of functionalities.
          R:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Poset"
            description: |
              Dictionary of resources.
            x-latex-property:
              color: "requirements"
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          C:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              excluded: true
          J:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              excluded: true
          type:
            type: "string"
            enum:
            - "NDP_Composite"
            - "NDP_Simple"
            - "NDP_Sum"
            - "NDP_TemplateHole"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "NDP"
            enum:
            - "NDP"
            default: "NDP"
        required:
        - "F"
        - "R"
        - "C"
        - "J"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 120
          color: "color-ndp"
          preamble: |
            \definecolor{color-ndp}{rgb}{0.0, 0.5, 0.5}
        discriminator:
          propertyName: "type"
          mapping:
            NDP_Composite: "#/components/schemas/NDP_Composite"
            NDP_Simple: "#/components/schemas/NDP_Simple"
            NDP_Sum: "#/components/schemas/NDP_Sum"
            NDP_TemplateHole: "#/components/schemas/NDP_TemplateHole"
    NDPInterface:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "The interface of a named DP."
        type: "object"
        properties:
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          type:
            type: "string"
            enum:
            - "NDPInterface_Explicit"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "NDPInterface"
            enum:
            - "NDPInterface"
            default: "NDPInterface"
        x-latex-schema:
          top_level: true
          order: 130
          color: "color-ndp"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            NDPInterface_Explicit: "#/components/schemas/NDPInterface_Explicit"
    NDPInterface_Explicit:
      allOf:
      - $ref: "#/components/schemas/NDPInterface"
      - properties:
          fs:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Poset"
            description: "Dictionary from functionality name to poset."
            x-latex-property:
              color: "functionalities"
          rs:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Poset"
            description: "Dictionary from requirement name to poset."
            x-latex-property:
              color: "requirements"
          type:
            type: "string"
            const: "NDPInterface_Explicit"
            enum:
            - "NDPInterface_Explicit"
            default: "NDPInterface_Explicit"
        type: "object"
        title: |
          The interface of a named DP, given by two dictionaries for functionalities and resources.
        required:
        - "fs"
        - "rs"
        - "type"
    NDPTemplate:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "A template for an NDP."
        properties:
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          type:
            type: "string"
            enum:
            - "NDPTemplate_Simple"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "NDPTemplate"
            enum:
            - "NDPTemplate"
            default: "NDPTemplate"
        x-latex-schema:
          top_level: true
          order: 140
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            NDPTemplate_Simple: "#/components/schemas/NDPTemplate_Simple"
    NDPTemplate_Simple:
      allOf:
      - $ref: "#/components/schemas/NDPTemplate"
      - properties:
          type:
            type: "string"
            const: "NDPTemplate_Simple"
            enum:
            - "NDPTemplate_Simple"
            default: "NDPTemplate_Simple"
          ndp:
            $ref: "#/components/schemas/NDP"
            x-latex-property:
              symbol: "N"
              description: |
                The NDP that this template can instantiate. 
                Inside, there are special ``holes''.
          parameters:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/NDPInterface"
            description: |
              The interface of the holes.
        type: "object"
        title: |
          A template described by a graph with holes.
        required:
        - "ndp"
        - "parameters"
        - "type"
    NDP_Composite:
      allOf:
      - $ref: "#/components/schemas/NDP"
      - properties:
          nodes:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/NDP"
            description: |
              A map of node identifiers to their corresponding NDPs in the graph.
              Each key is a unique identifier for a node, and the value is the NDP
              associated with that node.
          connections:
            type: "array"
            items:
              $ref: "#/components/schemas/Connection"
            description: |
              Connections between the nodes in the NDP graph.
          type:
            type: "string"
            const: "NDP_Composite"
            enum:
            - "NDP_Composite"
            default: "NDP_Composite"
        type: "object"
        title: "Graph of NDPs with connections between them."
        required:
        - "connections"
        - "nodes"
        - "type"
    NDP_Simple:
      allOf:
      - $ref: "#/components/schemas/NDP"
      - properties:
          dp:
            $ref: "#/components/schemas/DP"
            description: "The DP that this NDP contains. Must have poset products as resources and functionalities."
          type:
            type: "string"
            const: "NDP_Simple"
            enum:
            - "NDP_Simple"
            default: "NDP_Simple"
        type: "object"
        title: "An NDP that contains a single DP."
        required:
        - "dp"
        - "type"
    NDP_Sum:
      allOf:
      - $ref: "#/components/schemas/NDP"
      - properties:
          dps:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/NDP"
            description: |
              The NDPs to sum.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the NDPs.
          type:
            type: "string"
            const: "NDP_Sum"
            enum:
            - "NDP_Sum"
            default: "NDP_Sum"
        type: "object"
        title: "sum of NDPs"
        required:
        - "dps"
        - "type"
    NDP_TemplateHole:
      allOf:
      - $ref: "#/components/schemas/NDP"
      - properties:
          parameter_name:
            type: "string"
            description: |
              The name of the parameter that is to be filled in.
          type:
            type: "string"
            const: "NDP_TemplateHole"
            enum:
            - "NDP_TemplateHole"
            default: "NDP_TemplateHole"
        type: "object"
        title: "A special NDP to indicate a template hole in the NDP."
        required:
        - "parameter_name"
        - "type"
    NodeFunctionality:
      allOf:
      - $ref: "#/components/schemas/ConnectionTarget"
      - properties:
          node:
            type: "string"
            x-latex-property:
              description: |
                The name of the node that provides the functionality.
          node_functionality:
            type: "string"
            x-latex-property:
              description: |
                The name of the functionality that is provided by the node.
          type:
            type: "string"
            const: "NodeFunctionality"
            enum:
            - "NodeFunctionality"
            default: "NodeFunctionality"
        type: "object"
        title: |
          The target is the functionality of another subproblem.
        required:
        - "node"
        - "node_functionality"
        - "type"
    NodeRequirement:
      allOf:
      - $ref: "#/components/schemas/ConnectionSource"
      - allOf:
        - $ref: "#/components/schemas/ConnectionSource"
        - type: "object"
          title: "The source of a connection is a requirement of another node."
          properties:
            node:
              type: "string"
              x-latex-property:
                description: |
                  The name of the node that provides the requirement.
            node_requirement:
              type: "string"
              x-latex-property:
                description: |
                  The name of the requirement that is provided by the node.
            type:
              type: "string"
              enum:
              - "NodeRequirement"
          required:
          - "node"
          - "node_requirement"
          - "type"
        properties:
          type:
            type: "string"
            const: "NodeRequirement"
            enum:
            - "NodeRequirement"
            default: "NodeRequirement"
        type: "object"
        required:
        - "type"
    P_Bool:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          type:
            type: "string"
            const: "P_Bool"
            enum:
            - "P_Bool"
            default: "P_Bool"
        type: "object"
        title: "The poset of boolean values"
        x-latex-schema:
          group: "10_primitive"
          examples_description:
          - |
            This is how to define the poset. 
            There are no other parameters.
        examples:
        - kind: "Poset"
          type: "P_Bool"
        required:
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as booleans (`true` and `false`) in YAML/CBOR.
          serialization_examples:
          - - value: true
            - value: false
    P_C_Arrow:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_Arrow"
            enum:
            - "P_C_Arrow"
            default: "P_C_Arrow"
        type: "object"
        title: "Arrow constructors for posets."
        x-latex-schema:
          group: "40_composition_single"
          description: |
            The arrow construction of a poset is a poset where the values are pairs of elements from the underlying poset.

            This poset has the same elements as P\_C\_Twisted, but the order is different.
          examples_description:
          - |
            Poset of intervals of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_Arrow"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as array of two elements of the underlying poset.
          serialization_examples:
          - - value:
              - "0"
              - "10"
              description: |
                The interval from 0 to 10, inclusive.
    P_C_Discretized:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_Discretized"
            enum:
            - "P_C_Discretized"
            default: "P_C_Discretized"
        type: "object"
        title: "Discretized version of a poset."
        description: |
          This schema represents a discretized version of a poset, with the elements being the 
          same as the original poset, but where we have forgotten the relations.
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            The poset of boolean values without the order
        examples:
        - kind: "Poset"
          type: "P_C_Discretized"
          poset:
            kind: "Poset"
            type: "P_Bool"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented just like the elements of the underlying poset.
    P_C_Lexicographic:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          type:
            type: "string"
            const: "P_C_Lexicographic"
            enum:
            - "P_C_Lexicographic"
            default: "P_C_Lexicographic"
        type: "object"
        title: "Lexicographic product of posets"
        x-latex-schema:
          group: "50_composition_multi"
          description: |
            This is the lexicographic product of posets.
            The order is defined by the lexicographic order of the elements of the subs posets.

            The elements are the same as in P\_C\_Product, but the order is different.
        examples:
        - kind: "Poset"
          type: "P_C_Lexicographic"
          subs:
          - kind: "Poset"
            type: "P_Decimal"
          - kind: "Poset"
            type: "P_Bool"
        required:
        - "subs"
        - "type"
    P_C_LowerSets:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_LowerSets"
            enum:
            - "P_C_LowerSets"
            default: "P_C_LowerSets"
        type: "object"
        title: "The poset of lower sets of a given poset."
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            Poset of lowersets of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_LowerSets"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as arrays of elements of the underlying poset.
          serialization_examples:
          - - value:
              - "10"
              description: |
                The lower set $\downit{10}$.
    P_C_Multisets:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          values:
            $ref: "#/components/schemas/Poset"
            description: |
              Poset describing the values of the multisets.
            x-latex-property:
              color: "color-poset"
          counts:
            $ref: "#/components/schemas/Poset"
            description: |
              Poset describing the counts of the elements in the multisets.
            x-latex-property:
              color: "color-poset"
          type:
            type: "string"
            const: "P_C_Multisets"
            enum:
            - "P_C_Multisets"
            default: "P_C_Multisets"
        type: "object"
        title: "Poset of multisets"
        x-latex-schema:
          group: "70_misc"
          description: |
            This is the representation of a \emph{generalized multiset}.
            It is created out of two posets: one for the values, and one for the counts.
          excluded: true
        required:
        - "counts"
        - "type"
        - "values"
    P_C_Opposite:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_Opposite"
            enum:
            - "P_C_Opposite"
            default: "P_C_Opposite"
        type: "object"
        title: "Opposite of a poset"
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            The opposite poset of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_Opposite"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented just like the elements of the underlying poset.
    P_C_Power:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_Power"
            enum:
            - "P_C_Power"
            default: "P_C_Power"
        type: "object"
        title: "Power poset of a given poset."
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            Poset of powersets of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_Power"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as arrays of elements of the underlying poset.
          serialization_examples:
          - - value:
              - "10"
              - "20"
              description: |
                The set $\{10, 20\}$.
            - value: []
              description: |
                The empty subset.
    P_C_Product:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          type:
            type: "string"
            const: "P_C_Product"
            enum:
            - "P_C_Product"
            default: "P_C_Product"
        type: "object"
        title: "Cartesian product of posets"
        x-latex-schema:
          group: "50_composition_multi"
          description: |
            This is the Cartesian product of posets.
          preamble: |
            \newcommand{\tupp}[1]{\langle #1 \rangle}
          examples_description:
          - |
            The product of a decimal poset and a boolean poset.
          - |
            This is the empty product. It has exactly one element, the empty tuple.
        examples:
        - kind: "Poset"
          type: "P_C_Product"
          subs:
          - kind: "Poset"
            type: "P_Decimal"
          - kind: "Poset"
            type: "P_Bool"
        - kind: "Poset"
          type: "P_C_Product"
          subs: []
        required:
        - "subs"
        - "type"
        x-mcdp-poset:
          serialization_examples:
          - - value:
              - "10"
              - true
              description: |
                The element $\tupp{10, \top}$.
          - - value: []
              description: |
                The empty tuple
    P_C_ProductDS:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          type:
            type: "string"
            const: "P_C_ProductDS"
            enum:
            - "P_C_ProductDS"
            default: "P_C_ProductDS"
        type: "object"
        title: |
          A product of posets where the elements are dictionaries.
        x-latex-schema:
          group: "50_composition_multi"
          excluded: true
        required:
        - "subs"
        - "type"
    P_C_ProductSmash:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          ranges:
            type: "array"
            items:
              $ref: "#/components/schemas/Range"
            description: |
              The ranges of the posets in the smash product. See also P_C_Sum
          naked:
            type: "array"
            items:
              type: "boolean"
            description: "Whether each poset is \"naked\" or not."
          type:
            type: "string"
            const: "P_C_ProductSmash"
            enum:
            - "P_C_ProductSmash"
            default: "P_C_ProductSmash"
        type: "object"
        title: "Poset smash product"
        x-latex-schema:
          group: "50_composition_multi"
          preamble: |
            \newcommand{\PProductSmash}[1]{\text{ProductSmash}(#1)}
          description: |
            This poset is the ``smash product''. 
            Its elements are concatenation of tuples of elements of the underlying posets. 

            We say that a subposet is ``naked'' if it is not of a ``smash nature'' and its elements need to be wrapped as a tuple.

            The posets that are of a smash nature are:

            \begin{itemize}
                \item Other instances of SELF
                \item Instances of P\_C\_SumSmash
                \item Instances of filters (e.g. P\_F\_Subposet or P\_C\_Units) whose underlying poset is of smash nature.    
            \end{itemize} 

            The \texttt{ranges} parameter describe what is the range in the tuple of each subposet.

            Both the \texttt{ranges} and \texttt{naked} parameters are redundant in the sense that they can be computed from the subs,
            but they are provided for convenience and to avoid recomputing them.


                
                
          examples_description:
          - |
            The empty smash product. It has exactly one element, the empty tuple.
          - |
            This is the smash product of 3 posets: booleans, a smash product of two posets, and the decimals.
            The first and the last posets are naked, while the middle one is not because it is of a smash nature.
        examples:
        - kind: "Poset"
          type: "P_C_ProductSmash"
          subs: []
          naked: []
          ranges: []
        - kind: "Poset"
          type: "P_C_ProductSmash"
          subs:
          - kind: "Poset"
            type: "P_Decimal"
          - kind: "Poset"
            type: "P_C_ProductSmash"
            subs:
            - kind: "Poset"
              type: "P_Decimal"
            - kind: "Poset"
              type: "P_Bool"
            naked:
            - true
            - true
            ranges:
            - type: "Range"
              start: 0
              stop: 1
              ntot: 2
            - type: "Range"
              start: 1
              stop: 2
              ntot: 2
          - kind: "Poset"
            type: "P_Decimal"
          naked:
          - true
          - false
          - true
          ranges:
          - type: "Range"
            start: 0
            stop: 1
            ntot: 4
          - type: "Range"
            start: 1
            stop: 3
            ntot: 4
          - type: "Range"
            start: 3
            stop: 4
            ntot: 4
        required:
        - "naked"
        - "ranges"
        - "subs"
        - "type"
        x-mcdp-poset:
          serialization_examples:
          - - value: []
              description: |
                The empty tuple
          - - value:
              - true
              - "10"
              - false
              - "20"
    P_C_Sum:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          type:
            type: "string"
            const: "P_C_Sum"
            enum:
            - "P_C_Sum"
            default: "P_C_Sum"
        type: "object"
        title: "Direct sum of posets."
        x-latex-schema:
          group: "50_composition_multi"
        examples:
        - kind: "Poset"
          type: "P_C_Sum"
          subs:
          - kind: "Poset"
            type: "P_Decimal"
          - kind: "Poset"
            type: "P_Bool"
        required:
        - "subs"
        - "type"
    P_C_SumSmash:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            description: |
              A list of posets that are composed together.
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the posets.
          trivial:
            type: "boolean"
          ranges:
            type: "array"
            items:
              $ref: "#/components/schemas/Range"
          naked:
            type: "array"
            items:
              type: "boolean"
          type:
            type: "string"
            const: "P_C_SumSmash"
            enum:
            - "P_C_SumSmash"
            default: "P_C_SumSmash"
        type: "object"
        title: "Direct (smash) sum of posets"
        x-latex-schema:
          group: "50_composition_multi"
          description: |
            This is the direct sum of posets but of ``smash nature''.
            See the description of P\_C\_ProductSmash for the description of ranges and naked.
        examples:
        - kind: "Poset"
          type: "P_C_SumSmash"
          trivial: false
          subs:
          - kind: "Poset"
            type: "P_Decimal"
          - kind: "Poset"
            type: "P_Bool"
          naked:
          - true
          - true
          ranges:
          - type: "Range"
            start: 0
            stop: 1
            ntot: 2
          - type: "Range"
            start: 0
            stop: 1
            ntot: 2
        required:
        - "naked"
        - "ranges"
        - "subs"
        - "trivial"
        - "type"
    P_C_Twisted:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_Twisted"
            enum:
            - "P_C_Twisted"
            default: "P_C_Twisted"
        type: "object"
        title: "Twisted arrow construction of a poset."
        x-latex-schema:
          group: "40_composition_single"
          description: |
            The twisted arrow construction of a poset is a poset where the values are pairs of elements from the underlying poset.

            This poset has the same elements as P\_C\_Arrow, but the order is different.
          examples_description:
          - |
            Poset of intervals of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_Twisted"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as array of two elements of the underlying poset.
          serialization_examples:
          - - value:
              - "0"
              - "10"
              description: |
                The interval from 0 to 10, inclusive.
    P_C_Units:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          units:
            $ref: "#/components/schemas/Unit"
            description: |
              The units of the poset.
          type:
            type: "string"
            const: "P_C_Units"
            enum:
            - "P_C_Units"
            default: "P_C_Units"
        type: "object"
        title: "A poset with units"
        description: |
          Decorates a poset with units.

          The units are defined by a Unit object. 
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            The poset of decimal numbers with units of $m^2/s$.
          - |
            This is how to assign the units m (meters) and g (grams) to a product $P\times Q$.
        examples:
        - kind: "Poset"
          type: "P_C_Units"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          units:
            kind: "Unit"
            type: "Unit_Single"
            units: "m^2/s"
        - kind: "Poset"
          type: "P_C_Units"
          poset:
            kind: "Poset"
            type: "P_C_Product"
            subs:
            - kind: "Poset"
              type: "P_Decimal"
            - kind: "Poset"
              type: "P_Decimal"
          units:
            kind: "Unit"
            type: "Unit_Vector"
            subs:
            - kind: "Unit"
              type: "Unit_Single"
              units: "m"
            - kind: "Unit"
              type: "Unit_Single"
              units: "g"
        required:
        - "poset"
        - "type"
        - "units"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented like the elements of the underlying poset.
    P_C_UpperSets:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The base poset.
          type:
            type: "string"
            const: "P_C_UpperSets"
            enum:
            - "P_C_UpperSets"
            default: "P_C_UpperSets"
        type: "object"
        title: "The poset of upper sets of a given poset."
        x-latex-schema:
          group: "40_composition_single"
          examples_description:
          - |
            Poset of uppersets of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_C_UpperSets"
          poset:
            kind: "Poset"
            type: "P_Decimal"
        required:
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as arrays of elements of the underlying poset.
          serialization_examples:
          - - value:
              - "10"
              description: |
                The upper set $\upit{10}$.
    P_Decimal:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          precision:
            description: |
              Number of decimal places.
            type: "integer"
            default: 9
          type:
            type: "string"
            const: "P_Decimal"
            enum:
            - "P_Decimal"
            default: "P_Decimal"
        type: "object"
        title: "Decimal numbers with fixed precision."
        x-latex-schema:
          group: "10_primitive"
        examples:
        - kind: "Poset"
          type: "P_Decimal"
          precision: 9
        required:
        - "precision"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as \emph{strings} in YAML/CBOR.
            This ensures that the numbers are not mangled by the YAML/CBOR parsers.
          serialization_examples:
          - - value: "0.0"
              description: |
                The value 0.0.
            - value: "+inf"
            - value: "-inf"
    P_F_Bounded:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The ambient poset.
            x-latex-property:
              color: "color-poset"
          bottom:
            x-latex-property:
              symbol: "\\BPbottom"
              description: |
                The bottom element of the subposet.
          top:
            x-latex-property:
              symbol: "\\BPtop"
              description: |
                The top element of the subposet.
          step:
            type: "string"
            x-latex-property:
              symbol: "\\BPstep"
              description: |
                The step size of the subposet.
          offset:
            x-latex-property:
              symbol: "\\BPoffset"
              description: |
                The offset
          bound_high:
            x-latex-property:
              symbol: "\\BPboundHigh"
              description: |
                An upper bound.
          bound_low:
            x-latex-property:
              symbol: "\\BPboundLow"
              description: |
                A lower bound.
          type:
            type: "string"
            const: "P_F_Bounded"
            enum:
            - "P_F_Bounded"
            default: "P_F_Bounded"
        type: "object"
        title: "A subposet that allows to sample a numeric poset."
        x-latex-schema:
          group: "60_filters"
          preamble: |
            \newcommand{\BPbottom}{\mathrm{B}} 
            \newcommand{\BPtop}{\mathrm{T}} 
            \newcommand{\BPstep}{\mathrm{S}} 
            \newcommand{\BPoffset}{\mathrm{O}}
            \newcommand{\BPboundHigh}{\mathrm{H}} 
            \newcommand{\BPboundLow}{\mathrm{L}}
          description: |
            A bounded poset is a subposet of a numeric poset that allows to discretize it.

            It is defined by 5 values that satisfy:
            \begin{equation}
                \BPbottom \leq \BPboundLow \leq \BPoffset \leq \BPboundHigh \leq \BPtop
            \end{equation}

            The subposet is defined by
            \begin{equation}
                \{\BPbottom, \BPtop\} \ \cup \ \left(
                                                   [ \BPboundLow, \BPboundHigh ] \ \cap \ % 
                                                   \{ \BPoffset + i \cdot \BPstep \mid i \in \mathbb{Z}\}
                \right)
            \end{equation}
            where $[ \BPboundLow, \BPboundHigh ]$ is the closed interval between $\BPboundLow$ and $\BPboundHigh$.
          examples_description:
          - |
            This defines the nonnegative integers.
          - |
            This defines the odd integers. The offset is $1$ and the step is $2$.
          - |
            This complex example describes the poset with the elements $\{-\infty, 0, 1.5, 3, 4.5, 6,  +\infty\}$.
        examples:
        - kind: "Poset"
          type: "P_F_Bounded"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          bottom: "0"
          bound_low: "0"
          step: "1"
          offset: "0"
          bound_high: "+inf"
          top: "+inf"
        - kind: "Poset"
          type: "P_F_Bounded"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          bottom: "-inf"
          bound_low: "-inf"
          step: "2"
          offset: "1"
          bound_high: "+inf"
          top: "+inf"
        - kind: "Poset"
          type: "P_F_Bounded"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          bottom: "-inf"
          bound_low: "0"
          offset: "0"
          step: "1.5"
          bound_high: "6"
          top: "+inf"
        required:
        - "bottom"
        - "bound_high"
        - "bound_low"
        - "offset"
        - "poset"
        - "step"
        - "top"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          ambient:
            description: |
              The ambient poset that includes the others.
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              color: "color-poset"
          subs:
            description: |
              The posets that are included in the intersection. They are all subposets
              of the ambient poset.
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
            x-latex-property:
              color: "color-poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the posets.
          type:
            type: "string"
            const: "P_F_C_Intersection"
            enum:
            - "P_F_C_Intersection"
            default: "P_F_C_Intersection"
        type: "object"
        title: "Intersection of posets."
        x-latex-schema:
          group: "60_filters"
        required:
        - "ambient"
        - "subs"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_C_Union:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          ambient:
            description: |
              The ambient poset that includes the others.
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              color: "color-poset"
          subs:
            x-latex-property:
              color: "color-poset"
            description: |
              The posets that are included in the union. They are all subposets
              of the ambient poset.
            type: "array"
            items:
              $ref: "#/components/schemas/Poset"
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the posets.
          type:
            type: "string"
            const: "P_F_C_Union"
            enum:
            - "P_F_C_Union"
            default: "P_F_C_Union"
        type: "object"
        title: "Union of posets"
        description: |
          Note this is different from the `Sum` of posets (tagged union).
        x-latex-schema:
          group: "60_filters"
          examples_description:
          - |
            This describes the poset $[10, 20] \cup [30, 35]$ in the ambient poset of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_F_C_Union"
          ambient:
            kind: "Poset"
            type: "P_Decimal"
          subs:
          - kind: "Poset"
            type: "P_F_Interval"
            poset:
              kind: "Poset"
              type: "P_Decimal"
            low: "10"
            high: "20"
          - kind: "Poset"
            type: "P_F_Interval"
            poset:
              kind: "Poset"
              type: "P_Decimal"
            low: "30"
            high: "35"
        required:
        - "ambient"
        - "subs"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_Interval:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            description: "The ambient poset."
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              color: "color-poset"
          low:
            x-latex-property:
              symbol: "a"
            description: "The lower bound of the interval."
          high:
            x-latex-property:
              symbol: "b"
            description: "The upper bound of the interval."
          type:
            type: "string"
            const: "P_F_Interval"
            enum:
            - "P_F_Interval"
            default: "P_F_Interval"
        type: "object"
        title: |
          An interval in a poset.
        x-latex-schema:
          group: "60_filters"
          description: |
            Let $P$  be a poset, and let $a$  and $b$  be elements of $P$.
            Then this poset represents the subposet in $P$, given by $\{x \in P \mid a \leq x \leq b \}$.
          examples_description:
          - |
            This describes the interval $[10, 20]$ in the ambient poset of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_F_Interval"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          low: "10"
          high: "20"
        required:
        - "high"
        - "low"
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_LowerClosure:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: "The ambient poset."
            x-latex-property:
              color: "color-poset"
          ls:
            $ref: "#/components/schemas/LowerSet"
            description: "The lower set."
          type:
            type: "string"
            const: "P_F_LowerClosure"
            enum:
            - "P_F_LowerClosure"
            default: "P_F_LowerClosure"
        type: "object"
        title: "Lower closure in a poset."
        x-latex-schema:
          group: "60_filters"
          examples_description:
          - |
            This describes the lower closure of the set $\{\tupp{10, \top}, \tupp{20, \bot}\}$ in the ambient poset of decimal numbers and booleans.
        examples:
        - kind: "Poset"
          type: "P_F_LowerClosure"
          poset:
            kind: "Poset"
            type: "P_C_Product"
            subs:
            - kind: "Poset"
              type: "P_Decimal"
            - kind: "Poset"
              type: "P_Bool"
          ls:
            kind: "LowerSet"
            type: "LowerSet_LowerClosure"
            points:
            - - "10"
              - true
            - - "20"
              - false
        required:
        - "ls"
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_Subposet:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          elements:
            description: |
              The elements of the subposet.
            type: "array"
            items: {}
          poset:
            $ref: "#/components/schemas/Poset"
            description: |
              The ambient poset that contains the elements.
            x-latex-property:
              color: "color-poset"
          type:
            type: "string"
            const: "P_F_Subposet"
            enum:
            - "P_F_Subposet"
            default: "P_F_Subposet"
        type: "object"
        title: "A finite subposet of an ambient poset."
        x-latex-schema:
          group: "60_filters"
          description: |
            A finite subposet of an ambient poset specified by a list of elements.

            As a particular case, the list of elements can be empty. 
          examples_description:
          - |
            This describes the subposet $\{0.1, 0.2\}$ in the ambient poset of decimal numbers.
          - |
            This describes the empty subposet in the ambient poset of decimal numbers.
        examples:
        - kind: "Poset"
          type: "P_F_Subposet"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          elements:
          - "0.1"
          - "0.2"
        - kind: "Poset"
          type: "P_F_Subposet"
          poset:
            kind: "Poset"
            type: "P_Decimal"
          elements: []
        required:
        - "elements"
        - "poset"
        - "type"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_F_UpperClosure:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            description: "The ambient poset."
          us:
            $ref: "#/components/schemas/UpperSet"
            description: "The upper set."
          type:
            type: "string"
            const: "P_F_UpperClosure"
            enum:
            - "P_F_UpperClosure"
            default: "P_F_UpperClosure"
        type: "object"
        title: "Upper closure in a poset."
        x-latex-schema:
          group: "60_filters"
          examples_description:
          - |
            This describes the upper closure of the set $\{(10, \top), (20, \bot)\}$ in the ambient poset of decimal numbers and booleans.
        examples:
        - kind: "Poset"
          type: "P_F_UpperClosure"
          poset:
            kind: "Poset"
            type: "P_C_Product"
            subs:
            - kind: "Poset"
              type: "P_Decimal"
            - kind: "Poset"
              type: "P_Bool"
          us:
            kind: "UpperSet"
            type: "UpperSet_UpperClosure"
            points:
            - - "10"
              - true
            - - "20"
              - false
        required:
        - "poset"
        - "type"
        - "us"
        x-mcdp-poset:
          serialization: |
            As a ``filter'', the values of this poset are represented as in the ambient poset.
    P_Finite:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          elements:
            type: "array"
            items:
              type: "string"
            description: |
              The elements of the poset, strings.
          relations:
            type: "array"
            items:
              type: "array"
              items:
                type: "string"
            description: |
              The relations of the poset, each relation is a pair of elements.
              The first element is less than the second element.
          aliases:
            type: "object"
            additionalProperties:
              type: "array"
              items:
                type: "string"
            description: |
              Aliases for the elements of the poset.
              The keys are the aliases, and the values are arrays of elements that are equivalent to the alias.
          type:
            type: "string"
            const: "P_Finite"
            enum:
            - "P_Finite"
            default: "P_Finite"
        type: "object"
        title: "Arbitrary finite poset"
        x-latex-schema:
          group: "10_primitive"
          description: |
            A finite poset is a set of elements with a partial order defined by a set of relations.
          examples_description:
          - |
            This is the empty poset.
          - |
            This is a poset with two elements that are not related.
          - |
            This is a poset with two elements with $a \leq b$.
          - |
            This is a pre-order with 5 elements: \texttt{a}, \texttt{b}, \texttt{c}, \texttt{a1}, and \texttt{a2}.
            The elements \texttt{a}, \texttt{a1}, \texttt{a2} are equivalent. 
            The element \texttt{a} is used as the representative of the equivalence class.
        examples:
        - kind: "Poset"
          type: "P_Finite"
          elements: []
          relations: []
          aliases: {}
        - kind: "Poset"
          type: "P_Finite"
          elements:
          - "a"
          - "b"
          relations: []
        - kind: "Poset"
          type: "P_Finite"
          elements:
          - "a"
          - "b"
          relations:
          - - "a"
            - "b"
        - kind: "Poset"
          type: "P_Finite"
          elements:
          - "a"
          - "b"
          - "c"
          relations:
          - - "a"
            - "b"
          - - "b"
            - "c"
          aliases:
            a:
            - "a1"
            - "a2"
        required:
        - "elements"
        - "relations"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as \emph{strings} in YAML/CBOR.
          serialization_examples:
          - []
          - []
          - []
          - - value: "a"
            - value: "a2"
    P_Float:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          size:
            description: |
              Precision of the floating point number.
              Current supported values are f32 and f64.
            type: "string"
            enum:
            - "f8"
            - "f16"
            - "f32"
            - "f64"
            - "f80"
            - "f128"
          type:
            type: "string"
            const: "P_Float"
            enum:
            - "P_Float"
            default: "P_Float"
        type: "object"
        title: "Poset of floating point numbers."
        x-latex-schema:
          group: "10_primitive"
        examples:
        - kind: "Poset"
          type: "P_Float"
          size: "f32"
        required:
        - "size"
        - "type"
    P_Fractions:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          size:
            description: |
              Precision of the fraction.
            type: "string"
            enum:
            - "i8"
            - "i16"
            - "i32"
            - "i64"
            - "i128"
          max_abs_numerator:
            type: "integer"
            description: |
              Maximum absolute value for the numerator.
          max_abs_denominator:
            type: "integer"
            description: |
              Maximum absolute value for the denominator.
          type:
            type: "string"
            const: "P_Fractions"
            enum:
            - "P_Fractions"
            default: "P_Fractions"
        type: "object"
        title: |
          Fractions with a maximum absolute value for numerator and denominator.
        x-latex-schema:
          group: "10_primitive"
          description: |
            Fractions with a maximum absolute value for numerator and denominator.
        examples:
        - kind: "Poset"
          type: "P_Fractions"
          size: "i32"
          max_abs_numerator: 1000
          max_abs_denominator: 1000
        required:
        - "max_abs_denominator"
        - "max_abs_numerator"
        - "size"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as strings ``numerator/denominator'' in YAML/CBOR.
          serialization_examples:
          - - value: "0"
            - value: "1/0"
            - value: "-1/0"
    P_Integer:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          size:
            description: |
              Bit size of the integer.
            type: "string"
            enum:
            - "i8"
            - "i16"
            - "i32"
            - "i64"
            - "i128"
          type:
            type: "string"
            const: "P_Integer"
            enum:
            - "P_Integer"
            default: "P_Integer"
        type: "object"
        title: "Poset of integers."
        x-latex-schema:
          group: "10_primitive"
        examples:
        - kind: "Poset"
          type: "P_Integer"
          size: "i32"
        required:
        - "size"
        - "type"
        x-mcdp-poset:
          serialization: |
            The values of this poset are represented as strings in YAML/CBOR.
          serialization_examples:
          - - value: "0"
              description: |
                The value 0.
            - value: "1000000000000000"
              description: |
                A large positive integer.
    P_Unknown:
      allOf:
      - $ref: "#/components/schemas/Poset"
      - properties:
          type:
            type: "string"
            const: "P_Unknown"
            enum:
            - "P_Unknown"
            default: "P_Unknown"
        type: "object"
        title: "Placeholder for an unknown poset"
        description: |
          This schema is used internally when the type of the poset is not known.
        x-latex-schema:
          group: "10_primitive"
        examples:
        - kind: "Poset"
          type: "P_Unknown"
        required:
        - "type"
    Poset:
      allOf:
      - $ref: "#/components/schemas/Root"
      - properties:
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          type:
            type: "string"
            enum:
            - "P_Bool"
            - "P_C_Arrow"
            - "P_C_Discretized"
            - "P_C_Lexicographic"
            - "P_C_LowerSets"
            - "P_C_Multisets"
            - "P_C_Opposite"
            - "P_C_Power"
            - "P_C_Product"
            - "P_C_ProductDS"
            - "P_C_ProductSmash"
            - "P_C_Sum"
            - "P_C_SumSmash"
            - "P_C_Twisted"
            - "P_C_Units"
            - "P_C_UpperSets"
            - "P_Decimal"
            - "P_F_Bounded"
            - "P_F_C_Intersection"
            - "P_F_C_Union"
            - "P_F_Interval"
            - "P_F_LowerClosure"
            - "P_F_Subposet"
            - "P_F_UpperClosure"
            - "P_Finite"
            - "P_Float"
            - "P_Fractions"
            - "P_Integer"
            - "P_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "Poset"
            enum:
            - "Poset"
            default: "Poset"
        title: "A poset."
        x-latex-schema:
          top_level: true
          order: 1
          color: "color-poset"
          preamble: |
            \definecolor{color-poset}{rgb}{0.8,0.2,0.2}
          description: |
            The Poset objects represent various kinds of posets. 

            Some of these are ``primitive'' posets, such as P\_Decimal, P\_Bool, P\_Finite.

            Some of these are ``composite'' posets. They are indicated by a prefix P\_C\_.
            Some examples are P\_C\_Product and P\_C\_Sum.


            The posets whose name starts with P\_F are ``filters''. These represent a subposet of another poset.
            Some examples are P\_F\_Interval and P\_F\_Subposet.


            Each poset defines also the format in which its data can be serialized in YAML/CBOR.
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            P_Bool: "#/components/schemas/P_Bool"
            P_C_Arrow: "#/components/schemas/P_C_Arrow"
            P_C_Discretized: "#/components/schemas/P_C_Discretized"
            P_C_Lexicographic: "#/components/schemas/P_C_Lexicographic"
            P_C_LowerSets: "#/components/schemas/P_C_LowerSets"
            P_C_Multisets: "#/components/schemas/P_C_Multisets"
            P_C_Opposite: "#/components/schemas/P_C_Opposite"
            P_C_Power: "#/components/schemas/P_C_Power"
            P_C_Product: "#/components/schemas/P_C_Product"
            P_C_ProductDS: "#/components/schemas/P_C_ProductDS"
            P_C_ProductSmash: "#/components/schemas/P_C_ProductSmash"
            P_C_Sum: "#/components/schemas/P_C_Sum"
            P_C_SumSmash: "#/components/schemas/P_C_SumSmash"
            P_C_Twisted: "#/components/schemas/P_C_Twisted"
            P_C_Units: "#/components/schemas/P_C_Units"
            P_C_UpperSets: "#/components/schemas/P_C_UpperSets"
            P_Decimal: "#/components/schemas/P_Decimal"
            P_F_Bounded: "#/components/schemas/P_F_Bounded"
            P_F_C_Intersection: "#/components/schemas/P_F_C_Intersection"
            P_F_C_Union: "#/components/schemas/P_F_C_Union"
            P_F_Interval: "#/components/schemas/P_F_Interval"
            P_F_LowerClosure: "#/components/schemas/P_F_LowerClosure"
            P_F_Subposet: "#/components/schemas/P_F_Subposet"
            P_F_UpperClosure: "#/components/schemas/P_F_UpperClosure"
            P_Finite: "#/components/schemas/P_Finite"
            P_Float: "#/components/schemas/P_Float"
            P_Fractions: "#/components/schemas/P_Fractions"
            P_Integer: "#/components/schemas/P_Integer"
            P_Unknown: "#/components/schemas/P_Unknown"
    Projection:
      title: "Projection from a product."
      type: "object"
      properties:
        type:
          type: "string"
          enum:
          - "Projection"
          description: "Type marker"
        index:
          type: "integer"
          description: "The index of the element to project."
        ntot:
          type: "integer"
          description: "The total number of elements in the product."
      required:
      - "type"
      - "index"
      - "ntot"
      x-latex-schema:
        top_level: false
    Query:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Queries"
        properties:
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          type:
            type: "string"
            enum:
            - "Query_Single"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "Query"
            enum:
            - "Query"
            default: "Query"
        x-latex-schema:
          top_level: true
          order: 150
          description: |
            Queries represent the questions that can be asked about a model.
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            Query_Single: "#/components/schemas/Query_Single"
    QueryData:
      properties:
        type:
          type: "string"
          enum:
          - "QueryFixFunMinReqData"
          - "QueryFixReqMaxFunData"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          QueryFixFunMinReqData: "#/components/schemas/QueryFixFunMinReqData"
          QueryFixReqMaxFunData: "#/components/schemas/QueryFixReqMaxFunData"
      title: "Query data"
      required:
      - "type"
    QueryFixFunMinReqData:
      allOf:
      - $ref: "#/components/schemas/QueryData"
      - properties:
          type:
            type: "string"
            const: "QueryFixFunMinReqData"
            enum:
            - "QueryFixFunMinReqData"
            default: "QueryFixFunMinReqData"
          f:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Value"
            x-latex-property:
              description: |
                Lower bounds for the functionalities.
              color: "functionalities"
          r:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Value"
            x-latex-property:
              description: |
                Upper bounds for the requirements.
              color: "requirements"
          optimize_for:
            type: "array"
            items:
              type: "string"
            x-latex-property:
              color: "requirements"
              description: |
                The names of the functionalities that are optimized for.
                This is a list of the keys of the `f` object.
        type: "object"
        title: "Data for the query `FixFunMinReq`"
        required:
        - "f"
        - "optimize_for"
        - "r"
        - "type"
    QueryFixReqMaxFunData:
      allOf:
      - $ref: "#/components/schemas/QueryData"
      - properties:
          type:
            type: "string"
            const: "QueryFixReqMaxFunData"
            enum:
            - "QueryFixReqMaxFunData"
            default: "QueryFixReqMaxFunData"
          f:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Value"
            x-latex-property:
              color: "functionalities"
              description: |
                Lower bounds for the functionalities.
          r:
            type: "object"
            additionalProperties:
              $ref: "#/components/schemas/Value"
            x-latex-property:
              color: "requirements"
              description: |
                Upper bounds for the requirements.
          optimize_for:
            type: "array"
            items:
              type: "string"
            x-latex-property:
              color: "functionalities"
              description: |
                The functionalities that are optimized for.
                This is a list of the keys of the `f` object.
        type: "object"
        title: "Data for the query `FixReqMaxFun`"
        required:
        - "f"
        - "optimize_for"
        - "r"
        - "type"
    Query_Single:
      allOf:
      - $ref: "#/components/schemas/Query"
      - properties:
          model:
            $ref: "#/components/schemas/NDP"
            x-latex-property:
              description: |
                The model to which the query applies.
          query_data:
            $ref: "#/components/schemas/QueryData"
            x-latex-property:
              description: |
                The data that is used to answer the query.
          type:
            type: "string"
            const: "Query_Single"
            enum:
            - "Query_Single"
            default: "Query_Single"
        type: "object"
        title: "Single query"
        required:
        - "model"
        - "query_data"
        - "type"
    Range:
      title: "Description of a range of integers."
      type: "object"
      properties:
        start:
          type: "integer"
          description: "Start of the range (inclusive)."
        stop:
          type: "integer"
          description: "End of the range (exclusive)."
        ntot:
          type: "integer"
          description: "Total number of elements in the array."
        type:
          type: "string"
          enum:
          - "Range"
          description: "Type marker"
      required:
      - "start"
      - "stop"
      - "ntot"
      - "type"
      x-latex-schema:
        top_level: false
        description: "This represents the range \\texttt{start:stop} (inclusive of start, exclusive of stop) in a total of \\texttt{ntot} elements."
    Root:
      properties:
        version:
          type: "string"
          description: "Version of the MCDP format used to serialize this object (major.minor)."
        description:
          type: "string"
          description: "A human-readable description of the object used for debug purposes."
        hash:
          type: "string"
          description: "Unique hash for the object."
        kind:
          type: "string"
          enum:
          - "Check"
          - "ComputeJob"
          - "DP"
          - "L1Map"
          - "LMap"
          - "MonotoneMap"
          - "NDP"
          - "NDPInterface"
          - "NDPTemplate"
          - "Poset"
          - "Query"
          - "SL1Map"
          - "SLMap"
          - "SU1Map"
          - "SUMap"
          - "U1Map"
          - "UMap"
          - "Value"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "kind"
        mapping:
          Check: "#/components/schemas/Check"
          ComputeJob: "#/components/schemas/ComputeJob"
          DP: "#/components/schemas/DP"
          L1Map: "#/components/schemas/L1Map"
          LMap: "#/components/schemas/LMap"
          MonotoneMap: "#/components/schemas/MonotoneMap"
          NDP: "#/components/schemas/NDP"
          NDPInterface: "#/components/schemas/NDPInterface"
          NDPTemplate: "#/components/schemas/NDPTemplate"
          Poset: "#/components/schemas/Poset"
          Query: "#/components/schemas/Query"
          SL1Map: "#/components/schemas/SL1Map"
          SLMap: "#/components/schemas/SLMap"
          SU1Map: "#/components/schemas/SU1Map"
          SUMap: "#/components/schemas/SUMap"
          U1Map: "#/components/schemas/U1Map"
          UMap: "#/components/schemas/UMap"
          Value: "#/components/schemas/Value"
      title: "Top-level object types for what can be serialized in a file."
      description: "The Root schema contains as subtypes all kinds of objects that can serialized in a MCDP file during an export operation."
      required:
      - "kind"
    SL1Check:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Check_Data"
          type:
            type: "string"
            const: "SL1Check"
            enum:
            - "SL1Check"
            default: "SL1Check"
        type: "object"
        title: "Check for a SL1Map."
        required:
        - "data"
        - "m"
        - "type"
    SL1Check_Data:
      type: "object"
      title: "An input-output pair for the SL1Map"
      properties:
        x: {}
        pess: {}
        opt: {}
        pess_y:
          $ref: "#/components/schemas/LowerSet"
        opt_y:
          $ref: "#/components/schemas/LowerSet"
        pess_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
        opt_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "pess"
      - "opt"
      - "pess_y"
      - "opt_y"
      x-latex-schema:
        title: "An input-output pair"
    SL1Map:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Scalable map to lower sets of functionalities."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkdom"
              description: "The Kleisli domain of the map."
              color: "requirements"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\lkcod"
              description: "The Kleisli codomain of the map."
              color: "functionalities"
          pes:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\pes"
              description: "The resolution poset for pessimistic estimate."
          opt:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\opt"
              description: "The resolution poset for optimistic estimate."
          type:
            type: "string"
            enum:
            - "SL1_C_CodSum"
            - "SL1_C_CodSumSmash"
            - "SL1_C_ExplicitApprox"
            - "SL1_C_Intersection"
            - "SL1_C_Parallel"
            - "SL1_C_ProdIntersection"
            - "SL1_C_Product"
            - "SL1_C_RefineDomain"
            - "SL1_C_Series"
            - "SL1_C_Trace"
            - "SL1_C_Union"
            - "SL1_C_WrapUnits"
            - "SL1_Exact"
            - "SL1_Identity"
            - "SL1_InvMultiply"
            - "SL1_InvSum"
            - "SL1_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "SL1Map"
            enum:
            - "SL1Map"
            default: "SL1Map"
        required:
        - "kdom"
        - "kcod"
        - "pes"
        - "opt"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 70
          color: "color-lower"
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            SL1_C_CodSum: "#/components/schemas/SL1_C_CodSum"
            SL1_C_CodSumSmash: "#/components/schemas/SL1_C_CodSumSmash"
            SL1_C_ExplicitApprox: "#/components/schemas/SL1_C_ExplicitApprox"
            SL1_C_Intersection: "#/components/schemas/SL1_C_Intersection"
            SL1_C_Parallel: "#/components/schemas/SL1_C_Parallel"
            SL1_C_ProdIntersection: "#/components/schemas/SL1_C_ProdIntersection"
            SL1_C_Product: "#/components/schemas/SL1_C_Product"
            SL1_C_RefineDomain: "#/components/schemas/SL1_C_RefineDomain"
            SL1_C_Series: "#/components/schemas/SL1_C_Series"
            SL1_C_Trace: "#/components/schemas/SL1_C_Trace"
            SL1_C_Union: "#/components/schemas/SL1_C_Union"
            SL1_C_WrapUnits: "#/components/schemas/SL1_C_WrapUnits"
            SL1_Exact: "#/components/schemas/SL1_Exact"
            SL1_Identity: "#/components/schemas/SL1_Identity"
            SL1_InvMultiply: "#/components/schemas/SL1_InvMultiply"
            SL1_InvSum: "#/components/schemas/SL1_InvSum"
            SL1_Unknown: "#/components/schemas/SL1_Unknown"
    SL1_C_CodSum:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_CodSum"
            enum:
            - "SL1_C_CodSum"
            default: "SL1_C_CodSum"
        type: "object"
        title: "Sum of maps"
        x-latex-schema:
          group: "20_compositions"
          description: "This is the generalization of L1\\_C\\_CodSum."
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_Sum(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
    SL1_C_CodSumSmash:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_CodSumSmash"
            enum:
            - "SL1_C_CodSumSmash"
            default: "SL1_C_CodSumSmash"
        type: "object"
        title: "Smash sum"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of L1\_C\_CodSumSmash.
        required:
        - "ms"
        - "type"
    SL1_C_ExplicitApprox:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          optimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            description: |
              The optimistic approximations of the L1Map.
          optimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the optimistic approximations.
          pessimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/L1Map"
            description: |
              The pessimistic approximations of the L1Map.
          pessimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the pessimistic approximations.
          type:
            type: "string"
            const: "SL1_C_ExplicitApprox"
            enum:
            - "SL1_C_ExplicitApprox"
            default: "SL1_C_ExplicitApprox"
        type: "object"
        title: "Constructs a SL1Map from explicit approximations of L1Map maps."
        x-latex-schema:
          group: "70_other"
        required:
        - "optimistic"
        - "pessimistic"
        - "type"
    SL1_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_Intersection"
            enum:
            - "SL1_C_Intersection"
            default: "SL1_C_Intersection"
        type: "object"
        title: "Intersection of SL1 maps"
        x-latex-schema:
          group: "20_compositions_20_union_int"
          description: |
            This is the generalization of L1\_C\_Intersection.
        required:
        - "ms"
        - "type"
    SL1_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_Parallel"
            enum:
            - "SL1_C_Parallel"
            default: "SL1_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "10_compositions_10_simple"
          description: "This is the generalization of L1\\_C\\_Parallel."
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SL1_C_ProdIntersection:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_ProdIntersection"
            enum:
            - "SL1_C_ProdIntersection"
            default: "SL1_C_ProdIntersection"
        type: "object"
        title: "Product of domains, intersection of codomains"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of L1\_C\_ProdIntersection.
        required:
        - "ms"
        - "type"
    SL1_C_Product:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_Product"
            enum:
            - "SL1_C_Product"
            default: "SL1_C_Product"
        type: "object"
        title: "Product of SL1 maps"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of L1\_C\_Product.
        required:
        - "ms"
        - "type"
    SL1_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed"
          type:
            type: "string"
            const: "SL1_C_RefineDomain"
            enum:
            - "SL1_C_RefineDomain"
            default: "SL1_C_RefineDomain"
        type: "object"
        title: "Refinement of the domain"
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of L1\\_C\\_RefineDomain."
        required:
        - "m"
        - "type"
    SL1_C_Series:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_Series"
            enum:
            - "SL1_C_Series"
            default: "SL1_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "10_compositions_10_simple"
          description: "This is the generalization of L1\\_C\\_Series."
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SL1_C_Trace:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed"
          type:
            type: "string"
            const: "SL1_C_Trace"
            enum:
            - "SL1_C_Trace"
            default: "SL1_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of L1\\_C\\_Trace."
        required:
        - "m"
        - "type"
    SL1_C_Union:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "\\lmor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL1_C_Union"
            enum:
            - "SL1_C_Union"
            default: "SL1_C_Union"
        type: "object"
        title: "Union of SL1 maps"
        x-latex-schema:
          group: "20_compositions_20_union_int"
          description: |
            This is the generalization of L1\_C\_Union.
        required:
        - "ms"
        - "type"
    SL1_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "m"
              description: "The map to be transformed"
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              The units for the domain.
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              The units for the codomain.
          type:
            type: "string"
            const: "SL1_C_WrapUnits"
            enum:
            - "SL1_C_WrapUnits"
            default: "SL1_C_WrapUnits"
        type: "object"
        title: "Decorates a map with units for the domain and codomain."
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of L1\\_C\\_WrapUnits."
        required:
        - "kcod_units"
        - "kdom_units"
        - "m"
        - "type"
    SL1_Exact:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          m:
            $ref: "#/components/schemas/L1Map"
            x-latex-property:
              symbol: "\\lmor"
            description: |
              The L1Map to lift.
          type:
            type: "string"
            const: "SL1_Exact"
            enum:
            - "SL1_Exact"
            default: "SL1_Exact"
        type: "object"
        title: "Lifts a L1Map to a SL1Map."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
    SL1_Identity:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          type:
            type: "string"
            const: "SL1_Identity"
            enum:
            - "SL1_Identity"
            default: "SL1_Identity"
        type: "object"
        title: "Identity"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SL1_InvMultiply:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
            description: "The poset where the operation takes place"
          type:
            type: "string"
            const: "SL1_InvMultiply"
            enum:
            - "SL1_InvMultiply"
            default: "SL1_InvMultiply"
        type: "object"
        title: "The lower inverse of multiplication."
        x-latex-schema:
          group: "60_math"
        required:
        - "opspace"
        - "type"
    SL1_InvSum:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
            description: "The poset where the operation takes place"
          type:
            type: "string"
            const: "SL1_InvSum"
            enum:
            - "SL1_InvSum"
            default: "SL1_InvSum"
        type: "object"
        title: "The lower inverse of addition."
        x-latex-schema:
          group: "60_math"
        required:
        - "opspace"
        - "type"
    SL1_Unknown:
      allOf:
      - $ref: "#/components/schemas/SL1Map"
      - properties:
          type:
            type: "string"
            const: "SL1_Unknown"
            enum:
            - "SL1_Unknown"
            default: "SL1_Unknown"
        type: "object"
        title: "Placeholder for an unknown SL1Map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SLCheck:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/SLMap"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/SLCheck_Data"
          type:
            type: "string"
            const: "SLCheck"
            enum:
            - "SLCheck"
            default: "SLCheck"
        type: "object"
        title: "Check for a SL1Map."
        required:
        - "data"
        - "m"
        - "type"
    SLCheck_Data:
      type: "object"
      title: "An input-output pair for the SLMap"
      properties:
        x: {}
        pess: {}
        opt: {}
        pess_y:
          $ref: "#/components/schemas/LowerSet"
        opt_y:
          $ref: "#/components/schemas/LowerSet"
        pess_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
        opt_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "pess"
      - "opt"
      - "pess_y"
      - "opt_y"
    SLMap:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Scalable map to lower sets of functionalities and implementations."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            description: "Kleisli domain of the map."
            x-latex-property:
              symbol: "\\lkdom"
              color: "requirements"
          kcod:
            $ref: "#/components/schemas/Poset"
            description: "Kleisli co-domain of the map."
            x-latex-property:
              symbol: "\\lkcod"
              color: "functionalities"
          kimp:
            $ref: "#/components/schemas/Poset"
            description: "Poset of implementations."
            x-latex-property:
              symbol: "\\lkimp"
              color: "implementations"
          pes:
            $ref: "#/components/schemas/Poset"
            description: "Poset of resolutions (pessimistic)"
            x-latex-property:
              symbol: "\\pes"
          opt:
            $ref: "#/components/schemas/Poset"
            description: "Poset of resolutions (optimistic)"
            x-latex-property:
              symbol: "\\opt"
          type:
            type: "string"
            enum:
            - "SL_C_ITransform"
            - "SL_C_Intersection"
            - "SL_C_Parallel"
            - "SL_C_RefineDomain"
            - "SL_C_Series"
            - "SL_C_Trace"
            - "SL_C_Union"
            - "SL_C_WrapUnits"
            - "SL_Identity"
            - "SL_L_Exact"
            - "SL_L_Explicit_Approx"
            - "SL_L_Lift1_Constant"
            - "SL_L_Lift1_Transform"
            - "SL_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "SLMap"
            enum:
            - "SLMap"
            default: "SLMap"
        required:
        - "kdom"
        - "kcod"
        - "kimp"
        - "pes"
        - "opt"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 90
          color: "color-lower"
          preamble: |
            \newcommand{\lkdom}{{\color{requirements}\text{KDom}}}
            \newcommand{\lkcod}{{\color{functionalities}\text{KCod}}}
            \newcommand{\lkimp}{{\color{implementations}\text{KImp}}}
            \newcommand{\ukdom}{{\color{functionalities}\text{KDom}}}
            \newcommand{\ukcod}{{\color{requirements}\text{KCod}}}
            \newcommand{\ukimp}{{\color{implementations}\text{KImp}}}
            \newcommand{\pes}{\text{Pes}}
            \newcommand{\opt}{\text{Opt}}
            \newcommand{\mlopt}{m_\text{opt}}
            \newcommand{\mlpes}{m_\text{pes}}
            \newcommand{\muopt}{m_\text{opt}}
            \newcommand{\mupes}{_\text{pes}}
            \newcommand{\Pos}{\mathbf{Pos}}
            \newcommand{\toinpos}{\quad{{\to}_{\Pos}}\quad}
            \newcommand{\op}{\text{op}}
            \newcommand{\signSLMap}[5]{#1 \stackrel{#3,#4,#5}{\to_{\text{\textbf{SLMap}}}} #2 }
            \newcommand{\signSUMap}[5]{#1 \stackrel{#3,#4,#5}{\to_{\text{\textbf{SUMap}}}} #2 }
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            SL_C_ITransform: "#/components/schemas/SL_C_ITransform"
            SL_C_Intersection: "#/components/schemas/SL_C_Intersection"
            SL_C_Parallel: "#/components/schemas/SL_C_Parallel"
            SL_C_RefineDomain: "#/components/schemas/SL_C_RefineDomain"
            SL_C_Series: "#/components/schemas/SL_C_Series"
            SL_C_Trace: "#/components/schemas/SL_C_Trace"
            SL_C_Union: "#/components/schemas/SL_C_Union"
            SL_C_WrapUnits: "#/components/schemas/SL_C_WrapUnits"
            SL_Identity: "#/components/schemas/SL_Identity"
            SL_L_Exact: "#/components/schemas/SL_L_Exact"
            SL_L_Explicit_Approx: "#/components/schemas/SL_L_Explicit_Approx"
            SL_L_Lift1_Constant: "#/components/schemas/SL_L_Lift1_Constant"
            SL_L_Lift1_Transform: "#/components/schemas/SL_L_Lift1_Transform"
            SL_Unknown: "#/components/schemas/SL_Unknown"
    SL_C_ITransform:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SLMap"
            x-latex-property:
              symbol: "m"
              description: |
                The SLMap to be transformed.
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "f"
              description: |
                The monotone map that transforms the implementations of the SLMap.
          type:
            type: "string"
            const: "SL_C_ITransform"
            enum:
            - "SL_C_ITransform"
            default: "SL_C_ITransform"
        type: "object"
        title: "Transforms the implementations of a SLMap."
        x-latex-schema:
          group: "23_comp_singe"
          description: "This is the generalization of L\\_C\\_ITransform."
        required:
        - "m"
        - "transform"
        - "type"
    SL_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SLMap"
            description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL_C_Intersection"
            enum:
            - "SL_C_Intersection"
            default: "SL_C_Intersection"
        type: "object"
        title: "Intersection of the results of a set of maps."
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of L\_C\_Intersection.
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom:
            kcod:
            kimp: "P_C_ProductSmash(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SL_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SLMap"
            description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL_C_Parallel"
            enum:
            - "SL_C_Parallel"
            default: "SL_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
          description: "This is the generalization of L\\_C\\_Parallel."
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
            kimp: "P_C_ProductSmash(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SL_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SLMap"
            x-latex-property:
              symbol: "m"
              description: |
                The SLMap to be transformed.
          type:
            type: "string"
            const: "SL_C_RefineDomain"
            enum:
            - "SL_C_RefineDomain"
            default: "SL_C_RefineDomain"
        type: "object"
        title: "Refines the domain of another SLMap"
        x-latex-schema:
          group: "23_comp_singe"
          description: |
            This is the generalization of L\_C\_RefineDomain.
        required:
        - "m"
        - "type"
    SL_C_Series:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SLMap"
            description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL_C_Series"
            enum:
            - "SL_C_Series"
            default: "SL_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
          description: "This is the generalization of L\\_C\\_Series."
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
            kimp: "P_C_ProductSmash(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
            proj1: "SL1_C_Series(@(ms))"
    SL_C_Trace:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SLMap"
            x-latex-property:
              symbol: "m"
              description: |
                The SLMap to be transformed.
          m_proj:
            $ref: "#/components/schemas/SL1Map"
            x-latex-property:
              symbol: "m'"
              description: |
                The SL1Map projection of the SLMap $m$.
          type:
            type: "string"
            const: "SL_C_Trace"
            enum:
            - "SL_C_Trace"
            default: "SL_C_Trace"
        type: "object"
        title: "Trace of a SLMap."
        x-latex-schema:
          group: "23_comp_singe"
          description: |
            This is the generalization of L\_C\_Trace.
            For computational convenience, we also require to have $m'$, the SL1Map projection of the SLMap $m$.
        required:
        - "m"
        - "m_proj"
        - "type"
    SL_C_Union:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SLMap"
            description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SL_C_Union"
            enum:
            - "SL_C_Union"
            default: "SL_C_Union"
        type: "object"
        title: "Composition of SLMaps using the union of the results."
        x-latex-schema:
          group: "20_compositions"
          description: "This is the generalization of L\\_C\\_Union."
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom:
            kcod:
            kimp: "P_C_SumSmash(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SL_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SLMap"
            x-latex-property:
              symbol: "m"
              description: |
                The SLMap to be transformed.
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the domain of the SLMap.
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the codomain of the SLMap.
          kimp_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the implementations of the SLMap.
          type:
            type: "string"
            const: "SL_C_WrapUnits"
            enum:
            - "SL_C_WrapUnits"
            default: "SL_C_WrapUnits"
        type: "object"
        title: "Decorates with units another SLMap."
        x-latex-schema:
          group: "23_comp_singe"
          description: "This is the generalization of L\\_C\\_WrapUnits."
        required:
        - "kcod_units"
        - "kdom_units"
        - "kimp_units"
        - "m"
        - "type"
    SL_Identity:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          type:
            type: "string"
            const: "SL_Identity"
            enum:
            - "SL_Identity"
            default: "SL_Identity"
        type: "object"
        title: "Identity"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SL_L_Exact:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/LMap"
            description: |
              The LMap to be lifted to a SLMap.
          type:
            type: "string"
            const: "SL_L_Exact"
            enum:
            - "SL_L_Exact"
            default: "SL_L_Exact"
        type: "object"
        title: "Lifts a LMap to a SLMap."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
    SL_L_Explicit_Approx:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          optimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              The optimistic approximations of the SLMap.
          optimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the optimistic approximations.
          pessimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/LMap"
            description: |
              The pessimistic approximations of the SLMap.
          pessimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the pessimistic approximations.
          type:
            type: "string"
            const: "SL_L_Explicit_Approx"
            enum:
            - "SL_L_Explicit_Approx"
            default: "SL_L_Explicit_Approx"
        type: "object"
        title: "Construct a SLMap from explicit optimistic and pessimistic approximations."
        x-latex-schema:
          group: "40_lift"
        required:
        - "optimistic"
        - "pessimistic"
        - "type"
    SL_L_Lift1_Constant:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            description: |
              The SL1Map to be lifted to a SLMap.
          value:
            description: |
              The constant value to be used for the implementations
          type:
            type: "string"
            const: "SL_L_Lift1_Constant"
            enum:
            - "SL_L_Lift1_Constant"
            default: "SL_L_Lift1_Constant"
        type: "object"
        title: "Lifts a SL1Map to SLMap with a constant implementation."
        x-latex-schema:
          group: "40_lift"
          description: |
            This is the generalization of L\_L\_Lift1\_Constant.

            This is the particular case of SL\_L\_Lift1\_Transform where the transform map is constant.
        required:
        - "m"
        - "type"
        - "value"
    SL_L_Lift1_Transform:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          m:
            $ref: "#/components/schemas/SL1Map"
            description: |
              The SL1Map to be lifted to a SLMap.
            x-latex-property:
              symbol: "m"
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "f"
            description: |
              The monotone map that transforms the implementations of the SLMap.
          type:
            type: "string"
            const: "SL_L_Lift1_Transform"
            enum:
            - "SL_L_Lift1_Transform"
            default: "SL_L_Lift1_Transform"
        type: "object"
        title: "Lifts a SL1Map to SLMap by generating the implementations."
        x-latex-schema:
          group: "40_lift"
          description: "This is the generalization of L\\_L\\_Lift1\\_Transform."
        required:
        - "m"
        - "transform"
        - "type"
    SL_Unknown:
      allOf:
      - $ref: "#/components/schemas/SLMap"
      - properties:
          type:
            type: "string"
            const: "SL_Unknown"
            enum:
            - "SL_Unknown"
            default: "SL_Unknown"
        type: "object"
        title: "Placeholder for unknown SLMap"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SU1Check:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Check_Data"
          type:
            type: "string"
            const: "SU1Check"
            enum:
            - "SU1Check"
            default: "SU1Check"
        type: "object"
        title: "Check for a SU1Map."
        required:
        - "data"
        - "m"
        - "type"
    SU1Check_Data:
      type: "object"
      title: "An input-output pair for the SU1Map"
      properties:
        x: {}
        pess: {}
        opt: {}
        pess_y:
          $ref: "#/components/schemas/UpperSet"
        opt_y:
          $ref: "#/components/schemas/UpperSet"
        pess_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
        opt_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "pess"
      - "opt"
      - "pess_y"
      - "opt_y"
    SU1Map:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Scalable map to upper sets of resources."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukdom"
              description: "The Kleisli domain of the map."
              color: "requirements"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukcod"
              description: "The Kleisli codomain of the map."
              color: "functionalities"
          pes:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\pes"
              description: "The resolution poset for pessimistic estimate."
          opt:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\opt"
              description: "The resolution poset for optimistic estimate."
          type:
            type: "string"
            enum:
            - "SU1_C_CodSum"
            - "SU1_C_CodSumSmash"
            - "SU1_C_ExplicitApprox"
            - "SU1_C_Intersection"
            - "SU1_C_Parallel"
            - "SU1_C_ProdIntersection"
            - "SU1_C_Product"
            - "SU1_C_RefineDomain"
            - "SU1_C_Series"
            - "SU1_C_Trace"
            - "SU1_C_Union"
            - "SU1_C_WrapUnits"
            - "SU1_Exact"
            - "SU1_Identity"
            - "SU1_InvMultiply"
            - "SU1_InvSum"
            - "SU1_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "SU1Map"
            enum:
            - "SU1Map"
            default: "SU1Map"
        required:
        - "kdom"
        - "kcod"
        - "pes"
        - "opt"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 80
          color: "color-upper"
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            SU1_C_CodSum: "#/components/schemas/SU1_C_CodSum"
            SU1_C_CodSumSmash: "#/components/schemas/SU1_C_CodSumSmash"
            SU1_C_ExplicitApprox: "#/components/schemas/SU1_C_ExplicitApprox"
            SU1_C_Intersection: "#/components/schemas/SU1_C_Intersection"
            SU1_C_Parallel: "#/components/schemas/SU1_C_Parallel"
            SU1_C_ProdIntersection: "#/components/schemas/SU1_C_ProdIntersection"
            SU1_C_Product: "#/components/schemas/SU1_C_Product"
            SU1_C_RefineDomain: "#/components/schemas/SU1_C_RefineDomain"
            SU1_C_Series: "#/components/schemas/SU1_C_Series"
            SU1_C_Trace: "#/components/schemas/SU1_C_Trace"
            SU1_C_Union: "#/components/schemas/SU1_C_Union"
            SU1_C_WrapUnits: "#/components/schemas/SU1_C_WrapUnits"
            SU1_Exact: "#/components/schemas/SU1_Exact"
            SU1_Identity: "#/components/schemas/SU1_Identity"
            SU1_InvMultiply: "#/components/schemas/SU1_InvMultiply"
            SU1_InvSum: "#/components/schemas/SU1_InvSum"
            SU1_Unknown: "#/components/schemas/SU1_Unknown"
    SU1_C_CodSum:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_CodSum"
            enum:
            - "SU1_C_CodSum"
            default: "SU1_C_CodSum"
        type: "object"
        title: "Sum of maps"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U1\_C\_CodSum.
        required:
        - "ms"
        - "type"
    SU1_C_CodSumSmash:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_CodSumSmash"
            enum:
            - "SU1_C_CodSumSmash"
            default: "SU1_C_CodSumSmash"
        type: "object"
        title: "Smash sum"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U1\_C\_CodSumSmash.
        required:
        - "ms"
        - "type"
    SU1_C_ExplicitApprox:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          optimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            description: |
              The optimistic approximations of the map
          optimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the optimistic approximations.
          pessimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            description: |
              The pessimistic approximations of the map
          pessimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the pessimistic approximations.
          type:
            type: "string"
            const: "SU1_C_ExplicitApprox"
            enum:
            - "SU1_C_ExplicitApprox"
            default: "SU1_C_ExplicitApprox"
        type: "object"
        title: "Constructs a SU1Map from explicit approximations of U1Map maps."
        x-latex-schema:
          group: "70_other"
        required:
        - "optimistic"
        - "pessimistic"
        - "type"
    SU1_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_Intersection"
            enum:
            - "SU1_C_Intersection"
            default: "SU1_C_Intersection"
        type: "object"
        title: "Intersection of SU1 maps"
        x-latex-schema:
          group: "20_compositions_20_union_int"
          description: |
            This is the generalization of U1\_C\_Intersection.
        required:
        - "ms"
        - "type"
    SU1_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_Parallel"
            enum:
            - "SU1_C_Parallel"
            default: "SU1_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "10_compositions_10_simple"
          description: |
            This is the generalization of U1\_C\_Parallel.
        required:
        - "ms"
        - "type"
        x-mcdp-sul1:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
            opt: "P_C_ProductSmash(@(ms))"
            pes: "P_C_ProductSmash(@(ms))"
    SU1_C_ProdIntersection:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_ProdIntersection"
            enum:
            - "SU1_C_ProdIntersection"
            default: "SU1_C_ProdIntersection"
        type: "object"
        title: "Product of domains, intersection of codomains"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U1\_C\_ProdIntersection.
        required:
        - "ms"
        - "type"
    SU1_C_Product:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_Product"
            enum:
            - "SU1_C_Product"
            default: "SU1_C_Product"
        type: "object"
        title: "Product of SU1 maps"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U1\_C\_Product.
        required:
        - "ms"
        - "type"
    SU1_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor"
              description: "The map to be transformed"
          type:
            type: "string"
            const: "SU1_C_RefineDomain"
            enum:
            - "SU1_C_RefineDomain"
            default: "SU1_C_RefineDomain"
        type: "object"
        title: "Refinement of the domain"
        description: |
          Refinement of the domain for the wrapped map.
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of U1\\_C\\_RefineDomain."
        required:
        - "m"
        - "type"
    SU1_C_Series:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_Series"
            enum:
            - "SU1_C_Series"
            default: "SU1_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "10_compositions_10_simple"
          description: "This is the generalization of U1\\_C\\_Series."
        required:
        - "ms"
        - "type"
    SU1_C_Trace:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor"
              description: "The map to be transformed"
          type:
            type: "string"
            const: "SU1_C_Trace"
            enum:
            - "SU1_C_Trace"
            default: "SU1_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of U1\\_C\\_Trace. "
        required:
        - "m"
        - "type"
    SU1_C_Union:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor_\\idx"
              description: "Maps to be composed."
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps.
          type:
            type: "string"
            const: "SU1_C_Union"
            enum:
            - "SU1_C_Union"
            default: "SU1_C_Union"
        type: "object"
        title: "Union of SU1 maps"
        x-latex-schema:
          group: "20_compositions_20_union_int"
          description: |
            This is the generalization of U1\_C\_Union.
        required:
        - "ms"
        - "type"
    SU1_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "\\umor"
              description: "The map to be transformed"
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the domain
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the codomain
          type:
            type: "string"
            const: "SU1_C_WrapUnits"
            enum:
            - "SU1_C_WrapUnits"
            default: "SU1_C_WrapUnits"
        type: "object"
        title: "Wraps a map with units."
        x-latex-schema:
          group: "30_compositions_single"
          description: "This is the generalization of U1\\_C\\_WrapUnits."
        required:
        - "kcod_units"
        - "kdom_units"
        - "m"
        - "type"
    SU1_Exact:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "\\umor"
            description: |
              The U1Map to be lifted to a SU1Map.
          type:
            type: "string"
            const: "SU1_Exact"
            enum:
            - "SU1_Exact"
            default: "SU1_Exact"
        type: "object"
        title: "Lifts a U1Map to a SU1Map."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
    SU1_Identity:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          type:
            type: "string"
            const: "SU1_Identity"
            enum:
            - "SU1_Identity"
            default: "SU1_Identity"
        type: "object"
        title: "Identity"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SU1_InvMultiply:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "The poset where the operation is defined."
          type:
            type: "string"
            const: "SU1_InvMultiply"
            enum:
            - "SU1_InvMultiply"
            default: "SU1_InvMultiply"
        type: "object"
        title: "The upper inverse of multiplication."
        x-latex-schema:
          group: "60_math"
        required:
        - "opspace"
        - "type"
    SU1_InvSum:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          opspace:
            $ref: "#/components/schemas/Poset"
            description: "The poset where the operation is defined."
          type:
            type: "string"
            const: "SU1_InvSum"
            enum:
            - "SU1_InvSum"
            default: "SU1_InvSum"
        type: "object"
        title: "The inverse of addition."
        x-latex-schema:
          group: "60_math"
        required:
        - "opspace"
        - "type"
    SU1_Unknown:
      allOf:
      - $ref: "#/components/schemas/SU1Map"
      - properties:
          type:
            type: "string"
            const: "SU1_Unknown"
            enum:
            - "SU1_Unknown"
            default: "SU1_Unknown"
        type: "object"
        title: "Placeholder for an unknown SU1Map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SUCheck:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/SUCheck_Data"
          type:
            type: "string"
            const: "SUCheck"
            enum:
            - "SUCheck"
            default: "SUCheck"
        type: "object"
        title: "Check for a SUMap."
        required:
        - "data"
        - "m"
        - "type"
    SUCheck_Data:
      type: "object"
      title: "An input-output pair for the SUMap"
      properties:
        x: {}
        opt: {}
        opt_y:
          $ref: "#/components/schemas/UpperSet"
        pess: {}
        pess_y:
          $ref: "#/components/schemas/UpperSet"
        pess_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
        opt_elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "pess"
      - "opt"
      - "pess_y"
      - "opt_y"
    SUMap:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Scalable map to upper sets of resources and implementations."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            description: "Kleisli domain of the map."
            x-latex-property:
              symbol: "\\ukdom"
              description: "The Kleisli domain of the map."
              color: "functionalities"
          kcod:
            $ref: "#/components/schemas/Poset"
            description: "Kleisli co-domain of the map."
            x-latex-property:
              symbol: "\\ukcod"
              description: "The Kleisli codomain of the map."
              color: "requirements"
          kimp:
            $ref: "#/components/schemas/Poset"
            description: "Poset of implementations."
            x-latex-property:
              symbol: "\\ukimp"
              description: "The implementation poset of the map."
              color: "implementations"
          pes:
            $ref: "#/components/schemas/Poset"
            description: "Poset of resolutions (pessimistic)"
            x-latex-property:
              symbol: "\\pes"
              description: "The resolution poset for pessimistic estimate."
          opt:
            $ref: "#/components/schemas/Poset"
            description: "Poset of resolutions (optimistic)"
            x-latex-property:
              symbol: "\\opt"
              description: "The resolution poset for optimistic estimate."
          type:
            type: "string"
            enum:
            - "SU_C_ITransform"
            - "SU_C_Intersection"
            - "SU_C_Parallel"
            - "SU_C_RefineDomain"
            - "SU_C_Series"
            - "SU_C_Trace"
            - "SU_C_Union"
            - "SU_C_WrapUnits"
            - "SU_Identity"
            - "SU_L_Exact"
            - "SU_L_Explicit_Approx"
            - "SU_L_Lift1_Constant"
            - "SU_L_Lift1_Transform"
            - "SU_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "SUMap"
            enum:
            - "SUMap"
            default: "SUMap"
        required:
        - "kdom"
        - "kcod"
        - "kimp"
        - "pes"
        - "opt"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 100
          color: "color-upper"
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            SU_C_ITransform: "#/components/schemas/SU_C_ITransform"
            SU_C_Intersection: "#/components/schemas/SU_C_Intersection"
            SU_C_Parallel: "#/components/schemas/SU_C_Parallel"
            SU_C_RefineDomain: "#/components/schemas/SU_C_RefineDomain"
            SU_C_Series: "#/components/schemas/SU_C_Series"
            SU_C_Trace: "#/components/schemas/SU_C_Trace"
            SU_C_Union: "#/components/schemas/SU_C_Union"
            SU_C_WrapUnits: "#/components/schemas/SU_C_WrapUnits"
            SU_Identity: "#/components/schemas/SU_Identity"
            SU_L_Exact: "#/components/schemas/SU_L_Exact"
            SU_L_Explicit_Approx: "#/components/schemas/SU_L_Explicit_Approx"
            SU_L_Lift1_Constant: "#/components/schemas/SU_L_Lift1_Constant"
            SU_L_Lift1_Transform: "#/components/schemas/SU_L_Lift1_Transform"
            SU_Unknown: "#/components/schemas/SU_Unknown"
    SU_C_ITransform:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m"
              description: "Map to be transformed"
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "f"
              description: |
                The monotone map that transforms the implementations of the SUMap.
          type:
            type: "string"
            const: "SU_C_ITransform"
            enum:
            - "SU_C_ITransform"
            default: "SU_C_ITransform"
        type: "object"
        title: "Transforms the implementations of a SUMap."
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the generalization of U\_C\_ITransform.
        required:
        - "m"
        - "transform"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          - "transform"
          functors:
            kdom: "@(m)"
            kcod: "@(m)"
            kimp: "cod(transform)"
            opt: "@(m)"
            pes: "@(m)"
    SU_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m_i"
              description: "Maps to be composed"
          labels:
            type: "array"
            items:
              type: "string"
            description: "Labels for the maps"
          type:
            type: "string"
            const: "SU_C_Intersection"
            enum:
            - "SU_C_Intersection"
            default: "SU_C_Intersection"
        type: "object"
        title: "Intersection of the results of a set of maps."
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U\_C\_Intersection.
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom:
            kcod:
            kimp: "P_C_ProductSmash(@(*))"
            opt: "P_C_ProductSmash(@(*))"
            pes: "P_C_ProductSmash(@(*))"
    SU_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m_i"
              description: "Maps to be composed"
          labels:
            type: "array"
            items:
              type: "string"
            description: "Labels for the maps"
          type:
            type: "string"
            const: "SU_C_Parallel"
            enum:
            - "SU_C_Parallel"
            default: "SU_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U\_C\_Parallel.
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(*))"
            kcod: "P_C_Product(@(*))"
            kimp: "P_C_ProductSmash(@(*))"
            opt: "P_C_ProductSmash(@(*))"
            pes: "P_C_ProductSmash(@(*))"
    SU_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m"
              description: "Map to be transformed"
          type:
            type: "string"
            const: "SU_C_RefineDomain"
            enum:
            - "SU_C_RefineDomain"
            default: "SU_C_RefineDomain"
        type: "object"
        title: "Refines the domain of another SUMap"
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the generalization of U\_C\_RefineDomain.
            The map is refined by restricting the domain to a subset of the original domain.
        required:
        - "m"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          functors:
            kdom: "-"
            kcod: "@(m)"
            kimp: "@(m)"
            opt: "@(m)"
            pes: "@(m)"
    SU_C_Series:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m_i"
              description: "Maps to be composed"
          labels:
            type: "array"
            items:
              type: "string"
            description: "Labels for the maps"
          type:
            type: "string"
            const: "SU_C_Series"
            enum:
            - "SU_C_Series"
            default: "SU_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U\_C\_Series.
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom: |
              @(ms[0])
            kcod: |
              @(ms[-1])
            kimp: "P_C_ProductSmash(@(*))"
            opt: "P_C_ProductSmash(@(*))"
            pes: "P_C_ProductSmash(@(*))"
            proj1: "SU1_C_Series(@(ms))"
    SU_C_Trace:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m"
              description: "Map to be transformed"
          m_proj:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "m'"
              description: |
                The SU1Map projection of the SUMap $m$.
          type:
            type: "string"
            const: "SU_C_Trace"
            enum:
            - "SU_C_Trace"
            default: "SU_C_Trace"
        type: "object"
        title: "Trace of a SUMap."
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the generalization of U\_C\_Trace.

            For computational convenience, we also require to have $m'$, the SU1Map projection of the SUMap $m$.
        required:
        - "m"
        - "m_proj"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          functors:
            kdom: "@(m)[0]"
            kcod: "@(m)[0]"
            kimp: "P_C_ProductSmash([kdom(m)[1], @(m)])"
            opt: "@(m)"
            pes: "@(m)"
    SU_C_Union:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m_i"
              description: "Maps to be composed"
          labels:
            type: "array"
            items:
              type: "string"
            description: "Labels for the maps"
          type:
            type: "string"
            const: "SU_C_Union"
            enum:
            - "SU_C_Union"
            default: "SU_C_Union"
        type: "object"
        title: "Composition of SUMaps using the union of the results."
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the generalization of U\_C\_Union.
        required:
        - "ms"
        - "type"
        x-mcdp-sul:
          params:
          - "ms"
          functors:
            kdom:
            kcod:
            kimp: "P_C_SumSmash(@(*))"
            opt: "P_C_ProductSmash(@(*))"
            pes: "P_C_ProductSmash(@(*))"
    SU_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SUMap"
            x-latex-property:
              symbol: "m"
              description: "Map to be transformed"
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the domain of the SUMap.
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the codomain of the SUMap.
          kimp_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the implementations of the SUMap.
          type:
            type: "string"
            const: "SU_C_WrapUnits"
            enum:
            - "SU_C_WrapUnits"
            default: "SU_C_WrapUnits"
        type: "object"
        title: "Decorates with units another SUMap."
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the generalization of U\_C\_WrapUnits.
        required:
        - "kcod_units"
        - "kdom_units"
        - "kimp_units"
        - "m"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          functors:
            kdom: "P_C_Units(@(m), kdom_units)"
            kcod: "P_C_Units(@(m), kcod_units)"
            kimp: "P_C_Units(@(m), kimp_units)"
            opt: "@(m)"
            pes: "@(m)"
    SU_Identity:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          type:
            type: "string"
            const: "SU_Identity"
            enum:
            - "SU_Identity"
            default: "SU_Identity"
        type: "object"
        title: "Identity"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    SU_L_Exact:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            description: |
              The UMap to be lifted to a SUMap.
          type:
            type: "string"
            const: "SU_L_Exact"
            enum:
            - "SU_L_Exact"
            default: "SU_L_Exact"
        type: "object"
        title: "Lifts a UMap to a SUMap."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          functors:
            kdom: "@(*)"
            kcod: "@(*)"
            kimp: "@(*)"
            opt: "P_C_ProductSmash([])"
            pes: "P_C_ProductSmash([])"
    SU_L_Explicit_Approx:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          optimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              The optimistic approximations of the SUMap.
          optimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the optimistic approximations.
          pessimistic:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              The pessimistic approximations of the SUMap.
          pessimistic_labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the pessimistic approximations.
          type:
            type: "string"
            const: "SU_L_Explicit_Approx"
            enum:
            - "SU_L_Explicit_Approx"
            default: "SU_L_Explicit_Approx"
        type: "object"
        title: "Construct a SUMap from explicit optimistic and pessimistic approximations."
        x-latex-schema:
          group: "40_lift"
        required:
        - "optimistic"
        - "pessimistic"
        - "type"
        x-mcdp-sul:
          params:
          - "optimistic"
          - "pessimistic"
          functors:
            kimp: "P_C_SumSmash([P_Integer, P_Integer])"
            opt: "P_Integer"
            pes: "P_Integer"
    SU_L_Lift1_Constant:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            x-latex-property:
              symbol: "m"
            description: |
              The SU1Map to be lifted to a SUMap.
          value:
            description: |
              The constant value to be used for the implementations
          type:
            type: "string"
            const: "SU_L_Lift1_Constant"
            enum:
            - "SU_L_Lift1_Constant"
            default: "SU_L_Lift1_Constant"
        type: "object"
        title: "Lifts a SU1Map to SUMap with a constant implementation."
        x-latex-schema:
          group: "40_lift"
          description: |
            This is the generalization of U\_L\_Lift1\_Constant.

            This is the particular case of SU\_L\_Lift1\_Transform where the transform map is constant.
        required:
        - "m"
        - "type"
        - "value"
        x-mcdp-sul:
          params:
          - "m"
          functors:
            kdom: "@(m)"
            kcod: "@(m)"
            opt: "@(m)"
            pes: "@(m)"
    SU_L_Lift1_Transform:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          m:
            $ref: "#/components/schemas/SU1Map"
            description: |
              The SU1Map to be lifted to a SUMap.
            x-latex-property:
              symbol: "m"
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "f"
            description: |
              The monotone map that transforms the implementations of the SUMap.
          type:
            type: "string"
            const: "SU_L_Lift1_Transform"
            enum:
            - "SU_L_Lift1_Transform"
            default: "SU_L_Lift1_Transform"
        type: "object"
        title: "Lifts a SU1Map to SUMap by generating the implementations."
        x-latex-schema:
          group: "40_lift"
          description: |
            This is the generalization of U\_L\_Lift1\_Transform.
        required:
        - "m"
        - "transform"
        - "type"
        x-mcdp-sul:
          params:
          - "m"
          - "transform"
          functors:
            kdom: "@(m)"
            kcod: "@(m)"
            kimp: "cod(transform)"
            opt: "@(m)"
            pes: "@(m)"
    SU_Unknown:
      allOf:
      - $ref: "#/components/schemas/SUMap"
      - properties:
          type:
            type: "string"
            const: "SU_Unknown"
            enum:
            - "SU_Unknown"
            default: "SU_Unknown"
        type: "object"
        title: "Placeholder for unknown SUMap"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    U1Check:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/U1Check_Data"
          type:
            type: "string"
            const: "U1Check"
            enum:
            - "U1Check"
            default: "U1Check"
        type: "object"
        title: "Check for a U1Map."
        required:
        - "data"
        - "m"
        - "type"
    U1Check_Data:
      type: "object"
      title: "An input-output pair for the U1Map"
      properties:
        x: {}
        y:
          $ref: "#/components/schemas/UpperSet"
        elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "y"
    U1Map:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Map to upper sets of resources."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukdom"
              description: |
                Kleisli domain of the map
              color: "functionalities"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukcod"
              description: |
                Kleisli co-domain of the map
              color: "requirements"
          type:
            type: "string"
            enum:
            - "U1_C_CodSum"
            - "U1_C_CodSumSmash"
            - "U1_C_DomUnion"
            - "U1_C_Intersection"
            - "U1_C_Parallel"
            - "U1_C_ProdIntersection"
            - "U1_C_Product"
            - "U1_C_RefineDomain"
            - "U1_C_Series"
            - "U1_C_Trace"
            - "U1_C_Union"
            - "U1_C_WrapUnits"
            - "U1_Catalog"
            - "U1_Constant"
            - "U1_Entire"
            - "U1_Explicit"
            - "U1_FromFilter"
            - "U1_Identity"
            - "U1_IntersectionOfPrinUpperSets"
            - "U1_InvMul_Opt"
            - "U1_InvMul_Pes"
            - "U1_InvSum_Opt"
            - "U1_InvSum_Pes"
            - "U1_L_Uinv"
            - "U1_Lift"
            - "U1_RepresentPrincipalUpperSet"
            - "U1_Uinv_Join"
            - "U1_Uinv_JoinConstant"
            - "U1_UnionOfPrinUpperSets"
            - "U1_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "U1Map"
            enum:
            - "U1Map"
            default: "U1Map"
        required:
        - "kdom"
        - "kcod"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 40
          color: "color-upper"
          preamble: |
            \newcommand{\signUOMap}[3]{#1 \stackrel{#3}{\to}_{\text{\textbf{U1Map}}} #2 }
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            U1_C_CodSum: "#/components/schemas/U1_C_CodSum"
            U1_C_CodSumSmash: "#/components/schemas/U1_C_CodSumSmash"
            U1_C_DomUnion: "#/components/schemas/U1_C_DomUnion"
            U1_C_Intersection: "#/components/schemas/U1_C_Intersection"
            U1_C_Parallel: "#/components/schemas/U1_C_Parallel"
            U1_C_ProdIntersection: "#/components/schemas/U1_C_ProdIntersection"
            U1_C_Product: "#/components/schemas/U1_C_Product"
            U1_C_RefineDomain: "#/components/schemas/U1_C_RefineDomain"
            U1_C_Series: "#/components/schemas/U1_C_Series"
            U1_C_Trace: "#/components/schemas/U1_C_Trace"
            U1_C_Union: "#/components/schemas/U1_C_Union"
            U1_C_WrapUnits: "#/components/schemas/U1_C_WrapUnits"
            U1_Catalog: "#/components/schemas/U1_Catalog"
            U1_Constant: "#/components/schemas/U1_Constant"
            U1_Entire: "#/components/schemas/U1_Entire"
            U1_Explicit: "#/components/schemas/U1_Explicit"
            U1_FromFilter: "#/components/schemas/U1_FromFilter"
            U1_Identity: "#/components/schemas/U1_Identity"
            U1_IntersectionOfPrinUpperSets: "#/components/schemas/U1_IntersectionOfPrinUpperSets"
            U1_InvMul_Opt: "#/components/schemas/U1_InvMul_Opt"
            U1_InvMul_Pes: "#/components/schemas/U1_InvMul_Pes"
            U1_InvSum_Opt: "#/components/schemas/U1_InvSum_Opt"
            U1_InvSum_Pes: "#/components/schemas/U1_InvSum_Pes"
            U1_L_Uinv: "#/components/schemas/U1_L_Uinv"
            U1_Lift: "#/components/schemas/U1_Lift"
            U1_RepresentPrincipalUpperSet: "#/components/schemas/U1_RepresentPrincipalUpperSet"
            U1_Uinv_Join: "#/components/schemas/U1_Uinv_Join"
            U1_Uinv_JoinConstant: "#/components/schemas/U1_Uinv_JoinConstant"
            U1_UnionOfPrinUpperSets: "#/components/schemas/U1_UnionOfPrinUpperSets"
            U1_Unknown: "#/components/schemas/U1_Unknown"
    U1_C_CodSum:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_CodSum"
            enum:
            - "U1_C_CodSum"
            default: "U1_C_CodSum"
        type: "object"
        title: "Co-domain sum combination"
        x-latex-schema:
          group: "20_compositions"
          description: |
            Given two maps $f: A \to B$ and $g: A \to C$, the codomain sum of these maps is a map that combines the codomains of both maps:
            %
            \begin{equation}
                SELF(f, g) : A \to P\_C\_Sum(B, C)
            \end{equation}
            %
            It is defined as follows:
            \begin{equation}
                x \mapsto  \{ \text{in}_1 (b) \mid b \in f(x) \}  \cup \{ \text{in}_2 (c) \mid c \in g(x) \}
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_Sum(@(ms))"
    U1_C_CodSumSmash:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_CodSumSmash"
            enum:
            - "U1_C_CodSumSmash"
            default: "U1_C_CodSumSmash"
        type: "object"
        title: "Co-domain (smash) sum combination"
        x-latex-schema:
          group: "20_compositions"
          description: |
            Given two maps $f: A \to B$ and $g: A \to C$, the codomain sum of these maps is a map that combines the codomains of both maps:
            %
            \begin{equation}
                SELF(f, g) : A \to P\_C\_SumSmash(B, C)
            \end{equation}

            It is defined as follows:
            %
            \begin{equation}
                x \mapsto  \{ \text{in}_1 (b) \mid b \in f(x) \}  \cup \{ \text{in}_2 (c) \mid c \in g(x) \}
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_SumSmash(@(ms))"
    U1_C_DomUnion:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_DomUnion"
            enum:
            - "U1_C_DomUnion"
            default: "U1_C_DomUnion"
        type: "object"
        title: "Domain union"
        x-latex-schema:
          group: "20_compositions"
          description: |
            This is the equivalent of M\_C\_DomUnion.

            Given two maps $f: A \to B$ and $g: C \to B$, the domain union of these maps is a map that combines the domains of both maps:
            %
            \begin{equation}
                SELF(f, g) : P\_F\_C\_Union( A, C) \to B
            \end{equation}

            The value is defined as follows:
            %
            \begin{equation}
                SELF(f, g) : x \mapsto
                \begin{cases}
                    f(x) & \text{if } x \in A \\
                    g(x) & \text{if } x \in C
                \end{cases}
            \end{equation}

            Note that the order of the maps does matter.
            We use the first map whose domain contains the input.
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Union(@(ms))"
            kcod: "P_F_C_Union(@(ms))"
    U1_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_Intersection"
            enum:
            - "U1_C_Intersection"
            default: "U1_C_Intersection"
        type: "object"
        title: "Intersection"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X \to Y$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X  &\to Y \\
                x &\mapsto \bigcap_i m_i(x)
            \end{align*}
          order: 1000
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
    U1_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_Parallel"
            enum:
            - "U1_C_Parallel"
            default: "U1_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X_i \to Y_i$, the monoidal product is defined as:
            %
            \begin{align*}
                SELF( \{ m_i \} ) : P\_C\_Product(\{X_i\}) &\to P\_C\_Product(\{Y_i\}) 
            \end{align*}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
    U1_C_ProdIntersection:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_ProdIntersection"
            enum:
            - "U1_C_ProdIntersection"
            default: "U1_C_ProdIntersection"
        type: "object"
        title: "From product to intersection"
        x-latex-schema:
          group: "20_compositions"
          description: |
            The domain $\ukdom$ should be a product.
            %
            The map is defined as follows:
            %
            \begin{equation}
              \langle x_i \rangle_i \mapsto \bigcap_i f_i(x_i)
            \end{equation}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(*))"
            kcod: "P_F_C_Union(@(*))"
    U1_C_Product:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_Product"
            enum:
            - "U1_C_Product"
            default: "U1_C_Product"
        type: "object"
        title: "Product"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X \to Y_i$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X &\to P\_C\_Product(\{Y_i\})
            \end{align*}
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_C_Product(@(ms))"
    U1_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m"
              description: "Original map"
          type:
            type: "string"
            const: "U1_C_RefineDomain"
            enum:
            - "U1_C_RefineDomain"
            default: "U1_C_RefineDomain"
        type: "object"
        title: "Refines the domain of a monotone map."
        x-latex-schema:
          group: "30_comp_single"
          description: ""
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "given"
            kcod: "@(*)"
    U1_C_Series:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_Series"
            enum:
            - "U1_C_Series"
            default: "U1_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
          description: ""
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
    U1_C_Trace:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m"
              description: "Original map"
          type:
            type: "string"
            const: "U1_C_Trace"
            enum:
            - "U1_C_Trace"
            default: "U1_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the trace of the map. 
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "@(*)[0]"
            kcod: "@(*)[0]"
    U1_C_Union:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m_i"
              description: |
                Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              A list of labels for the maps
          type:
            type: "string"
            const: "U1_C_Union"
            enum:
            - "U1_C_Union"
            default: "U1_C_Union"
        type: "object"
        title: "Union"
        x-latex-schema:
          group: "20_compositions"
          description: |
            For a family of maps $m_i : X \to Y$, we define
            %
            \begin{align*}
                SELF( \{ m_i \} ) : X  &\to Y \\
                x &\mapsto \bigcup_i m_i(x)
            \end{align*}
          order: 1000
        required:
        - "ms"
        - "type"
        x-mcdp-ul1:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(*))"
            kcod: "P_F_C_Union(@(*))"
    U1_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m"
              description: "Original map"
          kdom_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the domain
          kcod_units:
            $ref: "#/components/schemas/Unit"
            description: |
              Units for the codomain
          type:
            type: "string"
            const: "U1_C_WrapUnits"
            enum:
            - "U1_C_WrapUnits"
            default: "U1_C_WrapUnits"
        type: "object"
        title: |
          Decorates a map with units.
        x-latex-schema:
          group: "30_comp_single"
          description: ""
        required:
        - "kcod_units"
        - "kdom_units"
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          - "kdom_units"
          - "kcod_units"
          functors:
            kdom: "P_C_Units(@(m), kdom_units)"
            kcod: "P_C_Units(@(m), kcod_units)"
    U1_Catalog:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          options:
            type: "array"
            items:
              $ref: "#/components/schemas/U1_Catalog_Options"
            x-latex-property:
              symbol: |
                \langle \fun_\idx, \rescj_\idx \rangle
              description: |
                The options in the catalog
          type:
            type: "string"
            const: "U1_Catalog"
            enum:
            - "U1_Catalog"
            default: "U1_Catalog"
        type: "object"
        title: "Map induced by a catalog of options."
        x-latex-schema:
          group: "10_simple"
          description: |
            This map is defined by a catalog of options $\makelist{ \tup{\fun_\idx,\rescj_\idx} }$:
            %
            \begin{equation}
                \funcj \mapsto  \bigcup_\idx \ \{ \upit r_\idx \mid   f_\idx \leq x \}
            \end{equation}
          order: 100
        required:
        - "options"
        - "type"
    U1_Catalog_Options:
      type: "object"
      properties:
        f:
          x-latex-property:
            symbol: "\\fun"
            description: |
              A point in the domain.
        r:
          x-latex-property:
            symbol: "\\rescj"
            description: |
              A point in the codomain
      required:
      - "f"
      - "r"
      x-latex-schema:
        title: "An option in the catalog"
        description: |
          An option in the catalog, where $f$ is a point in the domain and $r$ is the corresponding upper set in the codomain.
    U1_Constant:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          value:
            $ref: "#/components/schemas/UpperSet"
            x-latex-property:
              symbol: "U"
              description: |
                The constant value of the map, which is an upper set $U \subseteq \ukcod$.
          type:
            type: "string"
            const: "U1_Constant"
            enum:
            - "U1_Constant"
            default: "U1_Constant"
        type: "object"
        title: "Constant map"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a constant map, which maps every element of the domain to the same value $U$.
        required:
        - "type"
        - "value"
    U1_Entire:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_Entire"
            enum:
            - "U1_Entire"
            default: "U1_Entire"
        type: "object"
        title: "Returns the entire poset"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is the map defined as 
            %
            \begin{equation*}
                x \mapsto \ukcod
            \end{equation*}

            which maps every element of the domain to the entire codomain poset $\ukcod$.

            This is useful when the codomain doesn't have a compact representation in terms of antichains.
        required:
        - "type"
    U1_Explicit:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          options:
            type: "array"
            items:
              $ref: "#/components/schemas/U1_Explicit_Option"
            x-latex-property:
              description: |
                Option definitions
          type:
            type: "string"
            const: "U1_Explicit"
            enum:
            - "U1_Explicit"
            default: "U1_Explicit"
        type: "object"
        title: "Map defined pointwise"
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a map defined pointwise, where each option specifies a point in the domain and its corresponding value in the codomain.
        required:
        - "options"
        - "type"
    U1_Explicit_Option:
      type: "object"
      properties:
        x:
          x-latex-property:
            symbol: "x"
            description: |
              A point in the domain of the map.
        y:
          $ref: "#/components/schemas/UpperSet"
          x-latex-property:
            symbol: "y"
            description: |
              The upper set corresponding to the point  $x$ in the domain.
      required:
      - "x"
      - "y"
      x-latex-property:
        title: "An option in the map"
        description: |
          An option in the map, where $x$ is a point in the domain and $y$ is the corresponding upper set.
    U1_FromFilter:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map $m : \ukdom \to \Bool$
          type:
            type: "string"
            const: "U1_FromFilter"
            enum:
            - "U1_FromFilter"
            default: "U1_FromFilter"
        type: "object"
        title: "Filters based on a monotone map."
        x-latex-schema:
          group: "65_lift"
          preamble: |
            \newcommand{\Bool}{\mathbf{Bool}}
          description: |
            Defines the map:
            \begin{equation}
                x \mapsto \begin{cases}
                              \{ x \} & \text{if } m(x)  \\
                              \emptyset & \text{otherwise}
                \end{cases}
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "P_C_Opposite(dom(*))"
            kcod: "P_C_Opposite(dom(*))"
    U1_Identity:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_Identity"
            enum:
            - "U1_Identity"
            default: "U1_Identity"
        type: "object"
        title: "Lift of the identity map"
        x-latex-schema:
          group: "10_simple"
          description: |
            The identity map:

            \begin{equation*}
                x \mapsto \upit x
            \end{equation*}
        required:
        - "type"
    U1_IntersectionOfPrinUpperSets:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_IntersectionOfPrinUpperSets"
            enum:
            - "U1_IntersectionOfPrinUpperSets"
            default: "U1_IntersectionOfPrinUpperSets"
        type: "object"
        title: "Intersection of principal upper sets."
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            The domain $\ukdom$ must be a product poset.

            This map is defined as follows:
            %
            \begin{equation}
                \langle x_i \rangle_i \mapsto \bigcap_i \upit x_i
            \end{equation}
        required:
        - "type"
    U1_InvMul_Opt:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "U1_InvMul_Opt"
            enum:
            - "U1_InvMul_Opt"
            default: "U1_InvMul_Opt"
        type: "object"
        title: "Finite-resolution optimistic approximation of the inverse of a multiplication map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a \cdot b \geq x \}
            \end{equation}

            The approximation is optimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    U1_InvMul_Pes:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "U1_InvMul_Pes"
            enum:
            - "U1_InvMul_Pes"
            default: "U1_InvMul_Pes"
        type: "object"
        title: "Finite-resolution pessimistic approximation of the inverse of a multiplication map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a \cdot b \geq x \}
            \end{equation}

            The approximation is pessimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    U1_InvSum_Opt:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "U1_InvSum_Opt"
            enum:
            - "U1_InvSum_Opt"
            default: "U1_InvSum_Opt"
        type: "object"
        title: "Finite-resolution optimistic approximation of the inverse of an addition map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a + b \geq x \}
            \end{equation}

            The approximation is optimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    U1_InvSum_Pes:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          n:
            type: "integer"
            description: |
              Resolution (number of points in the produced antichain)
          opspace:
            $ref: "#/components/schemas/Poset"
            description: |
              The poset in which the operation is performed.
          type:
            type: "string"
            const: "U1_InvSum_Pes"
            enum:
            - "U1_InvSum_Pes"
            default: "U1_InvSum_Pes"
        type: "object"
        title: "Finite-resolution pessimistic approximation of the inverse of an addition map."
        x-latex-schema:
          group: "60_math"
          description: |
            This map provides an approximation of the map
            %
            \begin{equation}
                x  \mapsto \{ \langle a, b \rangle \mid a + b \geq x \}
            \end{equation}

            The approximation is pessimistic.
        required:
        - "n"
        - "opspace"
        - "type"
    U1_L_Uinv:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map $m : \ukdom \to \ukcod$
          type:
            type: "string"
            const: "U1_L_Uinv"
            enum:
            - "U1_L_Uinv"
            default: "U1_L_Uinv"
        type: "object"
        title: "Computes the upper inverse of a monotone map."
        x-latex-schema:
          group: "65_lift"
          preamble: |
            \newcommand{\Uinv}{\mathrm{Uinv}}
            \newcommand{\posA}{\mathcal{A}}
            \newcommand{\posB}{\mathcal{B}}
          description: |
            Computes the upper inverse of the monotone map $m$.
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "cod(*)"
            kcod: "dom(*)"
    U1_Lift:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          m:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "m"
              description: |
                A monotone map
          type:
            type: "string"
            const: "U1_Lift"
            enum:
            - "U1_Lift"
            default: "U1_Lift"
        type: "object"
        title: "Lifts a monotone map"
        x-latex-schema:
          group: "65_lift"
          description: |
            Lifts a monotone map to a U1Map in the obvious way:
            %
            \begin{equation}
                SELF(m) : x \mapsto \upit m(x) 
            \end{equation}
        required:
        - "m"
        - "type"
        x-mcdp-ul1:
          params:
          - "m"
          functors:
            kdom: "dom(*)"
            kcod: "cod(*)"
    U1_RepresentPrincipalUpperSet:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_RepresentPrincipalUpperSet"
            enum:
            - "U1_RepresentPrincipalUpperSet"
            default: "U1_RepresentPrincipalUpperSet"
        type: "object"
        title: "Represent a principal upper set"
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            This map is defined when $\ukdom$ and $\ukcod$ are subsets of a common ambient poset $P$.

            It is defined as:
            % 
            \begin{equation}
                x \mapsto \max \{ S \in P\_C\_UpperSets(\ukcod) \mid S \ \subseteq \ \upit_P x \}
            \end{equation}

            The idea is that we want to ``represent'' the principal upper set $\upit_P x$ in terms of the codomain $\ukcod$.

            For example, consider the case 
            % 
            \begin{itemize}
                \item $\ukdom = 2\mathbb{Z}$, the even integers
                \item $\ukcod = 3\mathbb{Z}$, the multiples of 3  
            \end{itemize} 

            They are both subposets of the ambient poset $\mathbb{Z}$.
            The map would associate each even integer $x$ with the up closure of the smallest multiple of 3 greater than or equal to $x$:
            %
            \begin{align*}
                10 &\mapsto \upit 12 \\
                8 &\mapsto \upit 9 \\
                6 &\mapsto \upit 6 \\
                4 &\mapsto \upit 6 \\
                2 &\mapsto \upit 3 \\
                0 &\mapsto \upit 0 
            \end{align*}

            In general, the posets are not total orders, so the result is not necessarily a principal upper set.
        required:
        - "type"
    U1_Uinv_Join:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          lower_bounds:
            type: "array"
            items:
              type: "array"
              items: {}
          type:
            type: "string"
            const: "U1_Uinv_Join"
            enum:
            - "U1_Uinv_Join"
            default: "U1_Uinv_Join"
        type: "object"
        x-latex-schema:
          group: "70_misc"
        required:
        - "lower_bounds"
        - "type"
    U1_Uinv_JoinConstant:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          join1_dom:
            $ref: "#/components/schemas/Poset"
          value:
            $ref: "#/components/schemas/Value"
          type:
            type: "string"
            const: "U1_Uinv_JoinConstant"
            enum:
            - "U1_Uinv_JoinConstant"
            default: "U1_Uinv_JoinConstant"
        type: "object"
        x-latex-schema:
          group: "70_misc"
        required:
        - "join1_dom"
        - "type"
        - "value"
    U1_UnionOfPrinUpperSets:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_UnionOfPrinUpperSets"
            enum:
            - "U1_UnionOfPrinUpperSets"
            default: "U1_UnionOfPrinUpperSets"
        type: "object"
        title: "Union of principal upper sets."
        x-latex-schema:
          group: "15_poset_ops"
          description: |
            The domain $\ukdom$ must be a product poset.

            This map is defined as follows:
            %
            \begin{equation}
                \langle x_i \rangle_i \mapsto \bigcup_i \upit x_i
            \end{equation}
        required:
        - "type"
    U1_Unknown:
      allOf:
      - $ref: "#/components/schemas/U1Map"
      - properties:
          type:
            type: "string"
            const: "U1_Unknown"
            enum:
            - "U1_Unknown"
            default: "U1_Unknown"
        type: "object"
        title: "Placeholder for an unknown map."
        x-latex-schema:
          group: "10_simple"
          description: |
            This is a placeholder for a map whose type is not known.
        required:
        - "type"
    UCheck:
      allOf:
      - $ref: "#/components/schemas/Check"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            x-latex-property:
              symbol: "m"
            description: "The map to check"
          data:
            description: "Test pairs"
            type: "array"
            items:
              $ref: "#/components/schemas/UCheck_Data"
          type:
            type: "string"
            const: "UCheck"
            enum:
            - "UCheck"
            default: "UCheck"
        type: "object"
        title: "Check for a UMap."
        required:
        - "data"
        - "m"
        - "type"
    UCheck_Data:
      type: "object"
      properties:
        x: {}
        y:
          $ref: "#/components/schemas/UpperSet"
        elapsed:
          type: "number"
          description: "Time taken for the check in seconds."
      required:
      - "x"
      - "y"
      title: "An input-output pair for the UMap"
    UMap:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "Map to upper sets of resources and implementations."
        properties:
          kdom:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukdom"
              description: |
                The Kleisli domain of the map.
              color: "functionalities"
          kcod:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukcod"
              description: |
                The Kleisli codomain of the map.
              color: "requirements"
          kimp:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "\\ukimp"
              description: |
                The implementation poset of the map.
              color: "implementations"
          type:
            type: "string"
            enum:
            - "U_C_ITransform"
            - "U_C_Intersection"
            - "U_C_Parallel"
            - "U_C_RefineDomain"
            - "U_C_Series"
            - "U_C_Trace"
            - "U_C_Union"
            - "U_C_WrapUnits"
            - "U_Catalog"
            - "U_Constant"
            - "U_Identity"
            - "U_L_Lift1_Constant"
            - "U_L_Lift1_Transform"
            - "U_Unknown"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "UMap"
            enum:
            - "UMap"
            default: "UMap"
        required:
        - "kdom"
        - "kcod"
        - "kimp"
        - "type"
        - "kind"
        x-latex-schema:
          top_level: true
          order: 60
          color: "color-upper"
          preamble: |
            \newcommand{\signUMAP}[3]{#1 \stackrel{#3}{\to}_{\text{\textbf{UMAP}}} #2 }
        type: "object"
        discriminator:
          propertyName: "type"
          mapping:
            U_C_ITransform: "#/components/schemas/U_C_ITransform"
            U_C_Intersection: "#/components/schemas/U_C_Intersection"
            U_C_Parallel: "#/components/schemas/U_C_Parallel"
            U_C_RefineDomain: "#/components/schemas/U_C_RefineDomain"
            U_C_Series: "#/components/schemas/U_C_Series"
            U_C_Trace: "#/components/schemas/U_C_Trace"
            U_C_Union: "#/components/schemas/U_C_Union"
            U_C_WrapUnits: "#/components/schemas/U_C_WrapUnits"
            U_Catalog: "#/components/schemas/U_Catalog"
            U_Constant: "#/components/schemas/U_Constant"
            U_Identity: "#/components/schemas/U_Identity"
            U_L_Lift1_Constant: "#/components/schemas/U_L_Lift1_Constant"
            U_L_Lift1_Transform: "#/components/schemas/U_L_Lift1_Transform"
            U_Unknown: "#/components/schemas/U_Unknown"
    U_C_ITransform:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            x-latex-property:
              symbol: "\\umor"
              description: "The original map"
          transform:
            $ref: "#/components/schemas/MonotoneMap"
          type:
            type: "string"
            const: "U_C_ITransform"
            enum:
            - "U_C_ITransform"
            default: "U_C_ITransform"
        type: "object"
        title: "Transforms the implementation of another map."
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "m"
        - "transform"
        - "type"
    U_C_Intersection:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "U_C_Intersection"
            enum:
            - "U_C_Intersection"
            default: "U_C_Intersection"
        type: "object"
        title: "Intersection of maps"
        x-latex-schema:
          group: "20_compositions"
          order: 200
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod:
            kimp: "P_C_ProductSmash(@(ms))"
    U_C_Parallel:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "U_C_Parallel"
            enum:
            - "U_C_Parallel"
            default: "U_C_Parallel"
        type: "object"
        title: "Monoidal product"
        x-latex-schema:
          group: "20_compositions"
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_C_Product(@(ms))"
            kcod: "P_C_Product(@(ms))"
            kimp: "P_C_ProductSmash(@(ms))"
    U_C_RefineDomain:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            x-latex-property:
              symbol: "\\umor"
              description: "The original map"
          type:
            type: "string"
            const: "U_C_RefineDomain"
            enum:
            - "U_C_RefineDomain"
            default: "U_C_RefineDomain"
        type: "object"
        title: "Refines the domain of a monotone map"
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "m"
        - "type"
    U_C_Series:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "U_C_Series"
            enum:
            - "U_C_Series"
            default: "U_C_Series"
        type: "object"
        title: "Series composition"
        x-latex-schema:
          group: "20_compositions"
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "@(ms[0])"
            kcod: "@(ms[-1])"
            kimp: "P_C_ProductSmash(@(ms))"
            proj1: "U1_C_Series(@(ms))"
    U_C_Trace:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            x-latex-property:
              symbol: "\\umor"
              description: "The original map"
          m_proj:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m'"
              description: |
                The projection of the UMap $m$.
          type:
            type: "string"
            const: "U_C_Trace"
            enum:
            - "U_C_Trace"
            default: "U_C_Trace"
        type: "object"
        title: "Trace"
        x-latex-schema:
          group: "30_comp_single"
          description: |
            This is the trace of a UMap $m$.

            Because of computation convenience, we also require the projection $m'$ of the UMap $m$ to a U1Map.
        required:
        - "m"
        - "m_proj"
        - "type"
        x-mcdp-ul:
          params:
          - "m"
          functors:
            kdom: "@(m)[0]"
            kcod: "@(m)[0]"
    U_C_Union:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          ms:
            type: "array"
            items:
              $ref: "#/components/schemas/UMap"
            description: |
              Maps to be composed.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              Labels for the maps.
          type:
            type: "string"
            const: "U_C_Union"
            enum:
            - "U_C_Union"
            default: "U_C_Union"
        type: "object"
        title: "Union of maps"
        x-latex-schema:
          group: "20_compositions"
          order: 200
        required:
        - "ms"
        - "type"
        x-mcdp-ul:
          params:
          - "ms"
          functors:
            kdom: "P_F_C_Intersection(@(ms))"
            kcod: "P_F_C_Union(@(ms))"
            kimp: "P_C_ProductSmash(@(ms))"
    U_C_WrapUnits:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/UMap"
            x-latex-property:
              symbol: "\\umor"
              description: "The original map"
          kdom_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukdom$"
          kcod_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukcod$"
          kimp_units:
            $ref: "#/components/schemas/Unit"
            x-latex-property:
              description: "The units for decorating $\\ukimp$"
          type:
            type: "string"
            const: "U_C_WrapUnits"
            enum:
            - "U_C_WrapUnits"
            default: "U_C_WrapUnits"
        type: "object"
        title: "Decorates a map with units."
        x-latex-schema:
          group: "30_comp_single"
        required:
        - "kcod_units"
        - "kdom_units"
        - "kimp_units"
        - "m"
        - "type"
        x-mcdp-ul:
          params:
          - "m"
          - "kdom_units"
          - "kcod_units"
          - "kimp_units"
          functors:
            kdom: "P_C_Units(@(m), kdom_units)"
            kcod: "P_C_Units(@(m), kcod_units)"
            kimp: "P_C_Units(@(m), kimp_units)"
    U_Catalog:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          options:
            type: "array"
            description: |
              The options in the catalog.
            items:
              $ref: "#/components/schemas/U_Catalog_Options"
          type:
            type: "string"
            const: "U_Catalog"
            enum:
            - "U_Catalog"
            default: "U_Catalog"
        type: "object"
        title: "UMap for a catalog"
        x-latex-schema:
          group: "10_simple"
          order: 1000
          description: |
            This is the UMap that arises from a DP\_Catalog.
        required:
        - "options"
        - "type"
    U_Catalog_Options:
      type: "object"
      title: "An option in the catalog"
      properties:
        f:
          x-latex-property:
            symbol: "\\fun"
        r:
          x-latex-property:
            symbol: "\\rescj"
        i:
          x-latex-property:
            symbol: "\\imp"
      required:
      - "f"
      - "r"
      - "i"
      x-latex-schema:
        description: |
          An option in the catalog. 
    U_Constant:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          value:
            $ref: "#/components/schemas/UpperSet"
            description: |
              The upper set that is the value of the constant map.
          type:
            type: "string"
            const: "U_Constant"
            enum:
            - "U_Constant"
            default: "U_Constant"
        type: "object"
        title: "Constant map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
        - "value"
    U_Identity:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          type:
            type: "string"
            const: "U_Identity"
            enum:
            - "U_Identity"
            default: "U_Identity"
        type: "object"
        title: "Identity"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    U_L_Lift1_Constant:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "\\umor"
              description: |
                A U1Map morphism.
          value:
            x-latex-property:
              symbol: "v"
              description: |
                A constant value for the implementation of the U1Map morphism.
          type:
            type: "string"
            const: "U_L_Lift1_Constant"
            enum:
            - "U_L_Lift1_Constant"
            default: "U_L_Lift1_Constant"
        type: "object"
        title: "Lifts a U1Map morphism with a constant value for the implementation."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "type"
        - "value"
    U_L_Lift1_Transform:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          m:
            $ref: "#/components/schemas/U1Map"
            x-latex-property:
              symbol: "m"
              description: |
                A U1Map morphism.
          transform:
            $ref: "#/components/schemas/MonotoneMap"
            x-latex-property:
              symbol: "f"
              description: |
                A function that computes the implementation of the U1Map morphism.
          type:
            type: "string"
            const: "U_L_Lift1_Transform"
            enum:
            - "U_L_Lift1_Transform"
            default: "U_L_Lift1_Transform"
        type: "object"
        title: "Lifts a U1Map morphism with a function to compute the implementation."
        x-latex-schema:
          group: "40_lift"
        required:
        - "m"
        - "transform"
        - "type"
    U_Unknown:
      allOf:
      - $ref: "#/components/schemas/UMap"
      - properties:
          type:
            type: "string"
            const: "U_Unknown"
            enum:
            - "U_Unknown"
            default: "U_Unknown"
        type: "object"
        title: "Placeholder for an unknown map"
        x-latex-schema:
          group: "10_simple"
        required:
        - "type"
    Unit:
      properties:
        kind:
          type: "string"
          const: "Unit"
          default: "Unit"
          enum:
          - "Unit"
          description: "Kind marker."
        description:
          type: "string"
          description: "A human-readable description of the unit (debug purposes)."
        type:
          type: "string"
          enum:
          - "Unit_None"
          - "Unit_Single"
          - "Unit_Vector"
          - "Unit_Wrapped"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          Unit_None: "#/components/schemas/Unit_None"
          Unit_Single: "#/components/schemas/Unit_Single"
          Unit_Vector: "#/components/schemas/Unit_Vector"
          Unit_Wrapped: "#/components/schemas/Unit_Wrapped"
      title: "Units specifications"
      x-latex-schema:
        top_level: false
      required:
      - "kind"
      - "type"
    Unit_None:
      allOf:
      - $ref: "#/components/schemas/Unit"
      - properties:
          type:
            type: "string"
            const: "Unit_None"
            enum:
            - "Unit_None"
            default: "Unit_None"
        type: "object"
        title: "Represents the absence of units."
        required:
        - "type"
    Unit_Single:
      allOf:
      - $ref: "#/components/schemas/Unit"
      - properties:
          units:
            type: "string"
            examples:
            - "m"
            - "m/s^2"
            description: |
              A string representing the unit.
          type:
            type: "string"
            const: "Unit_Single"
            enum:
            - "Unit_Single"
            default: "Unit_Single"
        type: "object"
        title: |
          A simple unit.
        required:
        - "type"
        - "units"
    Unit_Vector:
      allOf:
      - $ref: "#/components/schemas/Unit"
      - properties:
          subs:
            type: "array"
            items:
              $ref: "#/components/schemas/Unit"
            description: |
              The subunits.
          labels:
            type: "array"
            items:
              type: "string"
            description: |
              labels for the subunits
          type:
            type: "string"
            const: "Unit_Vector"
            enum:
            - "Unit_Vector"
            default: "Unit_Vector"
        type: "object"
        title: "A vector of units for a product of posets."
        required:
        - "subs"
        - "type"
    Unit_Wrapped:
      allOf:
      - $ref: "#/components/schemas/Unit"
      - properties:
          name:
            type: "string"
          inside:
            type: "array"
            items:
              $ref: "#/components/schemas/Unit"
          shape: {}
          type:
            type: "string"
            const: "Unit_Wrapped"
            enum:
            - "Unit_Wrapped"
            default: "Unit_Wrapped"
        type: "object"
        title: |
          A special type of unit that is used to describe the units of composite types.
        required:
        - "inside"
        - "name"
        - "shape"
        - "type"
    UpperSet:
      properties:
        kind:
          type: "string"
          const: "UpperSet"
          enum:
          - "UpperSet"
          description: "Kind marker."
        type:
          type: "string"
          enum:
          - "UpperSet_Unused"
          - "UpperSet_UpperClosure"
          description: "Discriminator variable to distinguish subtypes."
      type: "object"
      discriminator:
        propertyName: "type"
        mapping:
          UpperSet_Unused: "#/components/schemas/UpperSet_Unused"
          UpperSet_UpperClosure: "#/components/schemas/UpperSet_UpperClosure"
      title: "Upper sets"
      x-latex-schema:
        top_level: false
      required:
      - "kind"
      - "type"
    UpperSet_Unused:
      allOf:
      - $ref: "#/components/schemas/UpperSet"
      - properties:
          type:
            type: "string"
            const: "UpperSet_Unused"
            enum:
            - "UpperSet_Unused"
            default: "UpperSet_Unused"
        type: "object"
        title: "Unused"
        x-latex-schema:
          excluded: true
        required:
        - "type"
    UpperSet_UpperClosure:
      allOf:
      - $ref: "#/components/schemas/UpperSet"
      - properties:
          points:
            type: "array"
            items: {}
            description: "The points in the set."
          type:
            type: "string"
            const: "UpperSet_UpperClosure"
            enum:
            - "UpperSet_UpperClosure"
            default: "UpperSet_UpperClosure"
        type: "object"
        title: "An upper set defined as the up closure of a finite set of points."
        required:
        - "points"
        - "type"
    VU:
      allOf:
      - $ref: "#/components/schemas/Value"
      - properties:
          poset:
            $ref: "#/components/schemas/Poset"
            x-latex-property:
              symbol: "P"
              description: |
                The poset to which the value belongs.
          value:
            x-latex-property:
              symbol: "v \\in P"
              description: |
                The value that belongs to the poset.
          type:
            type: "string"
            const: "VU"
            enum:
            - "VU"
            default: "VU"
        type: "object"
        title: "A (poset, value) pair."
        required:
        - "poset"
        - "type"
        - "value"
    Value:
      allOf:
      - $ref: "#/components/schemas/Root"
      - title: "A typed value"
        properties:
          address:
            $ref: "#/components/schemas/Address"
            description: "Pointer to the entity that generated this object."
          type:
            type: "string"
            enum:
            - "VU"
            description: "Discriminator variable to distinguish subtypes."
          kind:
            type: "string"
            const: "Value"
            enum:
            - "Value"
            default: "Value"
        x-latex-schema:
          top_level: true
          order: 180
          description: |
            A typed value is a value that has a type (poset).
            This is used in contexts where the poset to which the value belongs is not clear from the context.
        type: "object"
        required:
        - "type"
        - "kind"
        discriminator:
          propertyName: "type"
          mapping:
            VU: "#/components/schemas/VU"
# autogenerated
